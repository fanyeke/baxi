actions:
  create_followup_task:
    name: create_followup_task
    description: "创建跟进任务"
    risk_level: medium
    requires_approval: true
    allowed_by: [analyst, manager]
    channels: [feishu]

  notify_owner:
    name: notify_owner
    description: "通知负责人处理异常"
    risk_level: medium
    requires_approval: true
    allowed_by: [analyst]
    channels: [feishu, email]

  export_report:
    name: export_report
    description: "导出报告"
    risk_level: medium
    requires_approval: true
    allowed_by: [analyst, manager]
    channels: [feishu, file]

  create_outbox_message:
    name: create_outbox_message
    description: "创建出站消息"
    risk_level: medium
    requires_approval: true
    allowed_by: [manager]
    channels: [feishu, webhook]
