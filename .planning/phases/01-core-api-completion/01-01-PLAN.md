---
phase: 01-core-api-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/handler/decision.go
  - internal/api/dto/decision.go
autonomous: true
requirements:
  - API-01
  - API-04
  - API-05

must_haves:
  truths:
    - POST /api/v1/decisions/cases/{case_id}/decide/llm returns 200 with decision body
    - GET /api/v1/decisions/cases/{case_id}/llm-decisions returns 200 with LLM decision history list
    - GET /api/v1/decisions/cases/{case_id}/evals returns 200 with evaluation results list
    - All three endpoints replace their 501 stubs with working implementations
  artifacts:
    - path: internal/api/handler/decision.go
      provides: Handler implementations for DecideLLM, ListLLMDecisions, ListEvals
      exports:
        - "func (h *DecisionHandler) DecideLLM"
        - "func (h *DecisionHandler) ListLLMDecisions"
        - "func (h *DecisionHandler) ListEvals"
    - path: internal/api/dto/decision.go
      provides: DTO types for LLM decision and eval responses
      contains:
        - "type LLMDecisionItem struct"
        - "type EvalItem struct"
  key_links:
    - from: "internal/api/handler/decision.go:DecideLLM"
      to: "internal/service/decision_service.go:DecideLLM"
      via: "h.svc.DecideLLM() call"
    - from: "internal/api/handler/decision.go:ListLLMDecisions"
      to: "internal/service/decision_service.go:ListLLMDecisions"
      via: "h.svc.ListLLMDecisions() call"
    - from: "internal/api/handler/decision.go:ListEvals"
      to: "internal/service/decision_service.go:ListEvals"
      via: "h.svc.ListEvals() call"
---

<objective>
Implement three decision endpoints currently returning 501: DecideLLM, ListLLMDecisions, and ListEvals.

Purpose: Replace stubs with working handlers per D-01 (DecideLLM proxies to Decide) and D-05 (list endpoints reuse service methods). These are the simplest endpoints to implement and unblock frontend expectations.
Output: Working handlers with proper DTO mapping.
</objective>

<execution_context>
@/home/zzz/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/zzz/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-api-completion/01-CONTEXT.md

@internal/api/handler/decision.go
@internal/api/dto/decision.go
@internal/service/decision_service.go
@internal/api/handler/error_helper.go
@internal/httputil/response.go
@internal/httputil/pagination.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend DecisionService interface and add DTO types</name>
  <files>internal/api/handler/decision.go, internal/api/dto/decision.go</files>
  <read_first>
    internal/api/handler/decision.go
    internal/api/dto/decision.go
    internal/service/decision_service.go
  </read_first>
  <action>
    1. In internal/api/handler/decision.go, extend the DecisionService interface (lines 22-30) by adding three method signatures:
       - DecideLLM(ctx context.Context, caseID string) (*decision.DecisionContext, *llm.DecisionOutput, []action.ActionProposal, error)
       - ListLLMDecisions(ctx context.Context, caseID string) (interface{}, error)
       - ListEvals(ctx context.Context, caseID string) (interface{}, error)
    2. In internal/api/dto/decision.go, add two new DTO structs for list responses:
       - LLMDecisionItem with fields: DecisionID string, CaseID string, Provider string, Model string, Confidence float64, ValidationStatus string, FallbackUsed bool, OutputJSON json.RawMessage, CreatedAt string — all with json tags matching field names in snake_case
       - EvalItem with fields: EvalID string, LLMDecisionID string, CaseID string, EvalRuleID string, EvalStatus string, Score float64, DetailsJSON json.RawMessage, CreatedAt string — all with json tags matching field names in snake_case
    3. Add wrapper list response types:
       - LLMDecisionListResponse with Items []LLMDecisionItem and Total int
       - EvalListResponse with Items []EvalItem and Total int
    4. Import "encoding/json" in dto/decision.go if not already present.
  </action>
  <verify>
    <automated>go build ./internal/api/handler/ && go build ./internal/api/dto/</automated>
  </verify>
  <done>
    - DecisionService interface contains DecideLLM, ListLLMDecisions, ListEvals signatures
    - dto/decision.go contains LLMDecisionItem, EvalItem, LLMDecisionListResponse, EvalListResponse types
    - Code compiles without errors
  </done>
  <acceptance_criteria>
    - internal/api/handler/decision.go contains "DecideLLM(ctx context.Context, caseID string)" in the DecisionService interface
    - internal/api/handler/decision.go contains "ListLLMDecisions(ctx context.Context, caseID string)" in the DecisionService interface
    - internal/api/handler/decision.go contains "ListEvals(ctx context.Context, caseID string)" in the DecisionService interface
    - internal/api/dto/decision.go contains "type LLMDecisionItem struct"
    - internal/api/dto/decision.go contains "type EvalItem struct"
    - go build ./internal/api/... exits with code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement DecideLLM, ListLLMDecisions, and ListEvals handlers</name>
  <files>internal/api/handler/decision.go</files>
  <read_first>
    internal/api/handler/decision.go
    internal/api/handler/error_helper.go
    internal/service/decision_service.go
  </read_first>
  <action>
    1. Implement DecideLLM handler (replaces the 501 stub at line 266-268):
       - Extract case_id from chi.URLParam(r, "case_id")
       - Call h.svc.DecideLLM(r.Context(), caseID)
       - Handle errors: pgx.ErrNoRows → 404, other errors → 500
       - On success, return dto.DecisionResponse with DecisionCaseID=decCtx.DecisionCaseID, Status="decision_generated", Decision=structToMap(output), Proposals=proposalsToDTO(proposals)
       - Use httputil.JSON(w, http.StatusOK, resp)

    2. Implement ListLLMDecisions handler (replaces the 501 stub at line 281-283):
       - Extract case_id from chi.URLParam(r, "case_id")
       - Call h.svc.ListLLMDecisions(r.Context(), caseID)
       - Handle errors: 500 on any error
       - On success, the service returns []llmDecisionRow (anonymous type with json tags). Return directly as JSON array using httputil.JSON(w, http.StatusOK, result)
       - Do NOT attempt to map to dto types — the service's anonymous struct already has correct json tags

    3. Implement ListEvals handler (replaces the 501 stub at line 286-288):
       - Extract case_id from chi.URLParam(r, "case_id")
       - Call h.svc.ListEvals(r.Context(), caseID)
       - Handle errors: 500 on any error
       - On success, return the result directly as JSON using httputil.JSON(w, http.StatusOK, result)
       - Same approach as ListLLMDecisions

    4. Follow the exact error handling pattern used in existing handlers (Decide, ListCases, etc.):
       - Use writeError(w, r, status, middleware.XXX, "message")
       - Check errors.Is(err, pgx.ErrNoRows) for 404 cases
  </action>
  <verify>
    <automated>go build ./internal/api/handler/ && go vet ./internal/api/handler/</automated>
  </verify>
  <done>
    - DecideLLM handler returns 200 with DecisionResponse body instead of 501
    - ListLLMDecisions handler returns 200 with JSON array instead of 501
    - ListEvals handler returns 200 with JSON array instead of 501
    - All handlers follow existing error handling patterns
    - go build and go vet pass
  </done>
  <acceptance_criteria>
    - internal/api/handler/decision.go contains "func (h *DecisionHandler) DecideLLM" with implementation body (not just writeError 501)
    - internal/api/handler/decision.go contains "func (h *DecisionHandler) ListLLMDecisions" with implementation body
    - internal/api/handler/decision.go contains "func (h *DecisionHandler) ListEvals" with implementation body
    - go build ./internal/api/handler/ exits with code 0
    - go vet ./internal/api/handler/ exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted HTTP requests with bearer token validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | ListLLMDecisions/ListEvals | accept | Response contains internal decision data; protected by auth middleware |
| T-01-02 | Denial of Service | List endpoints | mitigate | Use service-layer query (no unbounded results); pagination not required per D-05 |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; slopcheck not triggered |
</threat_model>

<verification>
- `go test ./internal/api/handler/...` passes (existing tests still work)
- `go build ./cmd/baxi-api` compiles successfully
- Verify each endpoint responds with 200 (not 501) via curl or API client
</verification>

<success_criteria>
- POST /api/v1/decisions/cases/{case_id}/decide/llm returns 200 with valid decision body
- GET /api/v1/decisions/cases/{case_id}/llm-decisions returns 200 with array of LLM decisions
- GET /api/v1/decisions/cases/{case_id}/evals returns 200 with array of eval results
- All endpoints follow existing handler patterns (error handling, DTO mapping, JSON response)
</success_criteria>

<output>
Create `.planning/phases/01-core-api-completion/01-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**New struct types (DTOs):**
- `dto.LLMDecisionItem` — fields: DecisionID, CaseID, Provider, Model, Confidence, ValidationStatus, FallbackUsed, OutputJSON, CreatedAt
- `dto.EvalItem` — fields: EvalID, LLMDecisionID, CaseID, EvalRuleID, EvalStatus, Score, DetailsJSON, CreatedAt
- `dto.LLMDecisionListResponse` — fields: Items []LLMDecisionItem, Total int
- `dto.EvalListResponse` — fields: Items []EvalItem, Total int

**New handler methods:**
- `(*DecisionHandler).DecideLLM(http.ResponseWriter, *http.Request)` — proxies to svc.DecideLLM
- `(*DecisionHandler).ListLLMDecisions(http.ResponseWriter, *http.Request)` — returns LLM decision history
- `(*DecisionHandler).ListEvals(http.ResponseWriter, *http.Request)` — returns eval results

**Extended interfaces:**
- `handler.DecisionService` — adds DecideLLM, ListLLMDecisions, ListEvals method signatures

**Modified files:**
- `internal/api/handler/decision.go`
- `internal/api/dto/decision.go`
