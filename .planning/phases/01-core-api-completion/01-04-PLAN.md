---
phase: 01-core-api-completion
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - docs/openapi.yml
autonomous: true
requirements:
  - API-07

must_haves:
  truths:
    - An OpenAPI 3.0 specification file exists documenting all 6 newly implemented endpoints
    - Each endpoint has documented request body schema, response schema, and error responses
    - Response schemas match the actual DTO structures used in handlers
  artifacts:
    - path: docs/openapi.yml
      provides: OpenAPI 3.0 specification for Phase 1 endpoints
      min_lines: 200
  key_links:
    - from: "docs/openapi.yml"
      to: "internal/api/dto/decision.go"
      via: "DTO schemas documented in OpenAPI components"
    - from: "docs/openapi.yml"
      to: "internal/api/dto/outbox.go"
      via: "Outbox DTO schemas documented in OpenAPI components"
---

<objective>
Create OpenAPI 3.0 documentation for all 6 endpoints implemented in Phase 1, ensuring response schemas match actual handler DTOs.

Purpose: Fulfill API-07 requirement that all endpoints return OpenAPI-documented response schemas. This serves as the contract between backend and frontend.
Output: docs/openapi.yml with complete endpoint specifications.
</objective>

<execution_context>
@/home/zzz/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/zzz/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-api-completion/01-CONTEXT.md

@internal/api/routes.go
@internal/api/dto/decision.go
@internal/api/dto/outbox.go
@internal/api/handler/decision.go
@internal/api/handler/outbox.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OpenAPI 3.0 specification for Phase 1 endpoints</name>
  <files>docs/openapi.yml</files>
  <read_first>
    internal/api/routes.go
    internal/api/dto/decision.go
    internal/api/dto/outbox.go
    internal/api/handler/decision.go
    internal/api/handler/outbox.go
  </read_first>
  <action>
    Create docs/openapi.yml with OpenAPI 3.0.3 specification covering:

    1. Document the following endpoints with full request/response schemas:
       - POST /api/v1/decisions/cases/{case_id}/decide/llm
       - POST /api/v1/decisions/cases/{case_id}/compare
       - POST /api/v1/decisions/cases/{case_id}/replay
       - GET /api/v1/decisions/cases/{case_id}/llm-decisions
       - GET /api/v1/decisions/cases/{case_id}/evals
       - POST /api/v1/outbox/dispatch

    2. For each endpoint document:
       - Operation summary and description
       - Path parameters (case_id where applicable)
       - Request body schema (for POST endpoints) matching dto structs:
         - Compare: no request body (uses case_id from path)
         - Replay: ReplayRequest schema with dry_run, model, temperature, context_overrides
         - BatchDispatch: BatchDispatchRequest schema with dry_run
       - Response 200 schema matching actual DTOs:
         - DecideLLM: DecisionResponse (reuse existing schema from Decide endpoint)
         - Compare: CompareResponse with added, removed, changed arrays and metadata
         - Replay: ReplayResponse with original_decision, replayed_decision, diff, context_hash, model, dry_run
         - ListLLMDecisions: array of LLMDecisionItem objects
         - ListEvals: array of EvalItem objects
         - BatchDispatch: BatchDispatchResponse with dispatched, failed, event_ids, dry_run
       - Error responses: 400, 404, 500, 503 (for Replay when service not configured)

    3. Define reusable components/schemas section with:
       - DecisionResponse (existing, reference from Decide endpoint)
       - DecisionCaseResponse (existing)
       - ProposalItem (existing)
       - LLMDecisionItem (new)
       - EvalItem (new)
       - CompareResponse (new)
       - DiffItem (new)
       - ReplayRequest (new)
       - ReplayResponse (new)
       - ReplayDiff (new)
       - BatchDispatchRequest (new)
       - BatchDispatchResponse (new)
       - ErrorResponse (standard 5-field error: request_id, error_code, message, diagnosis, suggested_action)

    4. Include security scheme: Bearer token (API_BEARER_TOKEN)

    5. Use YAML format, not JSON. Follow OpenAPI 3.0.3 specification.

    6. Do NOT document endpoints outside Phase 1 scope (existing working endpoints like CreateCase, GetCase, etc. are optional — focus on the 6 new ones).
  </action>
  <verify>
    <automated>test -f docs/openapi.yml && grep -c "openapi: 3.0" docs/openapi.yml | grep -q "1"</automated>
  </verify>
  <done>
    - docs/openapi.yml exists and is valid YAML
    - File contains OpenAPI 3.0.3 header
    - All 6 Phase 1 endpoints are documented
    - Schemas match actual DTO structures from internal/api/dto/
    - Security scheme (Bearer) is documented
  </done>
  <acceptance_criteria>
    - docs/openapi.yml exists
    - docs/openapi.yml contains "openapi: 3.0" version declaration
    - docs/openapi.yml contains paths for all 6 endpoints: /decisions/cases/{case_id}/decide/llm, /decisions/cases/{case_id}/compare, /decisions/cases/{case_id}/replay, /decisions/cases/{case_id}/llm-decisions, /decisions/cases/{case_id}/evals, /outbox/dispatch
    - docs/openapi.yml contains schemas for LLMDecisionItem, EvalItem, CompareResponse, ReplayResponse, BatchDispatchResponse
    - docs/openapi.yml contains ErrorResponse schema with request_id, error_code, message, diagnosis, suggested_action fields
    - docs/openapi.yml contains securitySchemes with bearerAuth
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries in documentation. This plan produces documentation only.
</threat_model>

<verification>
- docs/openapi.yml is valid YAML
- File contains all 6 endpoint specifications
- Schemas align with actual DTO types in internal/api/dto/
</verification>

<success_criteria>
- docs/openapi.yml exists with complete OpenAPI 3.0 specification
- All 6 Phase 1 endpoints have documented request/response schemas
- Error responses (400, 404, 500, 503) are documented for each endpoint
- Security scheme (Bearer token) is documented
- Schema definitions match actual handler DTO structures
</success_criteria>

<output>
Create `.planning/phases/01-core-api-completion/01-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**New file:**
- `docs/openapi.yml` — OpenAPI 3.0.3 specification documenting all Phase 1 endpoints

**OpenAPI schemas defined (components):**
- `DecisionResponse` — decision_case_id, status, decision, proposals
- `LLMDecisionItem` — decision_id, case_id, provider, model, confidence, validation_status, fallback_used, output_json, created_at
- `EvalItem` — eval_id, llm_decision_id, decision_case_id, eval_rule_id, eval_status, score, details_json, created_at
- `CompareResponse` — decision_case_id, added, removed, changed, metadata
- `DiffItem` — field, before, after, change_type
- `ReplayRequest` — dry_run, model, temperature, context_overrides
- `ReplayResponse` — original_decision, replayed_decision, diff, context_hash, model, dry_run
- `ReplayDiff` — decision_type_match, severity_match, confidence_diff, action_overlap, summary_changed, rationale_changed
- `BatchDispatchRequest` — dry_run
- `BatchDispatchResponse` — dry_run, dispatched, failed, event_ids
- `ErrorResponse` — request_id, error_code, message, diagnosis, suggested_action
