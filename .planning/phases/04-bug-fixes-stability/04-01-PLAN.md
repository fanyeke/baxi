---
phase: 04-bug-fixes-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/015_intentionally_skipped.sql
  - migrations/025_intentionally_skipped.sql
  - internal/feishu/client.go
autonomous: true
requirements: [BUG-03, BUG-04]
must_haves:
  truths:
    - "Goose migration 015 和 025 存在占位文件，goose up/down 不会报错"
    - "Feishu page_token 类型断言失败时记录日志并中断分页而非静默使用空值"
    - "Feishu 分页中断后返回已获取的部分数据，调用方不会得到错误"
  artifacts:
    - path: "migrations/015_intentionally_skipped.sql"
      provides: "迁移 015 占位文件，注释说明编号被跳过"
      contains: "+goose Up"
    - path: "migrations/025_intentionally_skipped.sql"
      provides: "迁移 025 占位文件，注释说明编号被跳过"
      contains: "+goose Up"
    - path: "internal/feishu/client.go"
      provides: "Client 结构体增加 logger 字段，page_token 类型断言增加防御性检查"
      exports: ["*Client.SetLogger"]
  key_links:
    - from: "internal/feishu/client.go:124"
      to: "data[\"page_token\"]"
      via: "type assertion with bool check"
      pattern: "pageToken, pageTokenOk = data\\[\"page_token\"\\]\\.\\(string\\)"
---

<objective>
**创建迁移占位文件 + 修复飞书 page_token 类型断言静默失败**

**目的:**
- BUG-04: Goose 迁移序列从 014 跳至 016、从 024 跳至 026，不存在 015 或 025。虽然 Goose 对缺口不敏感，但容易引发审计问题。添加空占位文件记录编号被有意跳过。
- BUG-03: `internal/feishu/client.go` 在 `ListRecords` 分页循环中，`data["page_token"].(string)` 类型断言失败时静默将 `pageToken` 设为空字符串，导致循环重复请求同一页。需要记录日志并中断分页。

**输出:**
- `migrations/015_intentionally_skipped.sql` + `migrations/025_intentionally_skipped.sql`
- `internal/feishu/client.go` — 修正后的 Client 结构体（含 zap.Logger 字段）和 ListRecords 方法
</objective>

<execution_context>
@/home/zzz/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/zzz/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-bug-fixes-stability/04-CONTEXT.md
@internal/feishu/client.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建迁移占位文件 015 和 025</name>
  <files>
    migrations/015_intentionally_skipped.sql
    migrations/025_intentionally_skipped.sql
  </files>
  <read_first>
    migrations/014_add_outbox_next_retry_at.sql
    migrations/016_config_versions.sql
    migrations/024_context_hash_proposal.sql
    migrations/026_action_outcome.sql
  </read_first>
  <action>
    创建两个空占位迁移文件。每个文件包含标准的 goose 标记：
    - `-- +goose Up` 和 `-- +goose Down` 块（见 `024_context_hash_proposal.sql` 的简洁格式）
    - Up/Down 块内各一行注释 `-- Migration {NUM}: intentionally skipped — no content was removed.`
    - 不使用 `+goose StatementBegin`/`+goose StatementEnd` 包裹——用简洁格式（如 024 风格）
    文件命名：
    - `migrations/015_intentionally_skipped.sql` — Up/Down 中注释写 `-- Migration 015: intentionally skipped — no content was removed.`
    - `migrations/025_intentionally_skipped.sql` — Up/Down 中注释写 `-- Migration 025: intentionally skipped — no content was removed.`
  </action>
  <verify>
    <automated>
      test -f migrations/015_intentionally_skipped.sql &amp;&amp; test -f migrations/025_intentionally_skipped.sql &amp;&amp; grep -q '+goose Up' migrations/015_intentionally_skipped.sql &amp;&amp; grep -q '+goose Down' migrations/025_intentionally_skipped.sql
    </automated>
  </verify>
  <acceptance_criteria>
    - 文件 `migrations/015_intentionally_skipped.sql` 存在，包含 `-- +goose Up` 和 `-- +goose Down`
    - 文件 `migrations/025_intentionally_skipped.sql` 存在，包含 `-- +goose Up` 和 `-- +goose Down`
    - 两个文件都包含注释 `intentionally skipped`
    - `go build ./...` 不受影响（纯 SQL 文件）
  </acceptance_criteria>
  <done>
    两个占位迁移文件创建完成，goose 兼容格式
  </done>
</task>

<task type="auto">
  <name>Task 2: 修复 feishu/client.go page_token 类型断言失败</name>
  <files>internal/feishu/client.go</files>
  <read_first>
    internal/feishu/client.go
    internal/logger/logger.go
  </read_first>
  <action>
    对 `internal/feishu/client.go` 做以下修改（per D-04, D-05）：

    **1. 导入 zap**
      - 在 import 块中添加 `"go.uber.org/zap"`

    **2. 给 Client 结构体添加 logger 字段**
      - 在 `Client` 结构体（第 28-37 行）中添加字段 `logger *zap.Logger`
      - 添加 setter 方法 `func (c *Client) SetLogger(l *zap.Logger) { c.logger = l }`
      - 不修改 `NewClient` 签名——使用 setter 注入

    **3. 修复 page_token 类型断言（第 124 行）**
      原代码：
      ```go
      pageToken, _ = data["page_token"].(string)
      ```
      改为（不包含代码块，以下为修正描述）：
      - 使用双返回值类型断言：`pageToken, pageTokenOk = data["page_token"].(string)`
      - 当 `!pageTokenOk` 时：
        - 如果 `c.logger != nil`，调用 `c.logger.Error("feishu page_token type assertion failed, stopping pagination", zap.String("table_id", tableID))`
        - 执行 `break` 退出分页循环
      - 当断言成功时，行为不变

    **4. 对照 D-05 确认**
      - 不返回错误给调用方——return allRecords, nil
      - 调用方获得已获取的部分数据

    不修改 `client_test.go` 或 `client_http_test.go`——现有测试不应受影响。
  </action>
  <verify>
    <automated>go build ./internal/feishu/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/feishu/...` 编译通过
    - `internal/feishu/client.go` 的 import 中包含 `"go.uber.org/zap"`
    - `Client` 结构体包含 `logger *zap.Logger` 字段
    - `Client` 上存在 `SetLogger` 方法
    - `data["page_token"].(string)` 类型断言使用双返回值检查
    - 断言失败时调用 `c.logger.Error()` 并 `break`
    - 函数签名仍返回 `([]map[string]any, error)`——错误情况只记录日志不返回错误
  </acceptance_criteria>
  <done>
    Client 结构体新增 logger 字段和 SetLogger 方法，page_token 类型断言有防御性检查和日志
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go build system | 添加新导入和字段 |
| Feishu API 响应解析 | 外部不可信 JSON 响应被类型断言 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | go.uber.org/zap | mitigate | 依赖已在 go.mod 中，无需新安装。无需包合法性审计。 |
| T-04-02 | Denial of Service | feishu/client.go 分页循环 | mitigate | 类型断言失败时 break 退出循环，避免静默无限循环（原 bug） |
| T-04-SC | Tampering | 无新包安装 | accept | 本 plan 不安装任何新依赖包 |
</threat_model>

<verification>
- `go build ./internal/feishu/...` 编译通过
- `ls migrations/015_intentionally_skipped.sql migrations/025_intentionally_skipped.sql` 两个文件存在
</verification>

<success_criteria>
1. 迁移目录下存在 015 和 025 占位文件，包含 goose 标记和跳过说明
2. feishu client 导入 `go.uber.org/zap`，Client 结构体有 `logger` 字段和 `SetLogger` 方法
3. page_token 类型断言使用双返回值检查，失败时记录错误并 break
4. 分页中断时返回已获取的部分数据，不返回错误
</success_criteria>

<output>
创建 `.planning/phases/04-bug-fixes-stability/04-01-SUMMARY.md` 执行完成后写入
</output>
