---
phase: 06-integration-end-to-end-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/action/proposal_service_test.go
  - internal/api/handler/outbox_test.go
  - internal/decision/context_builder_test.go
  - internal/service/alert_service_test.go
autonomous: true
requirements: [INT-02, INT-03]
user_setup: []
must_haves:
  truths:
    - "go vet ./internal/...  passes with no errors"
    - "4 Go test files compile cleanly with no type/naming errors"
    - "Pool parameter removed from all mock method signatures that don't match production interfaces"
    - "Type references updated from flat repository package to correct subpackage"
  artifacts:
    - path: internal/action/proposal_service_test.go
      provides: "Compiling test for ProposalService"
      min_lines: 760
    - path: internal/api/handler/outbox_test.go
      provides: "Compiling test for OutboxHandler"
      min_lines: 600
    - path: internal/decision/context_builder_test.go
      provides: "Compiling test for ContextBuilder"
      min_lines: 515
    - path: internal/service/alert_service_test.go
      provides: "Compiling test for AlertService"
      min_lines: 170
  key_links:
    - from: internal/action/proposal_service_test.go
      to: internal/action/proposal_service.go
      via: "mock implements ProposalRepository interface"
      pattern: "type ProposalRepository interface"
    - from: internal/decision/context_builder_test.go
      to: internal/decision/context_builder.go
      via: "mock implements DecisionCaseDataProvider interface"
      pattern: "type DecisionCaseDataProvider interface"
---

<objective>
修复 4 个 Go 测试文件的编译错误，使其通过 `go vet ./internal/...`。这些错误源自 Phase 3 仓库子包重构后残留的陈旧引用。

Purpose: 解除 INT-02 (E2E 集成测试) 和 INT-03 (安全 E2E 测试) 的编译阻塞。所有逻辑保持不变，只修复类型引用和构造函数签名。

Output: 4 个 Go 测试文件可通过编译
</objective>

<execution_context>
@/home/zzz/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/zzz/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-integration-end-to-end-demo/06-RESEARCH.md
@.planning/phases/06-integration-end-to-end-demo/06-PATTERNS.md

# 参考实现 - 生产接口定义（测试必须匹配的签名）
@internal/action/proposal_service.go
@internal/decision/context_builder.go
@internal/service/outbox_service.go
@internal/service/alert_service.go
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 修复 proposal_service_test.go — 移除重复导入 + pool 参数对齐接口签名</name>
  <files>internal/action/proposal_service_test.go</files>
  <read_first>
    - internal/action/proposal_service_test.go（整个文件，764行）
    - internal/action/proposal_service.go（第34-42行 — ProposalRepository 和 CaseStatusUpdater 接口定义）
  </read_first>
  <action>
    对 internal/action/proposal_service_test.go 执行以下所有修改：

    1. **删除重复导入**（第11行）：删除整行 `decisionRepo "baxi/internal/repository/decision"`（重复）。

    2. **修复 mockProposalRepo struct 类型字段（第20行）**：将 `createProposalFn` 签名中的 `pool *pgxpool.Pool` 移除，使其匹配 `ProposalRepository` 接口：
       - 旧：`createProposalFn func(ctx context.Context, pool *pgxpool.Pool, row *decisionRepo.ActionProposalRow) error`
       - 新：`createProposalFn func(ctx context.Context, row *decisionRepo.ActionProposalRow) error`

    3. **修复 mockProposalRepo.CreateProposal 方法体（第25行）**：移除对 `pool` 的引用：
       - 旧：`return m.createProposalFn(ctx, pool, row)`
       - 新：`return m.createProposalFn(ctx, row)`

    4. **修复 mockProposalRepo.ListProposalsByCase 方法体（第29行）**：移除对 `pool` 的引用：
       - 旧：`return m.listProposalsByCaseFn(ctx, pool, caseID)`
       - 新：`return m.listProposalsByCaseFn(ctx, caseID)`

    5. **修复 mockCaseUpdater struct 类型字段（第33行）**：将 `updateCaseStatusFn` 签名中的 `pool *pgxpool.Pool` 移除：
       - 旧：`updateCaseStatusFn func(ctx context.Context, pool *pgxpool.Pool, caseID string, status string, contextJSON *json.RawMessage, contextHash *string, governanceSnapshot *json.RawMessage) error`
       - 新：`updateCaseStatusFn func(ctx context.Context, caseID string, status string, contextJSON *json.RawMessage, contextHash *string, governanceSnapshot *json.RawMessage) error`

    6. **修复 mockCaseUpdater.UpdateCaseStatus 方法体（第37行）**：移除对 `pool` 的引用：
       - 旧：`return m.updateCaseStatusFn(ctx, pool, caseID, status, contextJSON, contextHash, governanceSnapshot)`
       - 新：`return m.updateCaseStatusFn(ctx, caseID, status, contextJSON, contextHash, governanceSnapshot)`

    7. **修复所有内联 lambda 定义中的 pool 参数（共12处）**：所有 `createProposalFn` 和 `updateCaseStatusFn` 的内联 lambda 都从参数中移除 `pool *pgxpool.Pool`。
       具体位置（按行号 - 约数，需逐行确认）：
       - `createProposalFn` lambda: 第54行(`func(ctx context.Context, row...)` - 已无 pool，检查不变)
       - `updateCaseStatusFn` lambda: 第61行 — 移除 `pool *pgxpool.Pool, ` 参数
       - `updateCaseStatusFn` lambda: 第148行 — 同上
       - `updateCaseStatusFn` lambda: 第185行 — 同上
       - `updateCaseStatusFn` lambda: 第221行 — 同上
       - `updateCaseStatusFn` lambda: 第256行 — 同上
       - `updateCaseStatusFn` lambda: 第403行 — 同上
       - `updateCaseStatusFn` lambda: 第500行 — 同上
       - `updateCaseStatusFn` lambda: 第619行 — 同上
       - `listProposalsByCaseFn` lambda: 第319行 — 移除 `pool *pgxpool.Pool, ` 参数
       - `listProposalsByCaseFn` lambda: 第351行 — 已无 pool，检查不变
       - `createProposalFn` lambda: 第487行 — 移除 `pool *pgxpool.Pool, ` 参数
       - `createProposalFn` lambda: 第605行 — 移除 `pool *pgxpool.Pool, ` 参数

    8. **注意不要移除 pgxpool 导入**：TestLLMDecision_CreateAndRetrieve 仍使用 `*pgxpool.Pool`（通过 setupApplyServiceTestDB 返回类型），所以 `"github.com/jackc/pgx/v5/pgxpool"` 导入必须保留。
  </action>
  <verify>
    <automated>go vet ./internal/action/...</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./internal/action/...` 通过（无错误）
    - `grep -c 'decisionRepo.*decisionRepo' internal/action/proposal_service_test.go` 返回 `0`（无重复导入）
    - `grep -c 'pool \*pgxpool.Pool' internal/action/proposal_service_test.go` 返回 `0`（无残留 pool 参数）
    - 不变式：`grep 'pgxpool' internal/action/proposal_service_test.go` 仍存在（保留 pgxpool 导入用于集成测试）
  </acceptance_criteria>
  <done>
    文件 internal/action/proposal_service_test.go 可通过 go vet 编译验证。所有 mock 类型签名匹配生产接口。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 修复 outbox_test.go — 移除构造函数中多余的 pool 参数</name>
  <files>internal/api/handler/outbox_test.go</files>
  <read_first>
    - internal/api/handler/outbox_test.go（第87-91行 — newOutboxHandlerForTest 函数）
    - internal/service/outbox_service.go（第18行 — NewOutboxService 构造函数签名）
  </read_first>
  <action>
    对 internal/api/handler/outbox_test.go 执行以下修改：

    1. **修复构造函数调用（第90行）**：
       - 旧：`svc := service.NewOutboxService(readRepo, pool)`
       - 新：`svc := service.NewOutboxService(readRepo)`

    2. **检查是否需要移除 unused import**：如果 `pgxpool` 在该文件中只用于第90行（作为第二个参数传给 NewOutboxService），则第15行 `"github.com/jackc/pgx/v5/pgxpool"` 导入在其他地方仍被使用（setupHlrOutboxTestDB 返回 `*pgxpool.Pool`，insertHlrTestEvent 等函数都使用 `*pgxpool.Pool`），所以保留导入。
  </action>
  <verify>
    <automated>go vet ./internal/api/handler/...</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./internal/api/handler/...` 通过
    - `grep 'NewOutboxService' internal/api/handler/outbox_test.go` 只包含一个参数（readRepo）
    - 函数 newOutboxHandlerForTest 编译无错误
  </acceptance_criteria>
  <done>
    Outbox handler test 编译通过，构造函数调用正确。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 修复 context_builder_test.go — 修复类型引用 + 移除 pool 参数</name>
  <files>internal/decision/context_builder_test.go</files>
  <read_first>
    - internal/decision/context_builder_test.go（整个文件，518行）
    - internal/decision/context_builder.go（第29-32行 — DecisionCaseDataProvider 接口定义）
  </read_first>
  <action>
    对 internal/decision/context_builder_test.go 执行以下所有修改：

    1. **修复导入**：
       - 添加导入：`decisionRepo "baxi/internal/repository/decision"`（在第9行 `"baxi/internal/ontology"` 之后）
       - 保留 `"baxi/internal/repository"` 导入（因为 `repository.ObjectInstance` 仍在使用）
       - 移除 `"github.com/jackc/pgx/v5/pgxpool"` 导入（移除所有 pool 引用后成为未使用导入）

    2. **修复 mockDecisionCaseDataProvider struct（第18行）**：将两个函数字段签名中的 `pool *pgxpool.Pool` 移除：
       - 旧：`getCaseByIDFn func(ctx context.Context, pool *pgxpool.Pool, caseID string) (*repository.DecisionCaseRow, error)`
       - 新：`getCaseByIDFn func(ctx context.Context, caseID string) (*decisionRepo.DecisionCaseRow, error)`
       - 旧：`getCaseBySourceFn func(ctx context.Context, pool *pgxpool.Pool, sourceType, sourceID string) (*repository.DecisionCaseRow, error)`
       - 新：`getCaseBySourceFn func(ctx context.Context, sourceType, sourceID string) (*decisionRepo.DecisionCaseRow, error)`
       - 注意：返回值类型从 `*repository.DecisionCaseRow` 改为 `*decisionRepo.DecisionCaseRow`

    3. **修复 GetCaseByID mock 方法（第22行）**：
       - 签名：移除 `pool *pgxpool.Pool` 参数
       - 返回类型：`*repository.DecisionCaseRow` → `*decisionRepo.DecisionCaseRow`
       - 方法体：移除 `pool` — `return m.getCaseByIDFn(ctx, caseID)`

    4. **修复 GetCaseBySource mock 方法（第26行）**：
       - 签名：移除 `pool *pgxpool.Pool` 参数
       - 返回类型：`*repository.DecisionCaseRow` → `*decisionRepo.DecisionCaseRow`
       - 方法体：移除 `pool` — `return m.getCaseBySourceFn(ctx, sourceType, sourceID)`

    5. **修复所有内联 lambda 定义（共~21处）**：所有 `getCaseByIDFn` 和 `getCaseBySourceFn` 的内联 lambda：
       - 参数：移除 `pool *pgxpool.Pool`
       - 返回类型：`*repository.DecisionCaseRow` → `*decisionRepo.DecisionCaseRow`
       - 具体位置（行号约数）：
         - `getCaseByIDFn`: 第55行、第123行、第198行、第296行、第383行、第398行、第442行
         - `getCaseBySourceFn`: 不存在此用例中

    6. **修复所有 DecisionCaseRow 结构体初始化的返回类型**（第60行、第128行等）：将 `&repository.DecisionCaseRow{...}` 改为 `&decisionRepo.DecisionCaseRow{...}`（共~7处）。

    7. **提示**：使用全局替换 `*repository.DecisionCaseRow` → `*decisionRepo.DecisionCaseRow` 和 `repository.DecisionCaseRow{` → `decisionRepo.DecisionCaseRow{` 以捕获所有位置。
  </action>
  <verify>
    <automated>go vet ./internal/decision/...</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./internal/decision/...` 通过
    - `grep 'repository.DecisionCaseRow' internal/decision/context_builder_test.go` 返回空（无陈旧类型引用）
    - `grep 'pgxpool' internal/decision/context_builder_test.go` 返回空（pgxpool 导入已移除）
    - `grep 'decisionRepo' internal/decision/context_builder_test.go` 返回非空（新导入存在）
  </acceptance_criteria>
  <done>
    文件 context_builder_test.go 编译通过。类型引用从 flat 仓库迁移到子包，mock 签名匹配生产接口。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: 修复 alert_service_test.go — 修复导入路径 + 构造函数签名</name>
  <files>internal/service/alert_service_test.go</files>
  <read_first>
    - internal/service/alert_service_test.go（整个文件，172行）
    - internal/service/alert_service.go（第21行 — NewAlertService 构造函数签名）
  </read_first>
  <action>
    对 internal/service/alert_service_test.go 执行以下修改：

    1. **修复导入（第14行）**：
       - 旧：`"baxi/internal/repository"`
       - 新：`alertRepo "baxi/internal/repository/alert"`

    2. **修复构造函数调用（第86行和第156行）**：移除多余的 `pool` 参数：
       - 旧（第86行、第156行）：`svc := NewAlertService(alertRepo.NewRepository(nil), pool)`
       - 新：`svc := NewAlertService(alertRepo.NewRepository(nil))`

    3. **检查 `pgxpool` 导入（第9行）**：保留 — `setupSvcAlertTestDB` 返回 `*pgxpool.Pool`，`insertSvcTestAlert` 接收 `*pgxpool.Pool`，测试函数中 pool 变量仍被使用。

    4. **检查 `"baxi/internal/model"` 导入（第13行）**：保留 — test 中使用 `model.AlertFilters{}`。
  </action>
  <verify>
    <automated>go vet ./internal/service/...</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./internal/service/...` 通过
    - `grep '"baxi/internal/repository"' internal/service/alert_service_test.go` → 空（旧导入已替换）
    - `grep 'alertRepo' internal/service/alert_service_test.go` → 非空（新导入存在）
    - `grep 'NewAlertService.*pool' internal/service/alert_service_test.go` → 空（构造函数无多余 pool）
  </acceptance_criteria>
  <done>
    文件 alert_service_test.go 编译通过。导入路径正确，构造函数调用匹配生产接口。
  </done>
</task>

</tasks>

<artifacts_produced>
## Artifacts This Phase Produces

本 Plan 不创建新符号/函数/类型。修复现有测试文件中的编译错误：

| 文件 | 修复类型 | 变更摘要 |
|------|----------|----------|
| `internal/action/proposal_service_test.go` | 编译错误修复 | 重复导入移除 + pool 参数从 mock 签名中移除（~15处） |
| `internal/api/handler/outbox_test.go` | 编译错误修复 | NewOutboxService 构造函数调用修复（移除 pool 参数） |
| `internal/decision/context_builder_test.go` | 编译错误修复 | DecisionCaseRow 类型引用重定向到子包 + pool 参数移除（~20处） |
| `internal/service/alert_service_test.go` | 编译错误修复 | 导入路径修复 + NewAlertService 构造函数调用修复（移除 pool 参数） |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → production interfaces | Test mocks must match production interface signatures exactly — mismatches cause silent test skips (wrong behavior coverage) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | proposal_service_test.go | mitigate | Verify mock signatures match ProposalRepository/CaseStatusUpdater interfaces at compile time via `go vet` |
| T-06-02 | Tampering | context_builder_test.go | mitigate | Verify mock signatures match DecisionCaseDataProvider at compile time via `go vet` |
| T-06-03 | Tampering | alert_service_test.go | mitigate | Verify NewAlertService call matches actual constructor signature via `go vet` |
| T-06-04 | Tampering | outbox_test.go | mitigate | Verify NewOutboxService call matches actual constructor signature via `go vet` |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | 本 Plan 不安装任何包 — 新外部依赖为零 |
</threat_model>

<verification>
```bash
go vet ./internal/action/... && echo "action OK"
go vet ./internal/api/handler/... && echo "handler OK"
go vet ./internal/decision/... && echo "decision OK"
go vet ./internal/service/... && echo "service OK"
```
</verification>

<success_criteria>
- `go vet ./internal/...` 对所有受影响的包通过
- 4 个 Go 测试文件编译无障碍
- 无需修改生产代码 — 所有更改仅限于 `*_test.go` 文件
</success_criteria>

<output>
创建 `.planning/phases/06-integration-end-to-end-demo/06-01-SUMMARY.md`
</output>
