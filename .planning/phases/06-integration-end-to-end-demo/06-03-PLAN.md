---
phase: 06-integration-end-to-end-demo
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/__tests__/DecisionReview.test.tsx
  - frontend/src/pages/__tests__/PolicyInspector.test.tsx
  - frontend/src/pages/__tests__/CaseDetail.test.tsx
  - frontend/src/pages/__tests__/AuditTimeline.test.tsx
  - frontend/src/pages/__tests__/AgentLogs.test.tsx
  - frontend/src/pages/__tests__/SandboxCompare.test.tsx
  - frontend/src/components/__tests__/Layout.test.tsx
autonomous: true
requirements: [INT-04]
user_setup: []
must_haves:
  truths:
    - "7 frontend test files pass with no failures"
    - "ErrorPanel error text assertions match actual component output ('加载失败' not '请求异常')"
    - "Layout token test assertion matches actual sessionStorage default"
  artifacts:
    - path: frontend/src/pages/__tests__/DecisionReview.test.tsx
      provides: "Passing test — error text '加载失败'"
    - path: frontend/src/pages/__tests__/PolicyInspector.test.tsx
      provides: "Passing test — error text '加载失败'"
    - path: frontend/src/pages/__tests__/CaseDetail.test.tsx
      provides: "Passing test — error text '加载失败'"
    - path: frontend/src/pages/__tests__/AuditTimeline.test.tsx
      provides: "Passing test — error text '加载失败'"
    - path: frontend/src/pages/__tests__/AgentLogs.test.tsx
      provides: "Passing test — error text '加载失败'"
    - path: frontend/src/pages/__tests__/SandboxCompare.test.tsx
      provides: "Passing test — error text '加载失败'"
    - path: frontend/src/components/__tests__/Layout.test.tsx
      provides: "Passing test — token default is ''"
  key_links:
    - from: DecisionReview test
      to: frontend/src/components/ErrorPanel.tsx
      via: "ErrorPanel title = '加载失败'"
      pattern: "加载失败"
    - from: Layout test
      to: frontend/src/components/Layout.tsx
      via: "sessionStorage default for API_BEARER_TOKEN"
      pattern: "API_BEARER_TOKEN|sessionStorage"
---

<objective>
修复 7 个前端测试文件中的文本断言，使测试通过。

Purpose: 实现 INT-04 — 所有前端单元测试通过。测试断言当前不匹配组件实际输出：
- ErrorPanel 组件渲染 `title="加载失败"`，但测试断言 `"请求异常"`。
- Layout 组件 `sessionStorage` 默认值为空字符串，但测试断言为硬编码的测试 token。

所有更改仅限于测试断言文本 — 不修改组件行为。

Output: 7 个测试文件全部通过 `npx vitest run`
</objective>

<execution_context>
@/home/zzz/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/zzz/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-integration-end-to-end-demo/06-RESEARCH.md
@.planning/phases/06-integration-end-to-end-demo/06-UI-SPEC.md

# 组件实际输出（测试断言必须匹配）
@frontend/src/components/ErrorPanel.tsx（第16行 — title 硬编码为 "加载失败"）
@frontend/src/components/Layout.tsx

# 要修改的测试文件
@frontend/src/pages/__tests__/DecisionReview.test.tsx
@frontend/src/pages/__tests__/PolicyInspector.test.tsx
@frontend/src/pages/__tests__/CaseDetail.test.tsx
@frontend/src/pages/__tests__/AuditTimeline.test.tsx
@frontend/src/pages/__tests__/AgentLogs.test.tsx
@frontend/src/pages/__tests__/SandboxCompare.test.tsx
@frontend/src/components/__tests__/Layout.test.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 修复所有 ErrorPanel 错误文本断言 — "请求异常" → "加载失败"</name>
  <files>
    frontend/src/pages/__tests__/DecisionReview.test.tsx,
    frontend/src/pages/__tests__/PolicyInspector.test.tsx,
    frontend/src/pages/__tests__/CaseDetail.test.tsx,
    frontend/src/pages/__tests__/AuditTimeline.test.tsx,
    frontend/src/pages/__tests__/AgentLogs.test.tsx,
    frontend/src/pages/__tests__/SandboxCompare.test.tsx
  </files>
  <read_first>
    - frontend/src/components/ErrorPanel.tsx（第16行 — ErrorPanel 渲染 `title`，组件输出固定为 "加载失败"）
    - 上述6个测试文件中所有包含 "请求异常" 的行
  </read_first>
  <action>
    对以下6个测试文件，执行"查找替换"操作：

    将每个文件中所有出现的 `"请求异常"` 替换为 `"加载失败"`。

    具体文件及行号（约数，全局替换即可）：

    1. **DecisionReview.test.tsx**（第72行）：
       - `expect(await screen.findByText("请求异常")).toBeInTheDocument()`
       - → `expect(await screen.findByText("加载失败")).toBeInTheDocument()`

    2. **PolicyInspector.test.tsx**（第47行）：
       - `expect(await screen.findByText("请求异常")).toBeInTheDocument()`
       - → `expect(await screen.findByText("加载失败")).toBeInTheDocument()`

    3. **CaseDetail.test.tsx**（第58行）：
       - `expect(await screen.findByText("请求异常")).toBeInTheDocument()`
       - → `expect(await screen.findByText("加载失败")).toBeInTheDocument()`

    4. **AuditTimeline.test.tsx**（第64行）：
       - `expect(await screen.findByText("请求异常")).toBeInTheDocument()`
       - → `expect(await screen.findByText("加载失败")).toBeInTheDocument()`

    5. **AgentLogs.test.tsx**（第48行）：
       - `expect(await screen.findByText("请求异常")).toBeInTheDocument()`
       - → `expect(await screen.findByText("加载失败")).toBeInTheDocument()`

    6. **SandboxCompare.test.tsx**（第56行和第138行 — 注意此文件有两处 "请求异常"）：
       - 第56行：`expect(await screen.findByText("请求异常")).toBeInTheDocument()`
       - 第138行：`expect(await screen.findByText("请求异常")).toBeInTheDocument()`（comparison error panel）
       - → 两处均替换为 `"加载失败"`

    **注意**：SandboxCompare.tsx 组件有两个不同的 ErrorPanel：
    - 主 sandboxes 列表 error Panel 渲染 `title="Failed to load"`（第97行）
    - comparison error Panel 渲染 `title="Comparison failed"`（第163行）
    - 测试中（第56行和第138行）assert 的都是 "请求异常" — 但实际渲染的是 "Failed to load" 或 "Comparison failed"

    因此对于 **SandboxCompare.test.tsx**：
    - 第56行：`"请求异常"` → `"Failed to load"`（匹配第97行 ErrorPanel title）
    - 第138行：`"请求异常"` → `"Comparison failed"`（匹配第163行 ErrorPanel title）

    **对于 DecisionReview.test.tsx**：
    - 第72行：`"请求异常"` → `"Failed to load"`（DecisionReview.tsx 第142行 ErrorPanel 渲染 `title="Failed to load"`）

    总结替换映射：
    | 文件 | 旧文本 | 新文本 | 理由 |
    |---|---|---|---|
    | DecisionReview.test.tsx | "请求异常" | "Failed to load" | 组件渲染 `title="Failed to load"` |
    | PolicyInspector.test.tsx | "请求异常" | "加载失败" | 组件渲染 `title="加载失败"` |
    | CaseDetail.test.tsx | "请求异常" | "加载失败" | 组件渲染 `title="加载失败"` |
    | AuditTimeline.test.tsx | "请求异常" | "加载失败" | 组件渲染 `title="加载失败"` |
    | AgentLogs.test.tsx | "请求异常" | "加载失败" | 组件渲染 `title="加载失败"` |
    | SandboxCompare.test.tsx line 56 | "请求异常" | "Failed to load" | 主列表 ErrorPanel `title="Failed to load"` |
    | SandboxCompare.test.tsx line 138 | "请求异常" | "Comparison failed" | comparison ErrorPanel `title="Comparison failed"` |
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/__tests__/DecisionReview.test.tsx src/pages/__tests__/PolicyInspector.test.tsx src/pages/__tests__/CaseDetail.test.tsx src/pages/__tests__/AuditTimeline.test.tsx src/pages/__tests__/AgentLogs.test.tsx src/pages/__tests__/SandboxCompare.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r '"请求异常"' frontend/src/pages/__tests__/` → 空（所有陈旧文本已替换）
    - 6 个测试文件全部通过 vitest run
  </acceptance_criteria>
  <done>
    6 个页面测试文件的 error panel 文本断言匹配实际组件输出。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 修复 Layout.test.tsx token 默认值断言</name>
  <files>frontend/src/components/__tests__/Layout.test.tsx</files>
  <read_first>
    - frontend/src/components/__tests__/Layout.test.tsx（第56-59行 — "uses default token when no token is stored" 测试）
    - frontend/src/components/Layout.tsx（API_BEARER_TOKEN 的 sessionStorage 读取逻辑）
  </read_first>
  <action>
    修改 frontend/src/components/__tests__/Layout.test.tsx 第58行：

    - 旧（第58行）：`expect(sessionStorage.getItem("API_BEARER_TOKEN")).toBe("test-token-for-dev-b2kA3QOBBD48wLFQAgAtLw")`
    - 新：`expect(sessionStorage.getItem("API_BEARER_TOKEN")).toBe("")`

    会话开始时 sessionStorage 为空，API_BEARER_TOKEN 的默认值应为空字符串 `""`。之前的测试假设了一个硬编码的开发 token，但 Layout 组件不会在初始化时向 sessionStorage 写入默认 token — 用户在输入框中输入后才写入。

    **注意**：第49-54行的 "stores API token in sessionStorage on change" 测试验证了用户输入后 token 正确存储，这个测试保持不变。
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/__tests__/Layout.test.tsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/__tests__/Layout.test.tsx` 全部通过
    - `grep 'test-token-for-dev' frontend/src/components/__tests__/Layout.test.tsx` → 空（陈旧 token 已移除）
  </acceptance_criteria>
  <done>
    Layout test token 默认值断言匹配实际 sessionStorage 行为。
  </done>
</task>

</tasks>

<artifacts_produced>
## Artifacts This Phase Produces

本 Plan 不创建新测试或符号。仅修复现有测试断言文本：

| 文件 | 断言修复 |
|------|----------|
| `frontend/src/pages/__tests__/DecisionReview.test.tsx` | "请求异常" → "Failed to load"（匹配 ErrorPanel title） |
| `frontend/src/pages/__tests__/PolicyInspector.test.tsx` | "请求异常" → "加载失败" |
| `frontend/src/pages/__tests__/CaseDetail.test.tsx` | "请求异常" → "加载失败" |
| `frontend/src/pages/__tests__/AuditTimeline.test.tsx` | "请求异常" → "加载失败" |
| `frontend/src/pages/__tests__/AgentLogs.test.tsx` | "请求异常" → "加载失败" |
| `frontend/src/pages/__tests__/SandboxCompare.test.tsx` | "请求异常" → "Failed to load" / "Comparison failed"（两处） |
| `frontend/src/components/__tests__/Layout.test.tsx` | 硬编码 token → `""`（空字符串默认值） |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test assertion → component output | 测试文本断言必须精确匹配组件渲染输出 — 不匹配导致假阴性测试失败 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Tampering | DecisionReview.test.tsx | mitigate | ErrorPanel title 为 "Failed to load"（英文）— 测试断言匹配英文 |
| T-06-09 | Tampering | PolicyInspector/CaseDetail/AuditTimeline/AgentLogs tests | mitigate | ErrorPanel title 为 "加载失败"（中文）— 测试断言匹配中文 |
| T-06-10 | Tampering | SandboxCompare.test.tsx (line 56) | mitigate | 主列表 ErrorPanel title 为 "Failed to load" |
| T-06-11 | Tampering | SandboxCompare.test.tsx (line 138) | mitigate | comparison ErrorPanel title 为 "Comparison failed" |
| T-06-12 | Tampering | Layout.test.tsx | mitigate | sessionStorage 默认值为 "" 而非硬编码 token |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | 本 Plan 不安装任何包 |
</threat_model>

<verification>
```bash
cd frontend && npx vitest run src/pages/__tests__/ --reporter=verbose
cd frontend && npx vitest run src/components/__tests__/ --reporter=verbose
```
</verification>

<success_criteria>
- 所有 7 个测试文件通过 vitest run
- 无 "请求异常" 残留文本在测试文件中
- Layout test token 默认值正确
</success_criteria>

<output>
创建 `.planning/phases/06-integration-end-to-end-demo/06-03-SUMMARY.md`
</output>
