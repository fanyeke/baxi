---
phase: 06-integration-end-to-end-demo
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-02, 06-03]
files_modified: []
autonomous: true
requirements: [INT-05]
user_setup: []
must_haves:
  truths:
    - "go vet ./internal/... passes with zero compilation errors"
    - "go test -short ./internal/... passes (unit tests only)"
    - "cd frontend && npx vitest run passes with all tests green"
    - "Docker is running, E2E tests can execute"
    - "CLI demo command runs without error"
  artifacts:
    - path: ".planning/phases/06-integration-end-to-end-demo/06-04-SUMMARY.md"
      provides: "Demo validation report with test results"
      contains: "test pass/fail, demo steps, verification results"
  key_links:
    - from: go vet
      to: "All 4 Go test files"
      via: "compilation succeeds"
    - from: vitest run
      to: "All 7 frontend test files"
      via: "assertions match component output"
---

<objective>
验证全闭环演示的端到端操作。在所有之前的修复合并后，运行完整的测试套件并验证闭环 demo 流程。

Purpose: 实现 INT-05 — 全闭环 demo 正常运行。这是验收门 — 验证 INT-01 到 INT-04 的修复正确，且完整的数据流（触发管道 → 治理规则 → 决策创建 → 操作执行 → 告警发送 → 前端显示）可被演示。

Output: 验证报告 + 所有测试通过
</objective>

<execution_context>
@/home/zzz/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/zzz/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-integration-end-to-end-demo/06-RESEARCH.md

# 已验证的 Plan SUMMARY
@.planning/phases/06-integration-end-to-end-demo/06-01-SUMMARY.md
@.planning/phases/06-integration-end-to-end-demo/06-02-SUMMARY.md
@.planning/phases/06-integration-end-to-end-demo/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 完整测试套件验证 — 运行所有测试并确认无失败</name>
  <files></files>
  <read_first>
    无 — 所有代码修改已在之前 Plan 中完成。此任务是仅验证。
  </read_first>
  <action>
    按依赖顺序运行以下测试命令。每个步骤必须通过后才能继续。如果任何步骤失败，记录失败详情并停止 — 需要在前序 Plan 中修复。

    1. **Go vet 编译检查**：
       ```bash
       go vet ./internal/action/... ./internal/api/handler/... ./internal/decision/... ./internal/service/...
       ```
       期望：无输出（成功）。验证所有 4 个测试文件编译通过。

    2. **Go 单元测试**（短模式 — 跳过 Docker 依赖测试）：
       ```bash
       go test -short ./internal/action/... ./internal/api/handler/... ./internal/decision/... ./internal/service/...
       ```
       期望：全部通过。这些是修改过的测试文件。

    3. **前端 TypeScript 编译**：
       ```bash
       cd frontend && npx tsc --noEmit --strict
       ```
       期望：无输出（成功）。

    4. **前端所有单元测试**：
       ```bash
       cd frontend && npx vitest run --reporter=verbose 2>&1
       ```
       期望：所有测试绿色通过。`grep -c 'FAIL'` 为 0。

    5. **如果 Docker 正在运行** — E2E 集成测试（仅用于验证，非阻塞）：
       ```bash
       docker info > /dev/null 2>&1 && go test -tags=integration -count=1 -timeout 180s ./test/integration/... 2>&1 | tail -30 || echo "Docker not running — skip E2E tests"
       ```

    6. **生成验证摘要报告**：将结果写入 SUMMARY.md。
  </action>
  <verify>
    <automated>go vet ./internal/... 2>&1 && echo "vet:OK" && go test -short ./internal/action/... ./internal/api/handler/... ./internal/decision/... ./internal/service/... 2>&1 | tail -5 && echo "---" && cd frontend && npx tsc --noEmit --strict 2>&1 && echo "tsc:OK" && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./internal/...` 通过（无错误输出）
    - `go test -short ./internal/action/... ./internal/api/handler/... ./internal/decision/... ./internal/service/...` 全部通过（PASS）
    - `cd frontend && npx tsc --noEmit --strict` 通过（无类型错误）
    - `cd frontend && npx vitest run` 通过（全部绿色，无 FAIL）
    - 验证摘要报告写入 SUMMARY.md
  </acceptance_criteria>
  <done>
    所有测试套件通过。验证报告已生成。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 全闭环演示验证</name>
  <files></files>
  <read_first>
    - .planning/phases/06-integration-end-to-end-demo/06-RESEARCH.md（第130-152行 — 闭环数据流图）
  </read_first>
  <action>
    验证全闭环演示路径可执行。注意：这不是自动化测试 — 这是验证每个环节的入口点和脚本可用性。

    验证以下每个步骤的入口点存在且可操作：

    1. **触发管道**：`make pipeline` 或 `go run ./cmd/baxi-cli pipeline run` 存在可执行。验证 CLI 子命令存在（`grep -c "pipeline" cmd/baxi-cli/main.go` 或确认 Makefile 中有 `pipeline` 目标）。

    2. **治理规则**：治理规则存在于 `config/` 目录中。验证 YAML 配置文件存在（`ls config/*.yml` 返回至少 10 个文件）。

    3. **决策创建**：API 端点 `POST /api/v1/decisions/cases` 存在。验证 handler 文件存在（`grep -c "HandleCreate" internal/api/handler/decision.go` 或类似模式）。

    4. **操作执行**：API 端点 `/api/v1/proposals/{id}/approve` 等存在。验证 review handler 文件存在。

    5. **告警发送**：警告引擎和 outbox 事件处理存在。验证 worker 入口存在。

    6. **前端可见性**：验证所有前端页面文件存在（`ls frontend/src/pages/*.tsx` — 至少 10 个页面）。

    通过 curl 测试 API 可达性（如果 API 正在运行）：
    ```bash
    curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/api/v1/health 2>/dev/null || echo "API not running — manual verification required"
    ```

    记录验证结果到 SUMMARY.md。
  </action>
  <verify>
    <automated>
      echo "=== Pipeline CLI ===" && go run ./cmd/baxi-cli 2>&1 | grep -c pipeline || true
      echo "=== Config Files ===" && ls config/*.yml | wc -l
      echo "=== Frontend Pages ===" && ls frontend/src/pages/*.tsx | wc -l
      echo "=== API Health Check ===" && curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/api/v1/health 2>/dev/null || echo "API not running"
    </automated>
  </verify>
  <acceptance_criteria>
    - `make pipeline` 或等价命令存在（验证入口点）
    - `config/*.yml` 至少包含 20 个文件
    - `frontend/src/pages/*.tsx` 至少包含 10 个页面
    - API 端点 handler 文件存在（decision.go, review.go 等）
    - 验证结果记录在 SUMMARY.md 中
  </acceptance_criteria>
  <done>
    全闭环演示验证完成。所有入口点可用，demo 路径可执行。
  </done>
</task>

</tasks>

<artifacts_produced>
## Artifacts This Phase Produces

本 Plan 不创建代码符号。产生验证报告和测试结果摘要：

| 产物 | 位置 | 说明 |
|------|------|------|
| 测试结果摘要 | `.planning/phases/06-integration-end-to-end-demo/06-04-SUMMARY.md` | go vet、go test、vitest 的通过/失败记录 |
| 闭环验证报告 | 同上 | 演示入口点检查、已验证的组件 |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → test execution | 测试必须运行在正确的环境中 — Docker 对 E2E 测试可用，Node 对前端测试可用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Tampering | Go test runner | mitigate | 先运行 `go vet` 再运行 `go test` — 编译错误必须在运行时失败前捕获 |
| T-06-14 | Tampering | Vitest runner | mitigate | 使用 `--reporter=verbose` 运行所有前端测试 — 确保所有 42+ 个测试文件被执行 |
| T-06-15 | Denial of Service | E2E test Docker dependency | accept | 测试有 `testing.Short()` guard — Docker 不可用时使用 `-short` 跳过 |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | 本 Plan 不安装任何包 |
</threat_model>

<verification>
```bash
echo "=== Go Vet ===" && go vet ./internal/... && echo "PASS" || echo "FAIL"
echo "=== Go Tests ===" && go test -short ./internal/... && echo "PASS" || echo "FAIL"
echo "=== Frontend TypeScript ===" && cd frontend && npx tsc --noEmit --strict && echo "PASS" || echo "FAIL"
echo "=== Frontend Tests ===" && npx vitest run --reporter=verbose 2>&1 | tail -5
```
</verification>

<success_criteria>
- Go vet 全部通过
- Go 单元测试通过
- TypeScript 编译通过
- 前端单元测试全部通过
- 闭环演示入口点验证完成
</success_criteria>

<output>
创建 `.planning/phases/06-integration-end-to-end-demo/06-04-SUMMARY.md`
</output>
