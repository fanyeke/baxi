---
phase: 06-integration-end-to-end-demo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/governance.ts
  - frontend/src/pages/Governance.tsx
  - frontend/src/pages/__tests__/Governance.test.tsx
  - frontend/src/pages/Pipeline.tsx
autonomous: true
requirements: [INT-01]
user_setup: []
must_haves:
  truths:
    - "Frontend Governance page renders catalog data from backend (objects/datasets shape)"
    - "Frontend Pipeline page sends {config: type} instead of {pipeline_type: type}"
    - "Governance.test.tsx passes with updated mock data"
    - "Frontend TypeScript compiles with no errors"
  artifacts:
    - path: frontend/src/api/governance.ts
      provides: "Updated types matching backend DTOs"
      contains: "CatalogObject, CatalogDataset, CatalogResponse with objects/datasets fields"
    - path: frontend/src/pages/Governance.tsx
      provides: "Governance page with CatalogTab using data.objects"
      contains: "data.objects.length"
    - path: frontend/src/pages/Pipeline.tsx
      provides: "Pipeline page with config field in request body"
      contains: "config: pipelineType"
  key_links:
    - from: frontend/src/api/governance.ts
      to: frontend/src/pages/Governance.tsx
      via: "useCatalog hook → CatalogTab consumer"
      pattern: "useCatalog|CatalogObject"
---

<objective>
将前端 Governance API 类型和后端 DTO 对齐，修复 Pipeline 页面请求体字段名。

Purpose: 实现 INT-01 — Governance 和 Pipeline 页面能显示后端真实数据。后端 DTO 是"source of truth"，前端类型简化匹配后端。
后端返回 `{objects, datasets}` 而非 `{data_catalog, assets}`。后端期望 `{config: type}` 而非 `{pipeline_type: type}`。

Output: 4 个前端文件修改完成，TypeScript 编译通过，Governance 测试通过。
</objective>

<execution_context>
@/home/zzz/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/zzz/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-integration-end-to-end-demo/06-RESEARCH.md
@.planning/phases/06-integration-end-to-end-demo/06-UI-SPEC.md
@.planning/phases/06-integration-end-to-end-demo/06-PATTERNS.md

# 要修改的文件
@frontend/src/api/governance.ts
@frontend/src/pages/Governance.tsx
@frontend/src/pages/__tests__/Governance.test.tsx
@frontend/src/pages/Pipeline.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 修复 governance API 类型 + Governance 页面组件 + Governance 测试</name>
  <files>frontend/src/api/governance.ts, frontend/src/pages/Governance.tsx, frontend/src/pages/__tests__/Governance.test.tsx</files>
  <read_first>
    - frontend/src/api/governance.ts（整个文件，143行 — 当前类型定义和后端 DTO 不匹配）
    - frontend/src/pages/Governance.tsx（整个文件，362行 — CatalogTab 组件使用 data.assets 和 CatalogAsset）
    - frontend/src/pages/__tests__/Governance.test.tsx（整个文件，199行 — mock 数据使用旧 assets 形状）
  </read_first>
  <action>
    ## A. 修改 frontend/src/api/governance.ts

    **替换类型定义（第4-17行）**：
    1. 删除 `CatalogAsset` 接口（第4-12行）
    2. 删除 `CatalogResponse` 接口中的 `data_catalog` 字段（第15行），将 `assets: CatalogAsset[]` 替换为 `objects: CatalogObject[]` 和 `datasets: CatalogDataset[]`
    3. 添加两个新接口：

    ```typescript
    export interface CatalogObject {
      object_type: string
      source_dataset: string
      primary_key: string
      properties_count: number
      links_count: number
    }

    export interface CatalogDataset {
      dataset: string
      schema: string
      table: string
    }
    ```

    4. 更新 `CatalogResponse`：
    ```typescript
    export interface CatalogResponse {
      objects: CatalogObject[]
      datasets: CatalogDataset[]
    }
    ```

    5. **保持所有其他类型不变**：Classification, ClassificationResponse, MarkingInfo, MarkingResponse, LineageNode, LineageEdge, LineageResponse, CheckpointRule, CheckpointResponse, HealthCheck, MonitoringView, HealthResponse — 这些已匹配后端 DTO。

    6. **保持所有 hook 函数不变**：useCatalog, useClassification, useMarkings, useLineage, useCheckpoints, useHealth — 这些函数的签名不变（只改变了它们引用的类型）。

    ## B. 修改 frontend/src/pages/Governance.tsx

    1. **更新导入（第10-18行）**：将 `CatalogAsset` 替换为 `CatalogObject`：
       - 旧：`import type { CatalogAsset, Classification, ... }`
       - 新：`import type { CatalogObject, Classification, ... }`

    2. **更新 CatalogTab 函数（第79-99行）**：
       - 空状态检查（第82行）：`!data || data.assets.length === 0` → `!data || data.objects.length === 0`
       - DataTable headers（第85行）：`["资产 ID", "名称", "类型", "位置", "描述", "粒度", "状态"]` → `["对象类型", "来源数据集", "主键", "属性数", "链接数"]`
       - map 迭代（第86行）：`data.assets.map((a: CatalogAsset)` → `data.objects.map((o: CatalogObject, i: number)`
       - 表格行内容（第87-95行）：
         - key: `a.asset_id` → `o.primary_key + i`
         - 列 1: `${a.asset_id}` → `${o.object_type}`
         - 列 2: `${a.name}` → `${o.source_dataset}`
         - 列 3: `${a.asset_type}` → `${o.primary_key}`
         - 列 4: `${a.location}` → `${o.properties_count}`
         - 列 5: `${a.description ?? "—"}` → `${o.links_count}`
         - 列 6-7: 删除（grain 和 status 列）
       - 删除 SeverityBadge 导入引用（如果不再需要）

    3. **更新 SummaryStats（第353行）**：
       - 旧：`catalogCount={catalog.data?.assets.length ?? 0}`
       - 新：`catalogCount={catalog.data?.objects.length ?? 0}`
       - 标签（第276行）：`"数据资产"` → `"数据对象"`（反映数据含义的变化）

    4. **注意**：保持 `import type { SeverityBadge }` 不变 — SeverityBadge 在 LineageTab、HealthTab 等其他 tabs 中仍被使用。

    ## C. 修改 frontend/src/pages/__tests__/Governance.test.tsx

    1. **更新 mockCatalogData（第37-50行）**：
       - 将 `data_catalog: {}` 和 `assets: [{ asset_id, asset_type, name, ... }]` 替换为：
       ```typescript
       const mockCatalogData = {
         objects: [
           { object_type: "table", source_dataset: "dw.orders", primary_key: "order_id", properties_count: 15, links_count: 3 },
         ],
         datasets: [
           { dataset: "orders", schema: "dw", table: "orders" },
         ],
       }
       ```

    2. **更新 catalog display 测试（第148-161行）**：检查新字段：
       - `screen.getByText("table")`（取代 `"asst-001"` 和 `"订单表"`）
       - `screen.getByText("dw.orders")`（来源数据集）
       - `screen.getByText("order_id")`（主键）

    3. **更新 empty state 测试（第190-198行）**：
       - 旧：`data: { assets: [], data_catalog: {} }`
       - 新：`data: { objects: [], datasets: [] }`

    4. **更新 "displays data across all tabs" 测试（第163-188行）**：检查新 mock 数据字段。
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --strict 2>&1 | head -30; cd frontend && npx vitest run src/pages/__tests__/Governance.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit --strict` 通过（无类型错误）
    - `cd frontend && npx vitest run src/pages/__tests__/Governance.test.tsx` 通过（全部 passed）
    - `grep 'data_catalog\|CatalogAsset\|assets:' frontend/src/api/governance.ts` → 空（无陈旧类型）
    - `grep 'data.assets' frontend/src/pages/Governance.tsx` → 空（无陈旧引用）
    - `grep 'data\.objects' frontend/src/pages/Governance.tsx` → 非空（新引用存在）
  </acceptance_criteria>
  <done>
    Governance API 类型匹配后端 DTO，Governance 页面渲染后端返回的 catalog 对象数据，所有测试通过。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 修复 Pipeline.tsx 请求体字段名</name>
  <files>frontend/src/pages/Pipeline.tsx</files>
  <read_first>
    - frontend/src/pages/Pipeline.tsx（第18行 — mutationFn 的请求体字段）
  </read_first>
  <action>
    修改 frontend/src/pages/Pipeline.tsx 第18行：
    - 旧（第18行）：`mutationFn: () => apiClient.post<PipelineRunResponse>("/pipeline/run", { pipeline_type: pipelineType }),`
    - 新：`mutationFn: () => apiClient.post<PipelineRunResponse>("/pipeline/run", { config: pipelineType }),`

    后端将 `pipeline_type` 重命名为 `config`，因此请求体必须发送 `{ config: "daily" }` 而不是 `{ pipeline_type: "daily" }`。

    **不需要修改其他任何内容** — PipelineRunResponse 类型（在 types.ts 中）已经正确包含 `pipeline_type` 和 `command` 等返回字段。后端返回的 shape 不变，只改请求字段名。
  </action>
  <verify>
    <automated>grep -n "config:" frontend/src/pages/Pipeline.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'pipeline_type:' frontend/src/pages/Pipeline.tsx | grep 'mutationFn\|post'` → 空（请求体中无陈旧字段名）
    - `grep 'config:' frontend/src/pages/Pipeline.tsx | grep 'mutationFn\|post'` → 非空（新字段名存在）
    - `cd frontend && npx tsc --noEmit --strict` 通过
  </acceptance_criteria>
  <done>
    Pipeline 页面发送正确字段名 `config` 匹配后端期望。
  </done>
</task>

</tasks>

<artifacts_produced>
## Artifacts This Phase Produces

本 Plan 不创建新组件/页面。修改现有类型和组件以匹配后端 DTO：

| 文件 | 变更类型 | 主要变更 |
|------|----------|----------|
| `frontend/src/api/governance.ts` | 类型定义 | 新增 `CatalogObject`、`CatalogDataset` 接口；替换 `CatalogAsset`；更新 `CatalogResponse` 字段为 `objects`/`datasets` |
| `frontend/src/pages/Governance.tsx` | 组件 | CatalogTab 从 `data.assets` 迁移到 `data.objects`；列从 7 列改为 5 列 |
| `frontend/src/pages/Pipeline.tsx` | 组件 | 请求体字段从 `pipeline_type` 改为 `config` |
| `frontend/src/pages/__tests__/Governance.test.tsx` | 测试 | mock 数据从旧 `assets` 形状更新为新 `objects`/`datasets` 形状 |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend TypeScript → backend API | 前端类型必须匹配后端 JSON 响应的实际形状 — 类型错配导致运行时静默数据丢失 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Information Disclosure | governance.ts CatalogResponse | mitigate | 后端是 source of truth — 不修改后端 DTO，只简化前端类型匹配实际后端响应 |
| T-06-06 | Tampering | Pipeline.tsx request body | mitigate | 后端期望 `config` 字段 — 前端发送 `config` 而非 `pipeline_type`。通过实际启动 API 并测试 POST 来验证 |
| T-06-07 | Tampering | Governance.test.tsx mock data | mitigate | 更新 mock 数据匹配新前端类型 — 确保测试覆盖非空渲染和空状态 |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | 本 Plan 不安装任何包 |
</threat_model>

<verification>
```bash
cd frontend && npx tsc --noEmit --strict && echo "TypeScript OK"
cd frontend && npx vitest run src/pages/__tests__/Governance.test.tsx --reporter=verbose
```
</verification>

<success_criteria>
- TypeScript 编译通过，无类型错误
- Governance 测试全部通过
- Pipeline 请求体字段名正确
</success_criteria>

<output>
创建 `.planning/phases/06-integration-end-to-end-demo/06-02-SUMMARY.md`
</output>
