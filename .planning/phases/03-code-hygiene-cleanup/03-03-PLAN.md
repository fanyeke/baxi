---
phase: 03-code-hygiene-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - internal/repository/governance_repository.go
  - internal/repository/decision_repository.go
  - internal/repository/ontology_repository.go
  - internal/repository/outbox_repository.go
  - internal/repository/log_repository.go
  - internal/repository/context_repository.go
  - internal/repository/alert_repository.go
  - internal/repository/status_repository.go
  - internal/repository/task_repository.go
  - internal/repository/interfaces.go
  - internal/repository/repository_test.go
  - internal/repository/repository_coverage_test.go
  - migration_baseline/
  - scripts/migration/
  - README.md
  - AGENTS.md
autonomous: true
requirements:
  - HYG-03
  - HYG-07
must_haves:
  truths:
    - "弃用的 repository shim 文件（6 + 3 + interfaces.go）全部删除"
    - "migration_baseline/ 目录已删除"
    - "scripts/migration/*.py 已删除"
    - "README.md 和 AGENTS.md 中无过时的 SQLite/Python 引用"
  artifacts:
    - path: internal/repository/
      provides: "只包含子包和 subpackage 组织，无弃用 shim"
    - path: README.md
      provides: "项目文档无 Python/SQLite 参考"
    - path: AGENTS.md
      provides: "项目知识库无 Python/SQLite 参考"
  key_links:
    - from: Plan 02 迁移完成
      to: 所有 9 个 shim 文件已删除
      via: "无引用 = 安全删除"
---

<objective>
删除弃用的 repository shim 文件、迁移基线目录和脚本，更新文档。

Purpose: 完成 HYG-03（删除 6 个指定 shim + interfaces.go）、HYG-07（删除 migration_baseline、更新文档）。同时删除 D-05 中额外的 3 个 shim（alert/status/task）和相关测试文件。
Output: 只包含子包 repository 的干净代码库，无 Python/SQLite 遗留文档引用。
</objective>

<execution_context>
@/home/zzz/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/zzz/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-code-hygiene-cleanup/03-CONTEXT.md
@.planning/phases/03-code-hygiene-cleanup/03-02-SUMMARY.md
@internal/repository/
@README.md
@AGENTS.md
@docs/
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除弃用的 repository shim 文件 (HYG-03, D-05)</name>
  <files>
    internal/repository/governance_repository.go
    internal/repository/decision_repository.go
    internal/repository/ontology_repository.go
    internal/repository/outbox_repository.go
    internal/repository/log_repository.go
    internal/repository/context_repository.go
    internal/repository/alert_repository.go
    internal/repository/status_repository.go
    internal/repository/task_repository.go
    internal/repository/interfaces.go
    internal/repository/repository_test.go
    internal/repository/repository_coverage_test.go
  </files>
  <read_first>
    internal/repository/ （目录下所有文件的完整结构）
  </read_first>
  <action>
    本任务执行实际的删除操作。

    **前提条件：** Plan 02 已完成，所有调用方已迁移，无代码引用弃用 shim。

    **删除文件列表（共 12 个）：**

    HYG-03 指定（6 个）：
    1. `internal/repository/governance_repository.go` — governance shim
    2. `internal/repository/decision_repository.go` — decision shim
    3. `internal/repository/ontology_repository.go` — ontology shim
    4. `internal/repository/outbox_repository.go` — outbox shim
    5. `internal/repository/log_repository.go` — log shim
    6. `internal/repository/context_repository.go` — context shim

    额外（D-05）：
    7. `internal/repository/alert_repository.go` — alert shim（与 alert/ 子包重复）
    8. `internal/repository/status_repository.go` — status shim
    9. `internal/repository/task_repository.go` — task shim
    10. `internal/repository/interfaces.go` — 旧接口定义（随 shim 删除）

    废弃测试文件：
    11. `internal/repository/repository_test.go` — 测试已废弃的 flat API
    12. `internal/repository/repository_coverage_test.go` — coverage 填充测试

    **删除步骤：**
    1. 逐个删除所有 12 个文件（使用 `rm` 或文件删除工具）
    2. 每次删除 2-3 个文件后运行 `go build ./internal/...` 验证
    3. 全部删除后运行 `go build ./...` 验证整个项目
    4. 运行 `go test ./internal/repository/...` 验证剩余子包测试

    **验证：**
    - `go build ./...` 通过（无引用弃用类型）
    - `go test ./internal/repository/...` 通过（子包测试不受影响）
    - `internal/repository/` 目录下只剩子包目录 + `common/` + `AGENTS.md`

    **NOTES:**
    - `repository_test.go` 和 `repository_coverage_test.go` 是 `package repository` 测试，测试的是 shim 功能。删除 shim 后它们无意义。
    - 子包测试在各自子包目录中（如 `decision/decision_test.go`、`governance/repository_test.go`），不受影响。
    - `ontology_aware_adapter.go` 和 `ontology_aware_repo.go` 是独立的非弃用文件，保留在 repository/ 根目录。
  </action>
  <acceptance_criteria>
    - 12 个目标文件已从磁盘删除
    - `go build ./...` 编译通过
    - `go test ./internal/repository/...` 测试通过（子包测试）
    - `internal/repository/interfaces.go` 中定义的旧类型不再被任何文件引用
  </acceptance_criteria>
  <verify>
    <automated>test ! -f internal/repository/governance_repository.go && test ! -f internal/repository/decision_repository.go && test ! -f internal/repository/ontology_repository.go && test ! -f internal/repository/interfaces.go && go build ./... && go test ./internal/repository/...</automated>
  </verify>
  <done>弃用 repository shim 和 interface 文件已删除，编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 删除 migration_baseline 和 scripts/migration 目录 (HYG-07, D-12, D-13)</name>
  <files>
    migration_baseline/
    scripts/migration/
  </files>
  <read_first>
    无（文件将被删除）
  </read_first>
  <action>
    本任务删除迁移基线目录和 Python 迁移脚本。

    **删除目录：**
    1. `migration_baseline/` — 整个目录（递归删除，不归档，per D-13）
       - 包含 sqlite_schema.sql、README.md、table_counts.json、api_responses/、configs_snapshot/、pipeline_outputs/
    2. `scripts/migration/` — 整个目录（per D-12）
       - 包含 compare_api_baseline.py、compare_pipeline_baseline.py、export_*.py、introspect_schema.sql、verify_schema.py

    **Makefile 清理（二次检查）：**
    - 确认 Plan 01 已删除 `api-compare` target
    - 检查是否还有其他 target 引用了 `scripts/migration/` 下的文件

    **验证：**
    - `ls migration_baseline/` 返回 "No such file or directory"
    - `ls scripts/migration/` 返回 "No such file or directory"
    - Makefile 中无 `migration_baseline` 或 `scripts/migration` 路径引用
    - `go build ./...` 不受影响
  </action>
  <acceptance_criteria>
    - `migration_baseline/` 目录已删除
    - `scripts/migration/` 目录已删除
    - 无编译错误
  </acceptance_criteria>
  <verify>
    <automated>test ! -d migration_baseline && test ! -d scripts/migration && go build ./...</automated>
  </verify>
  <done>迁移基线目录和 Python 迁移脚本已删除</done>
</task>

<task type="auto">
  <name>Task 3: 更新文档中的 Python/SQLite 引用 (HYG-07, D-14)</name>
  <files>
    README.md
    AGENTS.md
    docs/migration/go-postgres-migration-plan.md
    docs/migration/phase-1-foundation.md
    docs/migration/phase-2-schema-design.md
    docs/migration/phase-3-parity-report.md
    docs/migration/phase-3-pipeline-migration-plan.md
    docs/migration/phase-4-api-migration-plan.md
    docs/migration/phase-5-governance-ontology-runtime-plan.md
    docs/migration/phase-6-decision-case-llm-context-plan.md
    docs/migration/phase-7-review-action-outbox-plan.md
  </files>
  <read_first>
    README.md
    AGENTS.md
  </read_first>
  <action>
    本任务更新文档中过时的 Python/SQLite 引用。

    **1. README.md**
    - 第 8 行和第 14 行提及 "Migrated from Python/SQLite to Go/PostgreSQL"——这是历史说明，保留（这是项目历史，不是残留引用）
    - 全文搜索 `python3`、`sqlite`（不区分大小写）、`SQLite`——删除或替换为当前状态
    - 例如如果提到 `scripts/migration/*.py` 或 `python3` 运行方式，删除这些引用
    - **注意：** 仅在描述当前操作方式时移除过时引用。项目历史描述（"已完成迁移"）保留。

    **2. AGENTS.md**
    - 搜索 `Python`、`SQLite`、`sqlite`、`migration_baseline`、`scripts/migration`
    - 删除或更新过时引用
    - 第 43 行（AGENTS.md 开头部分）："All Python code has been removed"——保留（准确的）

    **3. docs/migration/*.md**
    - 这些是迁移计划文档，属于历史记录——主要更新过时的路径引用
    - 但如引用 `sqlite_schema.sql` 或 `migration_baseline`，可保留为历史记录或删除相关段落
    - 原则：历史文档可保留，但移除后不再存在的文件路径引用应清理
    - **重点检查：** 引用 `migration_baseline/`、`scripts/migration/` 中现有文件的路径

    **4. 其他 docs/*.md 文件**
    - 快速搜索 `python3`、`migration_baseline`、`SQLite`——如发现明确的过时操作说明，一并清理

    **5. 验证**
    - `grep -r "python3" README.md AGENTS.md docs/ 2>/dev/null`——确认无残留
    - `grep -ri "migration_baseline" README.md AGENTS.md 2>/dev/null`——确认无残留
  </action>
  <acceptance_criteria>
    - `README.md` 中无 `python3` 命令引用
    - `AGENTS.md` 中无 `migration_baseline` 路径引用
    - `README.md` 中无对已删除文件的路径引用
    - 文档描述与当前代码库状态一致
  </acceptance_criteria>
  <verify>
    <automated>grep -c "python3" README.md | xargs test 0 -eq && grep -c "migration_baseline" AGENTS.md README.md 2>/dev/null | grep -v ":0$" | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>文档中的 Python/SQLite 过时引用已清理</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 文件删除和文档更新不涉及信任边界变更 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | repository 子包 | accept | 子包已在 Plan 02 中验证，删除 shim 后继续运行测试 |
</threat_model>

<verification>
1. `go build ./...` 编译通过，无引用弃用类型
2. `go test ./internal/repository/...` 子包测试通过
3. `internal/repository/` 目录下无弃用 flat shim 文件
4. `migration_baseline/` 和 `scripts/migration/` 已删除
5. 文档中无过时的 Python/SQLite 操作说明
</verification>

<success_criteria>
- 弃用的 repository shim（9 个 + interfaces.go + 2 个测试文件）全部删除
- migration_baseline/ 和 scripts/migration/ 目录删除
- README.md、AGENTS.md 无过时 Python/SQLite 引用
- `go build ./...` + `go test ./internal/repository/...` 通过
</success_criteria>

<output>
创建 `.planning/phases/03-code-hygiene-cleanup/03-03-SUMMARY.md`
</output>
