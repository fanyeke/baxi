---
phase: 03-code-hygiene-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/service/pipeline_service.go
  - Makefile
  - cmd/baxi-cli/llm.go
  - cmd/baxi-cli/main.go
  - internal/worker/worker.go
autonomous: true
requirements:
  - HYG-01
  - HYG-02
  - HYG-05
  - HYG-06
must_haves:
  truths:
    - "Pipeline preview displays Go commands instead of Python scripts"
    - "Makefile contains no references to Python scripts"
    - "Dead CLI subcommand llm.go is removed; main.go no longer dispatches 'llm'"
    - "Placeholder worker.go is removed; dispatch_worker.go is unaffected"
  artifacts:
    - path: internal/service/pipeline_service.go
      provides: "Pipeline preview with Go commands"
      min_lines: 140
      contains: "go run ./cmd/baxi-cli pipeline run"
    - path: Makefile
      provides: "Clean build targets without Python references"
      not_contains: "python3"
    - path: cmd/baxi-cli/main.go
      provides: "CLI entry point without llm dispatch"
      not_contains: "handleLLM"
  key_links:
    - from: internal/service/pipeline_service.go
      to: pipelineRegistry
      via: "Script → go run ./cmd/baxi-cli pipeline run"
    - from: cmd/baxi-cli/main.go
      to: switch "llm" → removed
      via: "No 'llm' case in dispatch"
---

<objective>
删除死代码、占位文件和 Python 脚本引用，完成快速清理工作。

Purpose: 清除 Phase 3 中独立、无依赖的清理项，为后续仓库迁移和最终清理做好准备。
Output: pipeline 预览使用 Go 命令、Makefile 无 Python 引用、llm.go 已删除、worker.go 已删除。
</objective>

<execution_context>
@/home/zzz/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/zzz/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-code-hygiene-cleanup/03-CONTEXT.md
@internal/service/pipeline_service.go
@Makefile
@cmd/baxi-cli/main.go
@cmd/baxi-cli/llm.go
@internal/worker/worker.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 Pipeline 预览命令 + 删除占位 worker.go (HYG-01, HYG-06)</name>
  <files>
    internal/service/pipeline_service.go
    internal/worker/worker.go
  </files>
  <read_first>
    internal/service/pipeline_service.go
    internal/worker/worker.go
  </read_first>
  <action>
    1. 修改 `internal/service/pipeline_service.go` 中的 `pipelineRegistry`：将 `Script` 字段值从 Python 脚本名改为 Go 命令。

    - "daily": `Script: "run_daily_pipeline.py"` → 改为非 Script 模式。因为 pipeline 主要通过 `cmd/baxi-cli` 运行，将预览命令改为 `go run ./cmd/baxi-cli pipeline run`。
    - 具体做法：将 `pipelineDefinition` 的 `Script` 和 `Args` 字段合并到新的 `Command` 字段。或者保持 struct 不变，修改 `PreviewPipelineRun` 方法中的命令构建逻辑。

    最简单的方式：修改 `PreviewPipelineRun` 方法（第 76-80 行）：
    - 将 `scriptPath := filepath.Join("scripts", pipe.Script)` 这行和 `command := fmt.Sprintf("python3 %s", scriptPath)` 替换为：
    - `command := fmt.Sprintf("go run ./cmd/baxi-cli pipeline run")`
    - 对于 `pipe.Args` 不为空的 case，保持命令一致（pipeline CLI 支持 --mode 等参数，但根据 D-11 只使用基本命令）。
    - 无论 pipelineType 如何，都返回相同的 Go 命令（不同 pipeline type 在 CLI 中由 baxi-cli 处理）。
    - 保留 `pipe.Duration`、`pipe.Description`、`requiredEnvVars` 等预览元数据。

    2. 删除 `internal/worker/worker.go` 文件。该文件有 `// Deprecated` 注释，且实际逻辑在 `internal/worker/dispatch_worker.go` 中。
    - 删除后检查 `go build ./...` 是否干净（worker.go 中的 `Worker` 和 `New` 仅由 `cmd/baxi-worker/main.go` 引用——验证后再删除）。如果 `cmd/baxi-worker/main.go` 引用了 `worker.New`，不要改 worker.go 的删除行为——检查并确认 `cmd/baxi-worker/main.go` 是否真正引用了它。
  </action>
  <acceptance_criteria>
    - `internal/service/pipeline_service.go` 中的 `PreviewPipelineRun` 返回的 `Command` 以 `go run ./cmd/baxi-cli pipeline run` 开头
    - `internal/worker/worker.go` 文件已从磁盘删除
    - `go build ./...` 编译通过，无缺失符号错误
  </acceptance_criteria>
  <verify>
    <automated>grep -q "go run ./cmd/baxi-cli pipeline run" internal/service/pipeline_service.go && test ! -f internal/worker/worker.go && go build ./...</automated>
  </verify>
  <done>Pipeline 预览返回 Go 命令，worker.go 已删除且编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 清理 Makefile 中的 Python 脚本引用 (HYG-02) + 删除 llm.go (HYG-05)</name>
  <files>
    Makefile
    cmd/baxi-cli/llm.go
    cmd/baxi-cli/main.go
  </files>
  <read_first>
    Makefile
    cmd/baxi-cli/main.go
    cmd/baxi-cli/llm.go
  </read_first>
  <action>
    1. **Makefile 清理 (HYG-02)**：删除所有引用 `python3` 的 target。
    - 删除 `api-compare` target（第 71-72 行 `api-compare:` → `python3 scripts/migration/compare_api_baseline.py`）
    - 删除 `llm-status` target（第 113-114 行 `llm-status:` → `go run ./cmd/baxi-cli llm status`）——因为 llm.go 将被删除
    - 删除 `llm-metrics` target（第 116-117 行）
    - 更新第一行的 `.PHONY` 列表：移除 `api-compare`、`llm-status`、`llm-metrics`
    - 保留 `pipeline-compare` target（使用 `go run ./cmd/baxi-cli pipeline validate`——已经是 Go 命令）
    - 保留 `verify-phase*` targets（使用 shell 脚本，不是 Python）
    - 不修改其他 target。

    2. **删除 llm.go (HYG-05)**：直接删除 `cmd/baxi-cli/llm.go` 文件。

    3. **更新 main.go**（per D-06）：
    - 第 28 行：`switch os.Args[1]` 的 case 列表中移除 `"llm"` ——改为 `case "pipeline", "governance", "decision":`
    - 第 71-72 行：删除 `case "llm": handleLLM(ctx, os.Args[2:], zapLog, pool.Pool, cfg)` 整个 case 分支
    - 第 106-107 行：删除 `printHelp()` 中 `llm status` 和 `llm metrics` 的 help 文本行
    - 不修改 `cmd/baxi-cli/client.go`（per D-07）
    - 不修改其他 `decision.go`、`pipeline.go`、`governance.go` 文件（per D-08）
  </action>
  <acceptance_criteria>
    - `Makefile` 中不包含 `python3` 字符串
    - `cmd/baxi-cli/llm.go` 文件已从磁盘删除
    - `cmd/baxi-cli/main.go` 中没有 `"llm"` case 和 `handleLLM` 调用
    - `make pipeline`、`make api-compare`（已删除）不影响其他 Go target
    - `go build ./...` 编译通过
  </acceptance_criteria>
  <verify>
    <automated>test ! -f cmd/baxi-cli/llm.go && grep -v '^#' Makefile | grep -c python3 | xargs test 0 -eq && grep '"llm"' cmd/baxi-cli/main.go | wc -l | xargs test 0 -eq && go build ./...</automated>
  </verify>
  <done>Makefile 无 Python 引用，llm.go 已删除，main.go 不再分派 llm，编译通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 本计划仅涉及代码和文档清理，不涉及信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | go mod 依赖 | accept | 本计划不添加新依赖包 |

**注意：** `api-compare` target 引用的 `python3 scripts/migration/compare_api_baseline.py` 文件将在 Plan 03 中随 `scripts/migration/` 目录一起删除。
</threat_model>

<verification>
1. `go build ./...` 通过，无编译错误
2. `make pipeline` 能够正确输出 Go 命令
3. 未删除 `client.go`，未修改 `decision.go`/`pipeline.go`/`governance.go`
</verification>

<success_criteria>
- Pipeline 预览命令显示为 `go run ./cmd/baxi-cli pipeline run`
- Makefile 中无 `python3` 引用
- llm.go 删除 + main.go 正确更新
- worker.go 删除
- `go build ./...` 通过
</success_criteria>

<output>
创建 `.planning/phases/03-code-hygiene-cleanup/03-01-SUMMARY.md`
</output>
