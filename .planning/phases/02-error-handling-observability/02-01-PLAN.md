---
phase: 02-error-handling-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/middleware/error.go
  - internal/api/dto/error.go
  - internal/api/handler/error_helper.go
  - internal/api/middleware/error_test.go
  - internal/api/handler/error_helper_test.go
autonomous: true
requirements: [ERR-01, ERR-02, ERR-04]

must_haves:
  truths:
    - "新增错误码常量 CONFLICT 和 SERVICE_UNAVAILABLE 可在整个代码库中使用"
    - "错误响应包含 details 字段用于携带字段级校验详情"
    - "数据库连接失败可被检测并映射为 503 Service Unavailable"
    - "有可复用的 writeValidationError 和 writeDatabaseError 辅助函数"
    - "新增类型和函数的单元测试全部通过"
  artifacts:
    - path: internal/api/middleware/error.go
      provides: "扩展的 APIError 结构体（含 Details 字段）、新错误码常量、WriteErrorWithDetails 函数"
      exports: ["APIError", "CONFLICT", "SERVICE_UNAVAILABLE", "WriteErrorWithDetails"]
      contains: "type APIError struct"
    - path: internal/api/dto/error.go
      provides: "FieldError 和 ValidationError 类型（新增文件）"
      exports: ["FieldError", "ValidationError"]
      contains: "type FieldError struct"
    - path: internal/api/handler/error_helper.go
      provides: "新的辅助函数和增强的 classifyError（DB 连接错误检测）"
      exports: ["writeValidationError", "writeDatabaseError", "isDatabaseConnectionError"]
      contains: "func isDatabaseConnectionError"
  key_links:
    - from: middleware/error.go
      to: handler/error_helper.go
      via: "WriteErrorWithDetails 被 writeValidationError 和 writeDatabaseError 调用"
      pattern: "middleware.WriteErrorWithDetails"
    - from: handler/error_helper.go
      to: dto/error.go
      via: "writeValidationError 使用 dto.FieldError 和 dto.ValidationError"
      pattern: "dto.FieldError"
---

<objective>
构建结构化错误处理的核心基础设施。

**Purpose:** 为整个 API 层提供统一的结构化错误响应能力，包括：新的错误码常量（CONFLICT, SERVICE_UNAVAILABLE）、APIError 的 details 字段、字段级验证错误类型、数据库连接失败检测。所有后续计划依赖此基础设施。

**Output:** 扩展后的 middleware/error.go、新增的 dto/error.go、增强的 handler/error_helper.go、对应的测试文件。
</objective>

<execution_context>
@/home/zzz/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/zzz/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@internal/api/middleware/error.go
@internal/api/handler/error_helper.go
@internal/api/handler/error_helper_test.go
</context>

<tasks>

<task type="auto">
  <name>任务 1: 扩展 middleware/error.go — 新常量 + Details 字段 + WriteErrorWithDetails</name>

  <read_first>
  internal/api/middleware/error.go
  </read_first>

  <action>
  在 internal/api/middleware/error.go 中执行以下修改（per D-02, D-03）：

  1. 在已有常量块（约第 27-35 行）中添加两个新常量：
     - `CONFLICT = "CONFLICT"` — 用于 HTTP 409 冲突响应
     - `SERVICE_UNAVAILABLE = "SERVICE_UNAVAILABLE"` — 用于 HTTP 503 响应

  2. 在 APIError 结构体（约第 18-24 行）中添加 Details 字段：
     - `Details interface{} json:"details,omitempty"` — 用于承载字段级验证错误详情等附加信息
     - 现有字段保持不变（保持向前兼容）

  3. 新增 WriteErrorWithDetails 函数：
     - 签名：`func WriteErrorWithDetails(w http.ResponseWriter, r *http.Request, status int, code string, message string, diagnosis string, action string, details interface{})`
     - 行为：构造 APIError 时填充 Details 字段，其余逻辑与 WriteError 相同
     - 注意：复用 WriteError 的 JSON 编码逻辑，避免重复代码

  4. 在 internal/api/middleware/error_test.go 中新增测试：
     - TestErrorConstants_CONFLICT — 验证 CONFLICT == "CONFLICT"
     - TestErrorConstants_SERVICE_UNAVAILABLE — 验证 SERVICE_UNAVAILABLE == "SERVICE_UNAVAILABLE"
     - TestWriteErrorWithDetails — 验证 WriteErrorWithDetails 响应中包含 details 字段
     - TestAPIError_DetailsOmitted — 验证当 details 为 nil 时 JSON 中不输出 details 字段
  </action>

  <verify>
  <automated>go test ./internal/api/middleware/ -run "TestErrorConstants|TestWriteErrorWithDetails|TestAPIError_DetailsOmitted" -v</automated>
  </verify>

  <acceptance_criteria>
  - CONFLICT 和 SERVICE_UNAVAILABLE 常量已定义，值分别为 "CONFLICT" 和 "SERVICE_UNAVAILABLE"
  - APIError 结构体包含 Details interface{} `json:"details,omitempty"` 字段
  - WriteErrorWithDetails 函数存在，签名正确，可在响应中包含 details 字段
  - 当 details 为 nil 时，JSON 响应中不输出 details 字段（omitempty 生效）
  - 新增的单元测试全部通过
  </acceptance_criteria>

  <done>
  middleware/error.go 包含 CONFLICT 和 SERVICE_UNAVAILABLE 常量；APIError 包含 Details 字段；WriteErrorWithDetails 函数已实现并通过测试。
  </done>
</task>

<task type="auto">
  <name>任务 2: 新增 internal/api/dto/error.go — FieldError + ValidationError 类型</name>

  <read_first>
  internal/api/dto/decision.go
  internal/api/dto/sandbox.go
  </read_first>

  <action>
  在 internal/api/dto/ 目录下新建 error.go 文件，定义字段级验证错误类型（per D-05）：

  1. FileError 结构体：
     ```go
     type FieldError struct {
         Field   string `json:"field"`
         Message string `json:"message"`
         Code    string `json:"code,omitempty"`
     }
     ```
     - Field: 出错的字段名（如 "source_type"）
     - Message: 人类可读的错误描述（如 "source_type is required"）
     - Code: 可选错误码（如 "required", "invalid_format", "invalid_uuid"）

  2. ValidationError 结构体：
     ```go
     type ValidationError struct {
         Fields []FieldError `json:"fields"`
     }
     ```
     - Fields: 字段级错误列表

  3. 遵循现有 dto 包的模式：package dto，只有类型定义，无业务逻辑

  注意：不在 error.go 中添加 Run 检查或测试检查代码。
  </action>

  <verify>
  <automated>go build ./internal/api/dto/ && go test ./internal/api/dto/ -run Test -v</automated>
  </verify>

  <acceptance_criteria>
  - internal/api/dto/error.go 文件已创建
  - FieldError 结构体包含 Field、Message、Code 三个 JSON 标签字段
  - ValidationError 结构体包含 Fields 字段
  - 包编译通过，现有 dto 测试仍然通过
  </acceptance_criteria>

  <done>
  dto/error.go 创建完成，FieldError 和 ValidationError 类型定义正确。
  </done>
</task>

<task type="auto">
  <name>任务 3: 增强 handler/error_helper.go — 新辅助函数 + 增强 classifyError + 测试</name>

  <read_first>
  internal/api/handler/error_helper.go
  internal/api/handler/error_helper_test.go
  </read_first>

  <action>
  在 internal/api/handler/error_helper.go 中执行以下修改（per D-01, D-04）：

  1. 在 import 块中添加 `"strings"` 和 `"baxi/internal/api/dto"`

  2. 新增 isDatabaseConnectionError 函数：
     ```go
     func isDatabaseConnectionError(err error) bool {
         if err == nil { return false }
         msg := strings.ToLower(err.Error())
         return strings.Contains(msg, "connection refused") ||
             strings.Contains(msg, "no such host") ||
             strings.Contains(msg, "connect: connection refused") ||
             strings.Contains(msg, "pool closed") ||
             strings.Contains(msg, "failed to connect")
     }
     ```

  3. 增强 classifyError——在两个已有 case 之后添加 DB 连接错误检测：
     ```go
     if isDatabaseConnectionError(err) {
         return http.StatusServiceUnavailable, middleware.SERVICE_UNAVAILABLE
     }
     ```
     保持原有 pgx.ErrNoRows 检测不变。

  4. 新增 writeValidationError 函数：
     ```go
     func writeValidationError(w http.ResponseWriter, r *http.Request, message string, fields []dto.FieldError) {
         details := map[string]interface{}{
             "fields": fields,
         }
         middleware.WriteErrorWithDetails(w, r, http.StatusBadRequest, middleware.VALIDATION_FAILED,
             message, defaultDiagnosis(middleware.VALIDATION_FAILED),
             defaultAction(middleware.VALIDATION_FAILED), details)
     }
     ```

  5. 新增 writeDatabaseError 函数：
     ```go
     func writeDatabaseError(w http.ResponseWriter, r *http.Request, err error, message string) {
         if isDatabaseConnectionError(err) {
             w.Header().Set("Retry-After", "5")
             middleware.WriteError(w, r, http.StatusServiceUnavailable, middleware.SERVICE_UNAVAILABLE,
                 message, "Database connection failed. The service may be temporarily unavailable.",
                 "Wait a few seconds and retry the request")
             return
         }
         writeError(w, r, http.StatusInternalServerError, middleware.INTERNAL_ERROR, message)
     }
     ```

  6. 在 defaultDiagnosis 和 defaultAction 的 switch 中添加新常量 case：
     - CONFLICT: diagnosis = "The request conflicts with the current state of the resource", action = "Review the resource state and retry with updated data"
     - SERVICE_UNAVAILABLE: diagnosis = "The service is temporarily unavailable", action = "Wait a moment and retry the request"

  7. 在 internal/api/handler/error_helper_test.go 中新增测试：
     - TestIsDatabaseConnectionError_ConnectionRefused — 检测 "connection refused"
     - TestIsDatabaseConnectionError_PoolClosed — 检测 "pool closed"
     - TestIsDatabaseConnectionError_NilError — nil 返回 false
     - TestIsDatabaseConnectionError_Generic — 普通错误返回 false
     - TestClassifyError_DBConnectionError — verify 503 SERVICE_UNAVAILABLE
     - TestWriteValidationError — 验证 JSON 包含 details.fields 数组
     - TestWriteValidationError_FieldDetails — 验证每个 field error 包含 proper field/message/code
     - TestDefaultDiagnosis_NewCodes — CONFLICT 和 SERVICE_UNAVAILABLE 的 diagnosis 非空
     - TestDefaultAction_NewCodes — CONFLICT 和 SERVICE_UNAVAILABLE 的 action 非空
  </action>

  <verify>
  <automated>go test ./internal/api/handler/ -run "TestIsDatabaseConnectionError|TestClassifyError_DBConnectionError|TestWriteValidationError|TestDefaultDiagnosis_NewCodes|TestDefaultAction_NewCodes" -v</automated>
  </verify>

  <acceptance_criteria>
  - isDatabaseConnectionError 能正确检测 "connection refused", "pool closed" 等错误
  - classifyError 对 DB 连接错误返回 (503, SERVICE_UNAVAILABLE)
  - writeValidationError 写入包含 details.fields 的 400 响应
  - writeDatabaseError 对 DB 连接错误设置 Retry-After 头部并返回 503
  - writeDatabaseError 对非 DB 错误回退到 500 INTERNAL_ERROR
  - defaultDiagnosis 和 defaultAction 包含 CONFLICT 和 SERVICE_UNAVAILABLE 的处理
  - 所有新增测试通过
  </acceptance_criteria>

  <done>
  handler/error_helper.go 包含所有新辅助函数且通过测试；测试覆盖 DB 检测、验证错误写入和分类。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP Request → API Handler | 未信任的 HTTP 请求进入 API 层 |
| DB Layer → Service Layer | 数据库返回的错误可能包含内部实现细节 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | middleware/error.go — APIError 响应 | mitigate | Details 字段使用 `omitempty`，仅当明确提供时才包含额外信息。数据库错误从不在 details 中暴露原始错误消息 |
| T-02-02 | Tampering | dto/error.go — FieldError/ValidationError | mitigate | 验证错误类型只用于向下游发送已格式化的错误详情，不用于反向解析或恢复操作 |
| T-02-03 | Denial of Service | handler/error_helper.go — isDatabaseConnectionError | mitigate | 字符串匹配检测范围限定为已知的错误子串，避免误匹配。Retry-After: 5 建议合理重试间隔 |
| T-02-SC | Tampering | go install/build | mitigate | 无新的包依赖引入；所有修改均在现有标准库和已引入的 pgx 范围内 |
</threat_model>

<verification>
1. `go build ./internal/api/...` 编译通过
2. `go test ./internal/api/middleware/ -run "TestErrorConstants|TestWriteErrorWithDetails|TestAPIError_DetailsOmitted" -v` 通过
3. `go test ./internal/api/dto/ -v` 通过
4. `go test ./internal/api/handler/ -run "TestIsDatabaseConnectionError|TestClassifyError|TestWriteValidationError|TestDefaultDiagnosis|TestDefaultAction" -v` 通过
</verification>

<success_criteria>
1. middleware/error.go 包含 CONFLICT、SERVICE_UNAVAILABLE 常量和带 Details 的 WriteErrorWithDetails
2. dto/error.go 包含 FieldError 和 ValidationError 类型
3. handler/error_helper.go 包含 isDatabaseConnectionError、writeValidationError、writeDatabaseError
4. classifyError 能检测 DB 连接错误并返回 503
5. defaultDiagnosis 和 defaultAction 覆盖所有新错误码
6. 所有新增测试通过
</success_criteria>

<artifacts_this_phase_produces>
### 新增符号（函数/类型/常量）

**middleware/error.go:**
- 常量: `CONFLICT`, `SERVICE_UNAVAILABLE`
- 结构体字段: `APIError.Details`
- 函数: `WriteErrorWithDetails`

**dto/error.go:**
- 类型: `FieldError`, `ValidationError`

**handler/error_helper.go:**
- 函数: `isDatabaseConnectionError`, `writeValidationError`, `writeDatabaseError`
</artifacts_this_phase_produces>

<output>
创建 `.planning/phases/02-error-handling-observability/02-01-SUMMARY.md`
</output>
