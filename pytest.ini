[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short --cov=. --cov-config=pyproject.toml --cov-report=term-missing
markers =
    integration: marks tests that require the real database or external resources
