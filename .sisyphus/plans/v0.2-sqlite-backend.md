# v0.2: SQLite-backed Rule Engine & Decision Backend

## TL;DR

> **Quick Summary**: Upgrade the current CSV/JSON file-driven rule decision sandbox to a SQLite-backed local database system, enabling structured storage of facts, states, rules, events, and triggers — while keeping existing CSV pipeline and Feishu sync intact.
>
> **Deliverables**:
> - `sql/schema.sql`, `sql/indexes.sql`, `sql/seed_rules.sql` — Database schema files
> - `scripts/db_init.py`, `db_ingest.py`, `db_calculate_metrics.py`, `db_rule_engine.py`, `db_generate_recommendations.py`, `db_export_feishu.py`, `db_trigger_simulator.py`, `db_status.py`, `run_db_pipeline.py` — Database pipeline scripts
> - `tests/test_db_schema.py`, `test_db_ingestion.py`, `test_db_metrics.py`, `test_db_rule_engine.py`, `test_db_feishu_export.py` — Test suite
> - `docs/v0.2_db_backend_design.md`, `docs/v0.2_db_runbook.md` — Documentation
> - `reports/v0.2_acceptance_report.md` — Acceptance report
> - `data/olist_ops.db` — SQLite database (NOT committed to Git)
>
> **Estimated Effort**: Large
> **Parallel Execution**: YES — 4 waves
> **Critical Path**: T1 → T2 → T3 → T4 → T5 → T6 → F1-F4

---

## Context

### Original Request
User wants to upgrade the file-driven rule decision sandbox (v0.1) to a SQLite-backed system (v0.2). The goal is NOT to add AI capabilities, connect Qoder, or do production cron jobs. Just: SQLite database, ingestion, metrics, rule engine, strategy/task status tables, Feishu CSV export, trigger simulation, and minimal tests.

### Interview Summary
**Key Discussions**:
- User explicitly wants a "旁路升级" (bypass upgrade): keep existing CSV/JSON pipeline, add SQLite backend in parallel
- Feishu sync chain to be reused, not rewritten
- Rule configuration to be reused from existing `config/alert_rules.yml`
- 12 core tables: pipeline_runs, ingestion_batches, dwd_order_level, dwd_item_level, metric_daily, metric_dimension_daily, alert_events, strategy_recommendations, action_tasks, review_retro, event_outbox, qoder_jobs
- Data source: `data/interim/order_level_base.csv` (104,903 lines) and `data/interim/item_level_base.csv` (119,258 lines)
- 3 ingestion modes: full, range (date interval), date (single day)

**Research Findings**:
- `data/interim/` contains the source CSVs (not `data/` as original spec said)
- `data/feishu/` already has CSV exports for 5 Feishu tables
- `config/alert_rules.yml` has 8+ alert rules (gmv_drop, late_delivery_spike, cancel_rate_spike, etc.)
- `tests/` directory has existing test files (test_data_outputs.py, etc.)
- No `sql/` directory exists yet

### Metis Review
**Identified Gaps** (addressed):
- Source file path corrected to `data/interim/`
- Explicit guardrails added: no AI, no Qoder, no cron, no full EDA refactor
- Line counts adjusted (CSV includes header row)
- Feishu field mapping (`config/feishu_field_mapping.yml`) must be respected for export

---

## Work Objectives

### Core Objective
Build a SQLite-backed decision backend that runs parallel to the existing CSV/JSON pipeline, with ingestion, metrics calculation, rule engine execution, Feishu CSV export, and trigger simulation — all verified by tests and dry-run.

### Concrete Deliverables
- `data/olist_ops.db` with 12 core tables populated
- 9 new Python scripts in `scripts/`
- 3 SQL files in `sql/`
- 5 test files in `tests/`
- 2 documentation files
- 1 acceptance report
- All existing CSV pipeline scripts UNTOUCHED

### Definition of Done
- [ ] `python3 scripts/db_init.py` creates `data/olist_ops.db` with all 12 tables
- [ ] `python3 scripts/db_ingest.py --mode full` loads 99,441 orders and 112,650 items
- [ ] `python3 scripts/db_calculate_metrics.py --mode full` computes ~634 rows in metric_daily
- [ ] `python3 scripts/db_rule_engine.py --mode full` generates alert_events, strategy_recommendations, action_tasks
- [ ] `python3 scripts/db_export_feishu.py --all` produces 5 CSVs matching `feishu_field_mapping.yml`
- [ ] `python3 scripts/db_trigger_simulator.py --dry-run` reads pending outbox, writes simulation log
- [ ] All 5 test files pass via `pytest`
- [ ] `sync_feishu_bitable.py --all --dry-run` passes with exported CSVs

### Must Have
- SQLite database with all 12 core tables
- Idempotent `db_init.py` (repeat execution without errors)
- All 3 ingestion modes (full, range, date)
- Rule engine reads from existing `config/alert_rules.yml`
- Deterministic event/recommendation/task IDs (no duplicates on re-run)
- 5 Feishu CSVs with correct field mapping

### Must NOT Have (Guardrails)
- NO AI / LLM integration
- NO Qoder connection
- NO production cron / scheduler
- NO refactoring of historical EDA scripts (phase01-07 scripts are FROZEN)
- NO rewriting Feishu as database
- NO GitHub Issue as business state source
- NO complex permission system
- NO full service API
- NO modifying existing CSV/JSON pipeline scripts
- NO adding features beyond the 12 tables + 9 scripts scope

---

## Verification Strategy

### Test Decision
- **Infrastructure exists**: YES
- **Automated tests**: YES (Tests-after) — add test tasks after implementation tasks
- **Framework**: pytest (existing `conftest.py`, `tests/__init__.py`)

### QA Policy
Every task MUST include agent-executed QA scenarios. Evidence saved to `.sisyphus/evidence/task-{N}-{scenario-slug}.{ext}`.

- **Frontend/UI**: N/A (no UI in v0.2)
- **CLI scripts**: Use Bash — run with `python3`, verify exit code, check DB tables or CSV outputs
- **Database**: Use Bash (sqlite3) — query tables, verify row counts, check constraints
- **Tests**: Use Bash (pytest) — run test files, check pass/fail

---

## Execution Strategy

### Parallel Execution Waves

```
Wave 1 (Start — schema + initialization + ingestion):
├── Task 1: SQL schema files [quick]
├── Task 2: db_init.py [quick]
├── Task 3: db_ingest.py [deep]
└── Task 4: db_status.py [quick]

Wave 2 (After Wave 1 — metrics + rule engine + recommendations):
├── Task 5: db_calculate_metrics.py [unspecified-high]
├── Task 6: db_rule_engine.py [deep]
└── Task 7: db_generate_recommendations.py [unspecified-high]

Wave 3 (After Wave 2 — export + trigger + pipeline wrapper):
├── Task 8: db_export_feishu.py [unspecified-high]
├── Task 9: db_trigger_simulator.py [unspecified-high]
├── Task 10: run_db_pipeline.py [quick]
├── Task 11: Test suite (5 test files) [unspecified-high]
├── Task 12: Documentation (2 docs) [writing]
└── Task 13: Acceptance report [writing]

Wave FINAL (After ALL tasks — 4 parallel reviews, then user okay):
├── Task F1: Plan compliance audit (oracle)
├── Task F2: Code quality review (unspecified-high)
├── Task F3: Real manual QA (unspecified-high)
└── Task F4: Scope fidelity check (deep)
-> Present results -> Get explicit user okay
```

### Dependency Matrix
- **1**: - → 2
- **2**: 1 → 3, 4
- **3**: 2 → 5, 6
- **4**: 2 → 8, 9
- **5**: 3 → 6, 10
- **6**: 3 → 7, 9, 10
- **7**: 6 → 8, 10
- **8**: 5, 6, 7 → 11, 13
- **9**: 4, 6 → 10, 13
- **10**: 5, 6, 7, 9 → 11
- **11**: 8, 10 → 13
- **12**: 1 → 13
- **13**: 8, 9, 11, 12 → FINAL
- **FINAL**: F1-F4 → user okay

### Agent Dispatch Summary
- **Wave 1**: 4 tasks — T1 → `quick`, T2 → `quick`, T3 → `deep`, T4 → `quick`
- **Wave 2**: 3 tasks — T5 → `unspecified-high`, T6 → `deep`, T7 → `unspecified-high`
- **Wave 3**: 6 tasks — T8 → `unspecified-high`, T9 → `unspecified-high`, T10 → `quick`, T11 → `unspecified-high`, T12 → `writing`, T13 → `writing`
- **FINAL**: 4 tasks — F1 → `oracle`, F2 → `unspecified-high`, F3 → `unspecified-high`, F4 → `deep`

---

- [ ] 1. SQL Schema Files

  **What to do**:
  - Create `sql/schema.sql` with all 12 core tables:
    - `pipeline_runs`, `ingestion_batches`
    - `dwd_order_level` (PK: order_id), `dwd_item_level` (PK: item_key = order_id + '_' + order_item_id)
    - `metric_daily` (PK: metric_date), `metric_dimension_daily` (composite PK)
    - `alert_events`, `strategy_recommendations`, `action_tasks`, `review_retro`
    - `event_outbox`, `qoder_jobs`
  - Create `sql/indexes.sql` with performance indexes
  - Create `sql/seed_rules.sql` with initial rule seeding if needed
  - All tables use SQLite TEXT for timestamps (ISO 8601)

  **Must NOT do**:
  - Do NOT add tables beyond the 12 specified
  - Do NOT add foreign key constraints (SQLite FK enforcement is off by default)
  - Do NOT add complex indexes beyond obvious lookups

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Writing SQL DDL statements is straightforward, schema is fully specified
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T2-T4)
  - **Parallel Group**: Wave 1 (with Tasks 2, 3, 4)
  - **Blocks**: Tasks 2, 12
  - **Blocked By**: None

  **References**:
  - User's spec — exact column definitions for all 12 tables
  - `data/interim/order_level_base.csv` — column names for dwd_order_level
  - `data/interim/item_level_base.csv` — column names for dwd_item_level

  **Acceptance Criteria**:
  - [ ] `sql/schema.sql` exists with all 12 CREATE TABLE statements
  - [ ] `sql/indexes.sql` exists with CREATE INDEX statements
  - [ ] `sqlite3 data/test_schema.db < sql/schema.sql` succeeds (12 tables created)
  - [ ] `sqlite3 data/test_schema.db .tables` returns 12 tables
  - [ ] `sqlite3 data/test_schema.db < sql/indexes.sql` succeeds

  **QA Scenarios**:
  ```
  Scenario: Schema creates all tables
    Tool: Bash (sqlite3)
    Steps:
      1. sqlite3:memory: < sql/schema.sql
      2. SELECT name FROM sqlite_master WHERE type='table' ORDER BY name
      3. Assert 12 table names returned
    Expected Result: pipeline_runs, ingestion_batches, dwd_order_level, dwd_item_level, metric_daily, metric_dimension_daily, alert_events, strategy_recommendations, action_tasks, review_retro, event_outbox, qoder_jobs
    Evidence: .sisyphus/evidence/task-1-schema-tables.txt
  ```

- [ ] 2. db_init.py — Database Initialization Script

  **What to do**:
  - Create `scripts/db_init.py` that:
    - Creates `data/` directory if not exists
    - Reads `sql/schema.sql` and `sql/indexes.sql`
    - Creates or recreates `data/olist_ops.db`
    - Logs success to stdout
    - Idempotent: repeat execution does NOT error
  - Command: `python3 scripts/db_init.py`

  **Must NOT do**:
  - Do NOT populate any data
  - Do NOT drop existing tables if db already exists (use CREATE TABLE IF NOT EXISTS)
  - Do NOT modify existing pipeline scripts

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Simple script — read SQL files, execute against SQLite
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T1, T3, T4)
  - **Parallel Group**: Wave 1 (with Tasks 1, 3, 4)
  - **Blocks**: Tasks 3, 4
  - **Blocked By**: Task 1

  **References**:
  - `sql/schema.sql`, `sql/indexes.sql` (from Task 1)
  - `scripts/config.py` — existing config patterns to follow
  - `sqlite3` Python module docs

  **Acceptance Criteria**:
  - [ ] `python3 scripts/db_init.py` creates `data/olist_ops.db`
  - [ ] Repeat `python3 scripts/db_init.py` does NOT raise errors
  - [ ] `data/olist_ops.db` has 12 tables after initialization
  - [ ] Exit code 0 on success

  **QA Scenarios**:
  ```
  Scenario: First run creates database
    Tool: Bash
    Steps:
      1. rm -f data/olist_ops.db
      2. python3 scripts/db_init.py
      3. sqlite3 data/olist_ops.db '.tables' | wc -l
    Expected Result: 12 tables, exit code 0
    Evidence: .sisyphus/evidence/task-2-first-run.txt

  Scenario: Repeat run is idempotent
    Tool: Bash
    Steps:
      1. python3 scripts/db_init.py
      2. python3 scripts/db_init.py
      3. Check exit code = 0
    Expected Result: Both runs succeed with exit code 0, no errors
    Evidence: .sisyphus/evidence/task-2-idempotent.txt
  ```

- [ ] 3. db_ingest.py — Data Ingestion Script

  **What to do**:
  - Create `scripts/db_ingest.py` that:
    - Supports `--mode full|range|date` arguments
    - full: truncate dwd_order_level and dwd_item_level, then import all CSV data
    - range: import only rows where purchase_date BETWEEN start AND end
    - date: import only rows where purchase_date = target date
    - Records ingestion in `ingestion_batches` table
    - Records run in `pipeline_runs` table
    - Generates batch_id as UUID
    - Constructs item_key as order_id + '_' + order_item_id
  - Commands:
    - `python3 scripts/db_ingest.py --mode full`
    - `python3 scripts/db_ingest.py --mode range --start 2017-11-01 --end 2017-11-30`
    - `python3 scripts/db_ingest.py --mode date --date 2017-11-24`

  **Must NOT do**:
  - Do NOT modify source CSV files
  - Do NOT import data into tables beyond dwd_order_level and dwd_item_level
  - Do NOT calculate metrics

  **Recommended Agent Profile**:
  - **Category**: `deep`
    - Reason: CSV ingestion with 100K+ rows requires careful handling of data types, batch inserts, and error recovery
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T1, T2, T4)
  - **Parallel Group**: Wave 1 (with Tasks 1, 2, 4)
  - **Blocks**: Tasks 5, 6
  - **Blocked By**: Task 2

  **References**:
  - `data/interim/order_level_base.csv` — source CSV (104,903 lines, includes header)
  - `data/interim/item_level_base.csv` — source CSV (119,258 lines, includes header)
  - `scripts/run_daily_pipeline.py` — existing pipeline patterns
  - `sqlite3` batch insert patterns

  **Acceptance Criteria**:
  - [ ] `--mode full` → dwd_order_level has 99,441 rows (header excluded)
  - [ ] `--mode full` → dwd_item_level has 112,650 rows (header excluded)
  - [ ] `ingestion_batches` has 1 record after full ingest
  - [ ] `pipeline_runs` has 1 record after full ingest
  - [ ] dwd_order_level.order_id has no duplicates
  - [ ] dwd_item_level.item_key has no duplicates
  - [ ] `--mode date --date 2017-11-24` imports subset only

  **QA Scenarios**:
  ```
  Scenario: Full mode ingestion
    Tool: Bash + sqlite3
    Steps:
      1. python3 scripts/db_init.py
      2. python3 scripts/db_ingest.py --mode full
      3. sqlite3 data/olist_ops.db 'SELECT COUNT(*) FROM dwd_order_level'
      4. sqlite3 data/olist_ops.db 'SELECT COUNT(*) FROM dwd_item_level'
      5. sqlite3 data/olist_ops.db 'SELECT COUNT(*) FROM ingestion_batches'
    Expected Result: dwd_order_level=99441, dwd_item_level=112650, ingestion_batches>=1
    Evidence: .sisyphus/evidence/task-3-full-ingest.txt

  Scenario: Date mode ingestion (subset)
    Tool: Bash + sqlite3
    Steps:
      1. python3 scripts/db_init.py
      2. python3 scripts/db_ingest.py --mode date --date 2017-11-24
      3. sqlite3 data/olist_ops.db 'SELECT COUNT(*) FROM dwd_order_level WHERE purchase_date = "2017-11-24"'
    Expected Result: Non-zero count, all loaded rows have purchase_date = 2017-11-24
    Evidence: .sisyphus/evidence/task-3-date-ingest.txt
  ```

- [ ] 4. db_status.py — Database Status Script

  **What to do**:
  - Create `scripts/db_status.py` that:
    - Reports row counts for all 12 tables
    - Shows last pipeline_run status and timestamp
    - Shows last ingestion_batch details
    - Outputs human-readable summary
  - Command: `python3 scripts/db_status.py`

  **Must NOT do**:
  - Do NOT modify any data
  - Do NOT write anything to database

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Simple read-only script
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T1-T3)
  - **Parallel Group**: Wave 1 (with Tasks 1, 2, 3)
  - **Blocks**: Task 8, 9
  - **Blocked By**: Task 2

  **References**:
  - `scripts/config.py` — existing patterns

  **Acceptance Criteria**:
  - [ ] `python3 scripts/db_status.py` exits with code 0
  - [ ] Output includes row counts for all 12 tables
  - [ ] Output includes last pipeline_run info

  **QA Scenarios**:
  ```
  Scenario: Status reports table counts
    Tool: Bash
    Steps:
      1. python3 scripts/db_init.py
      2. python3 scripts/db_status.py
      3. Grep output for table names and counts
    Expected Result: All 12 table names visible with row counts (0 after init)
    Evidence: .sisyphus/evidence/task-4-status.txt
  ```

- [ ] 5. db_calculate_metrics.py — Database Metrics Calculation

  **What to do**:
  - Create `scripts/db_calculate_metrics.py` that reads from SQLite and writes to `metric_daily`:
    - gmv: SUM(payment_value) grouped by purchase_date
    - order_count: COUNT(distinct order_id)
    - customer_count: COUNT(distinct customer_unique_id)
    - seller_count: COUNT(distinct seller_id from item_level join)
    - avg_order_value: gmv / order_count
    - freight_value: AVG/total freight value
    - avg_review_score: AVG(review_score) where review_score IS NOT NULL
    - low_review_rate: fraction of reviews with score <= 2
    - late_delivery_rate: fraction of delivered orders where is_late = 1
    - cancel_rate: fraction where is_cancelled = 1
    - payment_installment_rate: fraction where payment_installments > 1
    - marketing_seller_share: fraction of sellers from marketing dataset
  - Commands:
    - `python3 scripts/db_calculate_metrics.py --mode full`
    - `python3 scripts/db_calculate_metrics.py --mode range --start --end`
  - Writes to `metric_daily` and optionally `metric_dimension_daily` (leave dimension empty for v0.2)

  **Must NOT do**:
  - Do NOT modify source data
  - Do NOT generate alert_events (that's Task 6)

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: SQL aggregation queries across joined tables, requires understanding of existing metrics calculation logic
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T6, T7)
  - **Parallel Group**: Wave 2 (with Tasks 6, 7)
  - **Blocks**: Tasks 6, 8, 10
  - **Blocked By**: Task 3

  **References**:
  - `scripts/calculate_daily_metrics.py` — existing metrics calculation logic to replicate in SQL
  - `config/metrics.yml` — metric definitions
  - `data/interim/order_level_base.csv`, `data/interim/item_level_base.csv` — source data schema

  **Acceptance Criteria**:
  - [ ] `--mode full` → metric_daily has ~634 rows
  - [ ] All metric columns non-NULL for each row
  - [ ] metric_date is unique (PRIMARY KEY constraint)
  - [ ] gmv >= 0, order_count >= 0 for all rows
  - [ ] cancel_rate between 0 and 1
  - [ ] Values match existing daily_metrics_full.csv on sampling

  **QA Scenarios**:
  ```
  Scenario: Full metrics calculation
    Tool: Bash + sqlite3
    Steps:
      1. python3 scripts/db_init.py && python3 scripts/db_ingest.py --mode full
      2. python3 scripts/db_calculate_metrics.py --mode full
      3. sqlite3 data/olist_ops.db 'SELECT COUNT(*) FROM metric_daily'
      4. sqlite3 data/olist_ops.db 'SELECT MIN(gmv), MAX(gmv), MIN(cancel_rate), MAX(cancel_rate) FROM metric_daily'
    Expected Result: ~634 rows, gmv >= 0, cancel_rate in [0,1]
    Evidence: .sisyphus/evidence/task-5-full-metrics.txt
  ```

- [ ] 6. db_rule_engine.py — Database Rule Engine

  **What to do**:
  - Create `scripts/db_rule_engine.py` that:
    - Reads `config/alert_rules.yml` for rule definitions
    - Reads `metric_daily` from SQLite
    - For each rule:
      - Computes current window (7d avg) and baseline window (prev 14d avg) from metric_daily
      - Evaluates rule condition against windows
      - If triggered: writes to `alert_events`, `strategy_recommendations`, `action_tasks`, `event_outbox`
    - Generates deterministic IDs: event_id = rule_id + '_' + event_date
    - Must be idempotent: re-run does NOT create duplicate events
    - Command: `python3 scripts/db_rule_engine.py --mode full`
    - Command: `python3 scripts/db_rule_engine.py --rule cancel_rate_spike` (single rule)

  **Must NOT do**:
  - Do NOT modify metric_daily
  - Do NOT connect to external systems
  - Do NOT use LLM/AI

  **Recommended Agent Profile**:
  - **Category**: `deep`
    - Reason: Rule engine requires parsing config YAML, evaluating conditions, generating deterministic IDs, and coordinating writes to 4 tables
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T5, T7)
  - **Parallel Group**: Wave 2 (with Tasks 5, 7)
  - **Blocks**: Tasks 7, 8, 9, 10
  - **Blocked By**: Task 3, Task 5

  **References**:
  - `config/alert_rules.yml` — existing rule definitions
  - `scripts/run_alert_detection.py` — existing alert detection logic
  - `scripts/run_hindsight_decisions.py` — existing strategy/action generation
  - `scripts/config.py` — YAML loading patterns

  **Acceptance Criteria**:
  - [ ] alert_events has records after full run
  - [ ] strategy_recommendations.decision_source = 'heuristic' for all
  - [ ] action_tasks.task_source not NULL
  - [ ] event_outbox has pending records
  - [ ] Re-run does NOT create duplicate event_ids / recommendation_ids / task_ids
  - [ ] `--rule cancel_rate_spike` only runs that rule

  **QA Scenarios**:
  ```
  Scenario: Full rule engine execution
    Tool: Bash + sqlite3
    Steps:
      1. Full pipeline: db_init + db_ingest --mode full + db_calculate_metrics --mode full
      2. python3 scripts/db_rule_engine.py --mode full
      3. sqlite3 data/olist_ops.db 'SELECT COUNT(*) FROM alert_events'
      4. sqlite3 data/olist_ops.db 'SELECT COUNT(*) FROM strategy_recommendations WHERE decision_source="heuristic"'
      5. sqlite3 data/olist_ops.db 'SELECT COUNT(*) FROM event_outbox WHERE status="pending"'
    Expected Result: Non-zero counts in all 3 tables, all decision_source = heuristic
    Evidence: .sisyphus/evidence/task-6-full-rules.txt

  Scenario: Idempotent re-run
    Tool: Bash + sqlite3
    Steps:
      1. python3 scripts/db_rule_engine.py --mode full
      2. python3 scripts/db_rule_engine.py --mode full
      3. sqlite3 data/olist_ops.db 'SELECT COUNT(*) FROM alert_events'
      4. sqlite3 data/olist_ops.db 'SELECT event_id, COUNT(*) FROM alert_events GROUP BY event_id HAVING COUNT(*) > 1'
    Expected Result: Same count as first run, zero duplicate event_ids
    Evidence: .sisyphus/evidence/task-6-idempotent.txt
  ```

- [ ] 7. db_generate_recommendations.py — Strategy Recommendation Generator

  **What to do**:
  - Create `scripts/db_generate_recommendations.py` that:
    - Reads triggered alert_events from SQLite
    - Generates strategy_recommendations based on event type and action registry
    - Reads `config/action_registry.yml` for available action templates
    - Writes new strategy_recommendations with decision_source = 'heuristic'
    - Generates corresponding action_tasks in 'todo' status
    - Can be called standalone or as part of rule engine
  - Command: `python3 scripts/db_generate_recommendations.py`

  **Must NOT do**:
  - Do NOT re-run rule engine
  - Do NOT modify alert_events
  - Do NOT generate LLM-based recommendations

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: Reads events, maps to strategies via config, creates tasks — moderate complexity
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T5, T6)
  - **Parallel Group**: Wave 2 (with Tasks 5, 6)
  - **Blocks**: Tasks 8, 10
  - **Blocked By**: Task 6

  **References**:
  - `config/action_registry.yml` — action template definitions
  - `config/owner_mapping.yml` — role ownership mapping
  - `scripts/run_ai_decision_engine.py` — existing recommendation patterns

  **Acceptance Criteria**:
  - [ ] strategy_recommendations populated with decision_source = 'heuristic'
  - [ ] action_tasks created with status = 'todo'
  - [ ] Each recommendation links to an event_id
  - [ ] No duplicate recommendation_ids on re-run

  **QA Scenarios**:
  ```
  Scenario: Recommendation generation
    Tool: Bash + sqlite3
    Steps:
      1. Ensure alert_events has records (from rule engine)
      2. python3 scripts/db_generate_recommendations.py
      3. sqlite3 data/olist_ops.db 'SELECT COUNT(*) FROM strategy_recommendations'
      4. sqlite3 data/olist_ops.db 'SELECT COUNT(*) FROM action_tasks WHERE status="todo"'
    Expected Result: Non-zero recommendations and tasks, all linked to events
    Evidence: .sisyphus/evidence/task-7-recommendations.txt
  ```

- [ ] 8. db_export_feishu.py — Database to Feishu CSV Export

  **What to do**:
  - Create `scripts/db_export_feishu.py` that:
    - Reads from SQLite tables: metric_daily, alert_events, strategy_recommendations, action_tasks, review_retro
    - Exports to CSVs matching Feishu field mapping:
      - `data/feishu/daily_metrics_for_feishu.csv`
      - `data/feishu/alert_events_for_feishu.csv`
      - `data/feishu/strategy_recommendations_for_feishu.csv`
      - `data/feishu/action_tasks_for_feishu.csv`
      - `data/feishu/review_retro_for_feishu.csv`
    - Uses `config/feishu_field_mapping.yml` to ensure field names match
    - Command: `python3 scripts/db_export_feishu.py --all`

  **Must NOT do**:
    - Do NOT call Feishu API directly
    - Do NOT modify existing sync_feishu_bitable.py logic
    - Do NOT change CSV column names from feishu_field_mapping.yml

  **Recommended Agent Profile**:
    - **Category**: `unspecified-high`
      - Reason: Must match existing Feishu field mapping exactly; CSV column ordering and naming must be correct
  - **Skills**: []

  **Parallelization**:
    - **Can Run In Parallel**: YES (with T9-T13)
    - **Parallel Group**: Wave 3 (with Tasks 9-13)
    - **Blocks**: Tasks 11, 13
    - **Blocked By**: Tasks 5, 6, 7

  **References**:
    - `config/feishu_field_mapping.yml` — field name mapping for Feishu tables
    - `scripts/sync_feishu_bitable.py` — existing sync patterns
    - `data/feishu/daily_metrics_for_feishu.csv` — existing CSV format to match

  **Acceptance Criteria**:
    - [ ] 5 CSV files exist after `--all` export
    - [ ] CSV column headers match feishu_field_mapping.yml
    - [ ] Primary keys non-NULL and unique in each CSV
    - [ ] `sync_feishu_bitable.py --all --dry-run` passes with exported CSVs

  **QA Scenarios**:
  ```
  Scenario: Export all 5 Feishu CSVs
    Tool: Bash
    Steps:
      1. Full pipeline + rule engine run
      2. python3 scripts/db_export_feishu.py --all
      3. ls -la data/feishu/*_for_feishu.csv
      4. head -1 data/feishu/daily_metrics_for_feishu.csv
    Expected Result: 5 CSV files exist, headers match feishu_field_mapping.yml
    Evidence: .sisyphus/evidence/task-8-export-csvs.txt

  Scenario: Dry-run sync passes
    Tool: Bash
    Steps:
      1. Ensure Feishu CSVs exist
      2. python3 scripts/sync_feishu_bitable.py --all --dry-run 2>&1
    Expected Result: Exit code 0, no field mismatch errors
    Evidence: .sisyphus/evidence/task-8-dryrun-sync.txt
  ```

- [ ] 9. db_trigger_simulator.py — Trigger Simulator

  **What to do**:
    - Create `scripts/db_trigger_simulator.py` that:
      - Reads `event_outbox WHERE status = 'pending'` from SQLite
      - By target_channel classifies output:
        - github_issue: generates GitHub Issue payload JSON
        - feishu_cli: generates Feishu CLI command payload
        - local_cli: generates local command suggestion
        - manual: records for human handling
      - Writes to `data/system/trigger_simulation_log.csv`
      - `--dry-run`: only reads and logs
      - `--apply`: updates event_outbox.status to 'simulated'
      - Does NOT create real GitHub Issues or call Qoder
    - Commands:
      - `python3 scripts/db_trigger_simulator.py --dry-run`
      - `python3 scripts/db_trigger_simulator.py --apply`

  **Must NOT do**:
    - Do NOT call GitHub API
    - Do NOT call Qoder or any LLM
    - Do NOT send real Feishu messages

  **Recommended Agent Profile**:
    - **Category**: `unspecified-high`
      - Reason: Reads outbox, format outputs per channel, update status — moderate complexity
  - **Skills**: []

  **Parallelization**:
    - **Can Run In Parallel**: YES (with T8, T10-T13)
    - **Parallel Group**: Wave 3 (with Tasks 8-13)
    - **Blocks**: Task 10, 13
    - **Blocked By**: Task 4, Task 6

  **References**:
    - `scripts/sync_to_feishu.py` — existing Feishu output patterns

  **Acceptance Criteria**:
    - [ ] `--dry-run` reads pending events, writes simulation log
    - [ ] `--apply` updates event_outbox.status to 'simulated'
    - [ ] No real GitHub Issues created
    - [ ] No real Qoder calls made
    - [ ] trigger_simulation_log.csv has records with channel, payload, status columns

  **QA Scenarios**:
  ```
  Scenario: Dry-run simulator
    Tool: Bash
    Steps:
      1. Ensure event_outbox has pending records
      2. python3 scripts/db_trigger_simulator.py --dry-run
      3. cat data/system/trigger_simulation_log.csv
      4. sqlite3 data/olist_ops.db 'SELECT status FROM event_outbox LIMIT 1'
    Expected Result: Simulation log has records, event_outbox status unchanged
    Evidence: .sisyphus/evidence/task-9-dry-run.txt

  Scenario: Apply mode updates status
    Tool: Bash
    Steps:
      1. python3 scripts/db_trigger_simulator.py --apply
      2. sqlite3 data/olist_ops.db 'SELECT COUNT(*) FROM event_outbox WHERE status="simulated"'
    Expected Result: Non-zero simulated records, no pending for those IDs
    Evidence: .sisyphus/evidence/task-9-apply.txt
  ```

- [ ] 10. run_db_pipeline.py — Unified Pipeline Wrapper

  **What to do**:
    - Create `scripts/run_db_pipeline.py` that orchestrates:
      1. db_init (always)
      2. db_ingest --mode {mode}
      3. db_calculate_metrics --mode {mode}
      4. db_rule_engine --mode {mode}
      5. db_export_feishu --all
      6. db_trigger_simulator --dry-run
    - Passes --mode argument through to sub-scripts
    - Logs each step with timestamps
    - Stops on first error with clear message
    - Command: `python3 scripts/run_db_pipeline.py --mode full`

  **Must NOT do**:
    - Do NOT implement scheduling/cron
    - Do NOT add extra steps beyond the 6 listed

  **Recommended Agent Profile**:
    - **Category**: `quick`
      - Reason: Simple subprocess/sequential wrapper
  - **Skills**: []

  **Parallelization**:
    - **Can Run In Parallel**: YES (with T8-T13)
    - **Parallel Group**: Wave 3 (with Tasks 8-13)
    - **Blocks**: Tasks 11
    - **Blocked By**: Tasks 5, 6, 7, 9

  **References**:
    - `scripts/run_full_pipeline.py` — existing pipeline wrapper patterns

  **Acceptance Criteria**:
    - [ ] `python3 scripts/run_db_pipeline.py --mode full` completes end-to-end
    - [ ] All 6 sub-steps execute successfully
    - [ ] db_init called first, trigger_simulator called last

  **QA Scenarios**:
  ```
  Scenario: Full pipeline wrapper execution
    Tool: Bash
    Steps:
      1. python3 scripts/run_db_pipeline.py --mode full 2>&1
      2. Check exit code = 0
      3. Verify metric_daily, alert_events, action_tasks have data
    Expected Result: All 6 steps complete, exit code 0, data populated
    Evidence: .sisyphus/evidence/task-10-full-pipeline.txt
  ```

- [ ] 11. Test Suite — 5 Database Test Files

  **What to do**:
    - Create 5 test files under `tests/`:
      - `test_db_schema.py` — checks tables exist, key columns, PKs
      - `test_db_ingestion.py` — checks order/item data loaded, PK uniqueness
      - `test_db_metrics.py` — checks metric_daily has data, date unique, constraints
      - `test_db_rule_engine.py` — checks event_id unique, decision_source=heuristic, outbox has payload
      - `test_db_feishu_export.py` — checks 5 CSVs exist, PKs non-null, field mapping
    - All tests must pass via `pytest tests/test_db_*.py -v`
    - Follow existing test patterns from `tests/test_data_outputs.py`

  **Must NOT do**:
    - Do NOT test existing CSV pipeline functionality
    - Do NOT test Feishu API calls

  **Recommended Agent Profile**:
    - **Category**: `unspecified-high`
      - Reason: Writing comprehensive test suite across multiple modules
  - **Skills**: []

  **Parallelization**:
    - **Can Run In Parallel**: NO (depends on T8, T10 completing)
    - **Parallel Group**: Wave 3
    - **Blocks**: Task 13
    - **Blocked By**: Tasks 8, 10

  **References**:
    - `tests/test_data_outputs.py` — existing test patterns
    - `tests/conftest.py` — pytest configuration
    - `tests/test_decision_outputs.py` — decision output test patterns

  **Acceptance Criteria**:
    - [ ] 5 test files created
    - [ ] `pytest tests/test_db_*.py -v` passes with 0 failures
    - [ ] Each test file has at least 3 test functions

  **QA Scenarios**:
  ```
  Scenario: All DB tests pass
    Tool: Bash
    Steps:
      1. Run full pipeline
      2. pytest tests/test_db_schema.py tests/test_db_ingestion.py tests/test_db_metrics.py tests/test_db_rule_engine.py tests/test_db_feishu_export.py -v
    Expected Result: All tests pass, 0 failures
    Evidence: .sisyphus/evidence/task-11-all-tests-pytest.txt
  ```

- [ ] 12. Documentation — Design Doc + Runbook

  **What to do**:
    - Create `docs/v0.2_db_backend_design.md`:
      - Architecture overview
      - 12 table descriptions with purpose
      - Data flow diagram (text-based)
      - Ingestion mode documentation
      - Rule engine design
    - Create `docs/v0.2_db_runbook.md`:
      - Setup instructions
      - Running each script
      - Troubleshooting guide
      - Common commands
    - Reference existing `README.md` and `README_RUNBOOK.md` style

  **Must NOT do**:
    - Do NOT add new feature proposals
    - Do NOT rewrite v0.1 documentation

  **Recommended Agent Profile**:
    - **Category**: `writing`
      - Reason: Technical documentation writing
  - **Skills**: []

  **Parallelization**:
    - **Can Run In Parallel**: YES (with T8-T11, T13)
    - **Parallel Group**: Wave 3
    - **Blocks**: Task 13
    - **Blocked By**: Task 1

  **References**:
    - `README.md`, `README_RUNBOOK.md` — existing documentation style

  **Acceptance Criteria**:
    - [ ] docs/v0.2_db_backend_design.md exists with architecture + schema
    - [ ] docs/v0.2_db_runbook.md exists with usage instructions

  **QA Scenarios**:
  ```
  Scenario: Documents exist with required sections
    Tool: Bash
    Steps:
      1. test -f docs/v0.2_db_backend_design.md
      2. test -f docs/v0.2_db_runbook.md
      3. grep -c 'schema\|architecture\|table' docs/v0.2_db_backend_design.md
    Expected Result: Both files exist, design doc has schema references
    Evidence: .sisyphus/evidence/task-12-docs-exist.txt
  ```

- [ ] 13. Acceptance Report

  **What to do**:
    - Create `reports/v0.2_acceptance_report.md` that:
      - Lists all 10 acceptance criteria from the spec
      - Shows pass/fail for each
      - Includes evidence file references
      - Documents the state transition: v0.1 (file-driven) → v0.2 (database-driven)
    - Generated after all other tasks complete

  **Must NOT do**:
    - Do NOT fabricate test results
    - Do NOT skip any acceptance criteria

  **Recommended Agent Profile**:
    - **Category**: `writing`
      - Reason: Report writing based on actual test results
  - **Skills**: []

  **Parallelization**:
    - **Can Run In Parallel**: NO (last task)
    - **Parallel Group**: Wave 3
    - **Blocks**: None
    - **Blocked By**: Tasks 8, 9, 11, 12

  **References**:
    - All evidence files from .sisyphus/evidence/
    - v0.2 acceptance criteria list

  **Acceptance Criteria**:
    - [ ] reports/v0.2_acceptance_report.md exists
    - [ ] All 10 acceptance criteria listed with actual pass/fail results
    - [ ] Evidence files referenced and exist

  **QA Scenarios**:
  ```
  Scenario: Report exists with all criteria
    Tool: Bash
    Steps:
      1. test -f reports/v0.2_acceptance_report.md
      2. grep -c 'acceptance\|criterion\|criteri' reports/v0.2_acceptance_report.md
    Expected Result: Report exists with criterion references
    Evidence: .sisyphus/evidence/task-13-report.txt
  ```

---

## Final Verification Wave (MANDATORY — after ALL implementation tasks)

> 4 review agents run in PARALLEL. ALL must APPROVE. Present consolidated results to user and get explicit "okay" before completing.
>
> **Do NOT auto-proceed after verification. Wait for user's explicit approval before marking work complete.**
> **Never mark F1-F4 as checked before getting user's okay.** Rejection or user feedback -> fix -> re-run -> present again -> wait for okay.

- [ ] F1. **Plan Compliance Audit** — `oracle`
  Read the plan end-to-end. For each "Must Have": verify implementation exists (read file, sqlite3 query, run command). For each "Must NOT Have": search codebase for forbidden patterns — reject with file:line if found. Check evidence files exist in .sisyphus/evidence/. Compare deliverables against plan.
  Output: `Must Have [N/N] | Must NOT Have [N/N] | Tasks [N/N] | VERDICT: APPROVE/REJECT`

- [ ] F2. **Code Quality Review** — `unspecified-high`
  Run `python3 -m py_compile` on all new scripts. Review all changed files for: unused imports, console.log equivalent, commented-out code. Check AI slop: excessive comments, over-abstraction, generic names (data/result/item/temp). Check that no existing pipeline scripts were modified.
  Output: `Build [PASS/FAIL] | Lint [PASS/FAIL] | Files [N clean/N issues] | No existing files modified [YES/NO] | VERDICT`

- [ ] F3. **Real Manual QA** — `unspecified-high`
  Start from clean state. Execute EVERY QA scenario from EVERY task — follow exact steps, capture evidence. Test cross-task integration (ingest → metrics → rule engine → export → trigger). Test edge cases: re-run idempotent, empty database, single-rule execution. Save to `.sisyphus/evidence/final-qa/`.
  Output: `Scenarios [N/N pass] | Integration [N/N] | Edge Cases [N tested] | VERDICT`

- [ ] F4. **Scope Fidelity Check** — `deep`
  For each task: read "What to do", read actual diff (git log/diff). Verify 1:1 — everything in spec was built (no missing), nothing beyond spec was built (no creep). Check "Must NOT do" compliance. Detect cross-task contamination. Flag unaccounted changes. Specifically verify: NO existing scripts in scripts/ were modified.
  Output: `Tasks [N/N compliant] | Contamination [CLEAN/N issues] | Existing files modified [CLEAN/N files] | VERDICT`

---

## Commit Strategy

- **Wave 1**: `feat(db): add schema and init scripts` — sql/schema.sql, sql/indexes.sql, scripts/db_init.py
- **Wave 1**: `feat(db): add data ingestion script` — scripts/db_ingest.py, scripts/db_status.py
- **Wave 2**: `feat(db): add metrics calculation and rule engine` — scripts/db_calculate_metrics.py, scripts/db_rule_engine.py, scripts/db_generate_recommendations.py
- **Wave 3**: `feat(db): add export, trigger, pipeline wrapper, tests, docs` — scripts/db_export_feishu.py, scripts/db_trigger_simulator.py, scripts/run_db_pipeline.py, tests/test_db_*.py, docs/v0.2_db_backend_design.md, docs/v0.2_db_runbook.md, reports/v0.2_acceptance_report.md

---

## Success Criteria

### Verification Commands
```bash
python3 scripts/db_init.py  # Expected: data/olist_ops.db created, 12 tables
python3 scripts/db_ingest.py --mode full  # Expected: 99,441 orders, 112,650 items
python3 scripts/db_calculate_metrics.py --mode full  # Expected: ~634 metric_daily rows
python3 scripts/db_rule_engine.py --mode full  # Expected: alert_events, strategies, tasks populated
python3 scripts/db_export_feishu.py --all  # Expected: 5 Feishu CSVs produced
python3 scripts/db_trigger_simulator.py --dry-run  # Expected: trigger_simulation_log.csv written
pytest tests/test_db_*.py -v  # Expected: all pass, 0 failures
python3 scripts/sync_feishu_bitable.py --all --dry-run  # Expected: passes
```

### Final Checklist
- [ ] SQLite database initialized with 12 tables
- [ ] order_level_base / item_level_base imported to database
- [ ] metric_daily calculated from database
- [ ] alert_events generated from rule engine
- [ ] strategy_recommendations / action_tasks written to database
- [ ] event_outbox records pending trigger events
- [ ] 5 Feishu CSVs exported from database
- [ ] Feishu dry-run sync passes
- [ ] Trigger simulator outputs GitHub / Feishu CLI / local_cli payloads
- [ ] pytest database tests all pass
- [ ] NO existing pipeline scripts modified
