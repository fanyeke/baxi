# v0.3.1: 飞书展示与决策质量校准

## TL;DR

> **Quick Summary**: 修复 v0.3 维度数据的飞书展示层（schema/字段映射/CSV列名），接入模板引擎提升策略可读性，创建状态回流脚本和视图管理脚本，抽样评估策略/任务/outbox质量，形成验收报告——证明"对象级事件对人有用"。
>
> **Deliverables**:
> - 修复 `feishu_field_mapping.yml`（P0：key名称对齐）
> - 修复 `feishu_base_schema.yml`（补充缺失维度字段）
> - 新增 `channel_routing_rules.yml`
> - 修复 `db_export_feishu.py`（CSV列名与schema对齐 + action_tasks补target_object列）
> - `action_tasks` 表新增 `target_object_type`/`target_object_id` 列（schema migration）
> - 修改 `db_dimensional_rule_engine.py`（接入 `action_templates.yml` 模板引擎 + 填充target_object列）
> - 新增 `db_import_feishu_status.py`（飞书状态回写SQLite）
> - 新增 `create_feishu_views.py`（YAML驱动飞书视图创建）
> - 5个TDD测试文件（新增/扩展）
> - `.sisyphus/v0.3.1-acceptance.md` 验收报告

> **Estimated Effort**: Medium
> **Parallel Execution**: YES — 5 waves of parallel tasks
> **Critical Path**: Task 1 → Task 5 → Task 6 → Task 7 → Task 8 → Task 14 → F1-F4

---

## Context

### Original Request
用户提供了完整的 v0.3.1 规范文档，涵盖6个部分：同步v0.3结果到飞书、飞书表字段校准、飞书视图和看板校准、策略与任务质量抽样评估、状态回流测试、v0.3.1验收报告。

### Interview Summary
**Key Discussions**:
- **测试策略**: TDD（先写测试，RED-GREEN-REFACTOR）。所有代码修改必须有测试覆盖。
- **飞书视图创建**: YAML配置 + 脚本自动化创建（`create_feishu_views.py`），非手动手动UI。
- **db_import_feishu_status.py**: 纳入v0.3.1，实现飞书状态→SQLite完整闭环。
- **模板引擎修复**: 纳入v0.3.1，让 `db_dimensional_rule_engine.py` 使用 `action_templates.yml` 模板替代内联生成。

**Research Findings** (via 3 parallel explore agents):
- `db_dimensional_rule_engine.py` (247行) 已完整实现4路输出，含 `object_type`/`object_id`/`affected_gmv`/`impact_score`
- `db_export_feishu.py` 导出 alert_events 有维度列，但 action_tasks 缺 `target_object_type`/`target_object_id`
- `sync_feishu_bitable.py` 有 HUMAN_FIELDS 保护机制（`{status, feedback, completed_at}` 不可覆盖）
- `feishu_field_mapping.yml` 使用 `metric_alerts`/`execution_reviews` 作为 key，但 `sync_feishu_bitable.py` VALID_TABLES 使用 `alert_events`/`review_retro`——这是 P0 同步阻断级 bug
- `feishu_base_schema.yml` 缺 11 个维度相关字段
- `action_templates.yml` 6个模板已定义但未接入规则引擎
- 无 `channel_routing_rules.yml`、无 `db_import_feishu_status.py`
- 现有 11 个 pytest 测试文件，含 `test_db_dimensional_rules.py` (15 tests) 和 `test_db_event_outbox_routing.py` (10 tests)

### Metis Review
**Identified Gaps** (all addressed in plan):
- P0: `feishu_field_mapping.yml` key名称与 `sync_feishu_bitable.py` VALID_TABLES 不匹配 → Task 1
- P0: CSV列名与schema field_ids不一致 → Task 5（统一方向：CSV列名→schema field_ids）
- 复用 `db_generate_recommendations.py` 的模板引擎（`load_dimensional_templates`/`RULE_TO_TEMPLATE`/`_render_template`）→ Task 6
- `db_import_feishu_status.py` 只用UPDATE(不INSERT)，匹配key为 `task_id` → Task 8
- 需为 `review_retro` 也定义 HUMAN_FIELDS → Task 2
- action_tasks 表需新增 `target_object_type`/`target_object_id` 列 → Task 4

---

## Work Objectives

### Core Objective
让 v0.3 生成的 seller/category/region 维度异常、策略和任务在飞书中真正可读、可处理、可复盘——通过修复展示层、接入模板引擎、创建闭环工具和抽样验证来证明"对象级事件对人有用"。

### Concrete Deliverables
- 修复后的 `config/feishu_field_mapping.yml`（key名称+源路径对齐）
- 修复后的 `config/feishu_base_schema.yml`（补充11+维度字段）
- 新增 `config/channel_routing_rules.yml`
- `action_tasks` 表 migration（新增 target_object_type/id 列）
- 修改后的 `scripts/db_export_feishu.py`（CSV列名对齐+action_tasks补列）
- 修改后的 `scripts/db_dimensional_rule_engine.py`（模板引擎接入+target_object填充）
- 新增 `scripts/db_import_feishu_status.py`
- 新增 `scripts/create_feishu_views.py`
- 5个测试文件（新增/扩展）
- `.sisyphus/v0.3.1-acceptance.md`

### Definition of Done
- [ ] `python3 scripts/run_db_pipeline.py --mode full --dimensional` 成功完成
- [ ] `python3 scripts/db_export_feishu.py --all` 产出5张CSV，alert_events含 `object_type`/`object_id`/`affected_orders`/`affected_gmv`/`impact_score`，action_tasks含 `target_object_type`/`target_object_id`
- [ ] `python3 scripts/sync_feishu_bitable.py --all --dry-run` 通过
- [ ] `python3 scripts/pull_feishu_status.py --dry-run` 通过
- [ ] `python3 scripts/db_import_feishu_status.py --dry-run` 通过
- [ ] `python3 scripts/create_feishu_views.py --dry-run` 通过
- [ ] `python -m pytest tests/ -v` 全部通过（≥80% 覆盖率）
- [ ] 策略抽样平均分 ≥ 8/10
- [ ] 任务抽样平均分 ≥ 6.5/8
- [ ] outbox 分流合理率 ≥ 80%
- [ ] 3条任务状态手动修改后能回流且不被覆盖
- [ ] `.sisyphus/v0.3.1-acceptance.md` 存在且全部AC通过

### Must Have
- 飞书字段映射 key 名称与 sync 脚本 VALID_TABLES 完全对齐
- alert_events 飞书表能展示：对象类型、对象ID、影响订单数、影响GMV、影响分
- 策略文本使用 action_templates.yml 模板生成（非内联字符串）
- 任务表包含 target_object_type 和 target_object_id
- HUMAN_FIELDS 保护机制验证通过
- 所有新脚本默认 --dry-run 模式

### Must NOT Have (Guardrails)
- 不接 Qoder（`qoder_jobs` 表保持为空）
- 不接大模型（无 LLM API 调用）
- 不做定时任务
- 不真实创建 GitHub Issue
- 不真实调用 Feishu CLI（`--dry-run` 是所有新脚本的默认模式）
- 不做新的复杂业务模型
- 不继续扩展过多维度（仅 seller/category/region）
- 不修改 `action_templates.yml` 中的模板文本内容（仅接入使用）
- 不实现 channel routing 引擎逻辑（config only）
- 不修改现有 `HUMAN_FIELDS` 定义内容

---

## Verification Strategy

### Pre-Flight Checklist (verified during planning)
- [x] `db_generate_recommendations.py` exports: `load_dimensional_templates()`, `RULE_TO_TEMPLATE`, `_render_template()`, `_compute_confidence()` — all confirmed (lines 14-21, 48-53, 62-67, 70-81)
- [x] `dimensional_alert_rules.yml` has `target_channel` for all 6 rules — confirmed (lines 9, 19, 30, 40, 50, 60)
- [x] `feishu_field_mapping.yml` P0 key mismatch (`metric_alerts`/`execution_reviews` vs `alert_events`/`review_retro`) — confirmed real bug
- [x] Evidence directory: `mkdir -p .sisyphus/evidence/` — Sisyphus MUST create this BEFORE Wave 1 execution (all QA scenarios write here)

> **ZERO HUMAN INTERVENTION** — ALL verification is agent-executed. No exceptions.

### Test Decision
- **Infrastructure exists**: YES (pytest, 11 test files)
- **Automated tests**: TDD — tests written BEFORE implementation, RED-GREEN-REFACTOR cycle
- **Framework**: pytest (existing `tests/` directory with conftest.py)

### QA Policy
Every task MUST include agent-executed QA scenarios.
Evidence saved to `.sisyphus/evidence/task-{N}-{scenario-slug}.{ext}`.

- **CLI/Backend**: Use Bash — Run pipeline commands, validate CSV output, check DB state
- **API verification**: Use Bash (curl) for Feishu API dry-run validation
- **CSV/DB verification**: Use Bash (python -c) to inspect CSV contents and SQLite tables

---

## Execution Strategy

### Parallel Execution Waves

```
Wave 1 (Foundation — YAML Config Fixes, MAX PARALLEL):
├── Task 1: Fix feishu_field_mapping.yml [quick]
├── Task 2: Fix feishu_base_schema.yml [quick]
└── Task 3: Create channel_routing_rules.yml [quick]

Wave 2 (DB Schema + CSV Alignment):
├── Task 4: Add target_object columns to action_tasks table [quick]
└── Task 5: Align db_export_feishu.py CSV columns with schema field_ids [quick]

Wave 3 (Template Engine + Export Fix):
├── Task 6: Wire action_templates.yml into db_dimensional_rule_engine.py [unspecified-high]
└── Task 7: Add target_object columns to action_tasks export [quick]

Wave 4 (New Scripts, MAX PARALLEL):
├── Task 8: Create db_import_feishu_status.py [unspecified-high]
└── Task 9: Create create_feishu_views.py [unspecified-high]

Wave 5 (Tests — TDD, MAX PARALLEL):
├── Task 10: Extend/update test_db_feishu_export.py [quick]
├── Task 11: Extend test_db_rule_engine.py [quick]
├── Task 12: Create test_db_import_feishu_status.py [quick]
└── Task 13: Create test_feishu_views.py [quick]

Wave 6 (Pipeline Run + Sampling Assessment, MAX PARALLEL):
├── Task 14: Run pipeline, verify data, sample strategies [unspecified-high]
├── Task 15: Sample tasks & outbox quality [unspecified-high]
└── Task 16: Status pullback round-trip test [unspecified-high]

Wave 7 (Acceptance Report):
└── Task 17: Generate v0.3.1 acceptance report [writing]

Wave FINAL (After ALL tasks — 4 parallel reviews, then user okay):
├── Task F1: Plan Compliance Audit [oracle]
├── Task F2: Code Quality Review [unspecified-high]
├── Task F3: Real Manual QA [unspecified-high]
└── Task F4: Scope Fidelity Check [deep]

Critical Path: Task 1 → Task 5 → Task 6 → Task 7 → Task 14 → Task 17 → F1-F4
Parallel Speedup: ~60% faster than sequential (Waves 1, 4, 5, 6 all run multiple tasks in parallel)
Max Concurrent: 4 (Waves 5 & 6)
```

### Dependency Matrix

| Task | Blocked By | Blocks | Wave |
|------|-----------|--------|------|
| 1 | — | 5 | 1 |
| 2 | — | 5, 9 | 1 |
| 3 | — | — | 1 |
| 4 | — | 6, 7 | 2 |
| 5 | 1, 2 | 10, 14 | 2 |
| 6 | 4 | 7, 11, 14 | 3 |
| 7 | 4, 6 | 10, 14 | 3 |
| 8 | — | 12, 16 | 4 |
| 9 | 2 | 13 | 4 |
| 10 | 5, 7 | 17 | 5 |
| 11 | 6 | 17 | 5 |
| 12 | 8 | 17 | 5 |
| 13 | 9 | 17 | 5 |
| 14 | 5, 6, 7 | 17 | 6 |
| 15 | 14 | 17 | 6 |
| 16 | 8 | 17 | 6 |
| 17 | 10-16 | F1-F4 | 7 |
| F1-F4 | 17 | — | FINAL |

### Agent Dispatch Summary

| Wave | Tasks | Agent Profiles |
|------|-------|---------------|
| 1 | T1-T3 | 3× `quick` |
| 2 | T4-T5 | 2× `quick` |
| 3 | T6 | `unspecified-high`, T7 `quick` |
| 4 | T8-T9 | 2× `unspecified-high` |
| 5 | T10-T13 | 4× `quick` |
| 6 | T14-T16 | 3× `unspecified-high` |
| 7 | T17 | `writing` |
| FINAL | F1-F4 | `oracle` + `unspecified-high` + `unspecified-high` + `deep` |

---

## TODOs

- [ ] 1. Fix `feishu_field_mapping.yml` — align key names with VALID_TABLES

  **What to do**:
  - Rename `metric_alerts` key → `alert_events` (matches `sync_feishu_bitable.py` line 31: `VALID_TABLES`)
  - Rename `execution_reviews` key → `review_retro` (matches VALID_TABLES)
  - Fix `alert_events.local_table` from `data/ads/metric_alerts.csv` → `data/feishu/alert_events_for_feishu.csv`
  - Fix `strategy_recommendations.local_table` from `data/aip/aip_action_recommendations.json` → `data/feishu/strategy_recommendations_for_feishu.csv`
  - Fix `action_tasks.local_table` from `data/outputs/action_tasks.json` → `data/feishu/action_tasks_for_feishu.csv`
  - Add missing dimensional fields to `alert_events` field mapping: `rule_id`, `event_date`, `metric_name`, `object_type`, `object_id`, `change_rate`, `sample_size`, `affected_orders`, `affected_gmv`, `impact_score`
  - Add missing fields to `strategy_recommendations` mapping: `decision_source`, `rule_id`, `target_object_type`, `target_object_id`, `confidence`, `approval_status`, `execution_status`, `success_metric`, `created_at`
  - Add missing fields to `action_tasks` mapping: `recommendation_id`, `event_id`, `task_source`, `target_object_type`, `target_object_id`, `due_at`, `created_at`
  - Add `review_retro` HUMAN_FIELDS context comment (not actual field change — this is just documentation)

  **Must NOT do**:
  - Do NOT change the existing field mappings that are correct
  - Do NOT add new tables beyond the 5 existing
  - Do NOT modify `daily_metrics` mapping (out of scope unless broken)

  **Recommended Agent Profile**:
  > Select category + skills based on task domain. Justify each choice.
  - **Category**: `quick`
    - Reason: Single YAML file edit with clear key→key mapping. Pure config change, no logic.
  - **Skills**: [`writing`]
    - `writing`: For clear YAML field comments explaining dimensional semantics

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with Tasks 2, 3)
  - **Blocks**: Task 5
  - **Blocked By**: None

  **References**:
  - `config/feishu_field_mapping.yml:1-87` — Current field mapping with incorrect key names
  - `scripts/sync_feishu_bitable.py:31` — VALID_TABLES list: `["daily_metrics", "alert_events", "strategy_recommendations", "action_tasks", "review_retro"]`
  - `scripts/db_export_feishu.py:19-52` — EXPORT_SPECS with actual column names to map

  **Acceptance Criteria**:
  - [ ] `feishu_field_mapping.yml` top-level keys are: `daily_metrics`, `alert_events`, `strategy_recommendations`, `action_tasks`, `review_retro`
  - [ ] All `local_table` paths end with `*_for_feishu.csv` in `data/feishu/`
  - [ ] `alert_events.fields` contains at minimum: `rule_id`, `metric_name`, `object_type`, `object_id`, `affected_orders`, `affected_gmv`, `impact_score`
  - [ ] `strategy_recommendations.fields` contains: `decision_source`, `rule_id`, `target_object_type`, `target_object_id`, `confidence`, `success_metric`
  - [ ] `action_tasks.fields` contains: `target_object_type`, `target_object_id`

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: All mapping keys match VALID_TABLES
    Tool: Bash (python -c)
    Preconditions: Fixed feishu_field_mapping.yml exists
    Steps:
      1. python3 -c "import yaml; m=yaml.safe_load(open('config/feishu_field_mapping.yml')); keys=list(m.keys()); assert 'metric_alerts' not in keys, 'metric_alerts still present'; assert 'alert_events' in keys, 'alert_events missing'; assert 'execution_reviews' not in keys, 'execution_reviews still present'; assert 'review_retro' in keys; print('PASS: all keys correct')"
      2. python3 -c "from scripts.sync_feishu_bitable import VALID_TABLES; import yaml; m=yaml.safe_load(open('config/feishu_field_mapping.yml')); assert set(m.keys()) == set(VALID_TABLES), f'Mismatch: {set(m.keys()) ^ set(VALID_TABLES)}'; print('PASS: exact match')"
    Expected Result: Both assertions pass without error
    Failure Indicators: AssertionError with key mismatch details
    Evidence: .sisyphus/evidence/task-1-key-alignment.txt

  Scenario: alert_events has dimensional field mappings
    Tool: Bash (python -c)
    Preconditions: Fixed feishu_field_mapping.yml exists
    Steps:
      1. python3 -c "import yaml; m=yaml.safe_load(open('config/feishu_field_mapping.yml')); ae=m['alert_events']['fields']; required=['object_type','object_id','affected_orders','affected_gmv','impact_score']; missing=[r for r in required if r not in ae]; assert not missing, f'Missing fields: {missing}'; print('PASS: all dimensional fields mapped')"
    Expected Result: "PASS: all dimensional fields mapped"
    Failure Indicators: Lists missing field names
    Evidence: .sisyphus/evidence/task-1-dimensional-fields.txt
  ```

  **Commit**: YES (groups with Task 2, 3 in Wave 1)
  - Message: `fix: align feishu_field_mapping keys with VALID_TABLES`
  - Files: `config/feishu_field_mapping.yml`

- [ ] 2. Fix `feishu_base_schema.yml` — add missing dimensional fields

  **What to do**:
  - **alert_events table**: Add field definitions for: `rule_id` (text), `event_date` (date), `change_rate` (number), `sample_size` (number), `affected_orders` (number), `affected_gmv` (number, format: currency), `impact_score` (number)
  - **recommendations table**: Add field definitions for: `decision_source` (text), `rule_id` (text), `confidence` (text), `success_metric` (text). Split `target_object` into `target_object_type` (text) and `target_object_id` (text) — deprecate/remove single `target_object` field
  - **action_tasks table**: Add field definitions for: `target_object_type` (text), `target_object_id` (text), `recommendation_id` (text), `event_id` (text), `created_at` (datetime)
  - **review_retro table**: Add field definitions for: `status` (single_select, options: `[draft, in_review, completed]`, default: `draft`), `feedback` (text_long)

  **Must NOT do**:
  - Do NOT remove existing fields
  - Do NOT change existing field types
  - Do NOT create new tables
  - Do NOT modify `daily_metrics` schema

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Pure YAML schema additions following existing patterns. No logic changes.
  - **Skills**: []
    - Skills Evaluated but Omitted: None needed — straightforward config edit

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with Tasks 1, 3)
  - **Blocks**: Tasks 5, 9
  - **Blocked By**: None

  **References**:
  - `config/feishu_base_schema.yml:55-106` — Current alert_events schema (missing dimensional fields)
  - `config/feishu_base_schema.yml:107-152` — Current action_tasks schema
  - `config/feishu_base_schema.yml:154-206` — Current recommendations schema
  - `config/feishu_base_schema.yml:207-238` — Current review_retro schema
  - `scripts/db_export_feishu.py:27-31` — alert_events export columns (source of truth for field names)
  - `scripts/db_export_feishu.py:33-38` — strategy_recommendations export columns
  - `sql/schema.sql:alert_events` — DB column names (field_ids should match these)

  **Acceptance Criteria**:
  - [ ] alert_events schema has `affected_orders`, `affected_gmv`, `impact_score`, `change_rate`, `sample_size` fields defined
  - [ ] recommendations schema has `decision_source`, `rule_id`, `confidence`, `success_metric`, `target_object_type`, `target_object_id` fields
  - [ ] action_tasks schema has `target_object_type`, `target_object_id`, `created_at` fields
  - [ ] review_retro schema has `status` and `feedback` fields
  - [ ] No existing fields removed from any table

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: alert_events has all dimensional fields
    Tool: Bash (python -c)
    Preconditions: Fixed feishu_base_schema.yml exists
    Steps:
      1. python3 -c "
  import yaml
  s=yaml.safe_load(open('config/feishu_base_schema.yml'))
  ae=[t for t in s['tables'] if t['table_id']=='alert_events'][0]
  fields={f['field_id'] for f in ae['fields']}
  required={'affected_orders','affected_gmv','impact_score','change_rate','sample_size','rule_id','event_date'}
  missing=required-fields
  assert not missing, f'Missing alert_events fields: {missing}'
  print(f'PASS: {len(fields)} total fields, all dimensional present')
  "
    Expected Result: "PASS: N total fields, all dimensional present"
    Failure Indicators: Lists missing field names
    Evidence: .sisyphus/evidence/task-2-alert-fields.txt

  Scenario: recommendations has target_object_type/id split
    Tool: Bash (python -c)
    Preconditions: Fixed feishu_base_schema.yml exists
    Steps:
      1. python3 -c "
  import yaml
  s=yaml.safe_load(open('config/feishu_base_schema.yml'))
  rec=[t for t in s['tables'] if t['table_id']=='recommendations'][0]
  fields={f['field_id'] for f in rec['fields']}
  required={'decision_source','rule_id','confidence','success_metric','target_object_type','target_object_id'}
  missing=required-fields
  assert not missing, f'Missing rec fields: {missing}'
  print('PASS: all strategy fields present')
  "
    Expected Result: "PASS: all strategy fields present"
    Failure Indicators: Lists missing field names
    Evidence: .sisyphus/evidence/task-2-rec-fields.txt
  ```

  **Commit**: YES (groups with Task 1, 3)
  - Message: `fix: add missing dimensional fields to feishu_base_schema`
  - Files: `config/feishu_base_schema.yml`

- [ ] 3. Create `channel_routing_rules.yml` config

  **What to do**:
  - Create `config/channel_routing_rules.yml` with routing rules extracted from `config/dimensional_alert_rules.yml`
  - Structure: top-level `rules` key, each rule maps to channel routing by severity
  - Include 6 rule entries matching the 6 dimensional rules:
    - `seller_late_delivery_spike: {high: feishu_cli}`
    - `seller_review_score_drop: {medium: local_cli}`
    - `category_gmv_drop: {medium: feishu_cli}`
    - `category_low_review_cluster: {medium: local_cli}`
    - `region_cancel_rate_spike: {medium: manual}`
    - `region_late_delivery_spike: {high: feishu_cli}`
  - Add `defaults` section with fallback routing
  - Add comments explaining each channel's purpose

  **Must NOT do**:
  - Do NOT implement routing engine logic (no Python code that reads this config and routes)
  - Do NOT modify `dimensional_alert_rules.yml` (keep `target_channel` there as well for now)
  - Do NOT add rules beyond the existing 6

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Create new YAML file from existing data. Pure config extraction.
  - **Skills**: [`writing`]
    - `writing`: For clear channel routing documentation comments

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with Tasks 1, 2)
  - **Blocks**: None (config only, no code reads it)
  - **Blocked By**: None

  **References**:
  - `config/dimensional_alert_rules.yml:1-66` — Source of target_channel values per rule
  - `scripts/db_dimensional_rule_engine.py:202-218` — event_outbox INSERT with target_channel from rule config

  **Acceptance Criteria**:
  - [ ] `config/channel_routing_rules.yml` file exists
  - [ ] Contains `rules` section with 6 entries (one per dimensional rule)
  - [ ] Contains `defaults` section with fallback routing
  - [ ] All channel values are valid: `feishu_cli`, `local_cli`, `manual`
  - [ ] File is valid YAML (can be loaded by `yaml.safe_load`)

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: channel_routing_rules.yml is valid YAML with required structure
    Tool: Bash (python -c)
    Preconditions: File created at config/channel_routing_rules.yml
    Steps:
      1. python3 -c "
  import yaml
  r=yaml.safe_load(open('config/channel_routing_rules.yml'))
  assert 'rules' in r, 'Missing rules section'
  assert 'defaults' in r, 'Missing defaults section'
  assert len(r['rules']) == 6, f'Expected 6 rules, got {len(r[\"rules\"])}'
  valid_channels={'feishu_cli','local_cli','manual'}
  for rule_id, routing in r['rules'].items():
      for sev, channel in routing.items():
          assert channel in valid_channels, f'{rule_id}.{sev}: invalid channel {channel}'
  print('PASS: 6 rules, all channels valid')
  "
    Expected Result: "PASS: 6 rules, all channels valid"
    Failure Indicators: YAML parse error or assertion failure
    Evidence: .sisyphus/evidence/task-3-routing-config.txt
  ```

  **Commit**: YES (groups with Task 1, 2)
  - Message: `feat: add channel_routing_rules.yml config`
  - Files: `config/channel_routing_rules.yml`

- [ ] 4. Add `target_object_type`/`target_object_id` to `action_tasks` table schema

  **What to do**:
  - Create migration SQL: `sql/migrations/004_action_tasks_dimensional.sql`
  - SQL: `ALTER TABLE action_tasks ADD COLUMN target_object_type TEXT;` and `ALTER TABLE action_tasks ADD COLUMN target_object_id TEXT;`
  - Update `scripts/db_migrate.py` to include this migration (or create migration runner if needed)
  - Run migration: `python3 scripts/db_migrate.py` (or direct sqlite3 execution)
  - Verify columns exist: `sqlite3 data/olist_ops.db "PRAGMA table_info(action_tasks)" | grep target_object`
  - Update `tests/conftest.py` fixtures if needed to handle new columns

  **Must NOT do**:
  - Do NOT drop and recreate the table (use ALTER TABLE only)
  - Do NOT add constraints like NOT NULL (existing rows won't have these values)
  - Do NOT modify other tables' schemas

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Simple ALTER TABLE migration. Two columns, no data migration needed.
  - **Skills**: [`database-design`]
    - `database-design`: For correct SQLite ALTER TABLE syntax and migration pattern

  **Parallelization**:
  - **Can Run In Parallel**: YES (with Task 5)
  - **Parallel Group**: Wave 2
  - **Blocks**: Tasks 6, 7
  - **Blocked By**: None

  **References**:
  - `sql/migrations/003_dimensional_alerts.sql` — Existing migration pattern to follow
  - `sql/schema.sql:action_tasks` — Current action_tasks column definitions
  - `scripts/db_migrate.py` — Existing migration runner (if exists) or pattern to follow

  **Acceptance Criteria**:
  - [ ] `sql/migrations/004_action_tasks_dimensional.sql` exists with correct ALTER TABLE statements
  - [ ] `sqlite3 data/olist_ops.db "PRAGMA table_info(action_tasks)"` shows `target_object_type` and `target_object_id` columns
  - [ ] Migration is idempotent (running twice doesn't fail)
  - [ ] Existing data in action_tasks table is preserved

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: Migration adds target_object columns
    Tool: Bash (sqlite3)
    Preconditions: Fresh DB or existing DB with action_tasks table
    Steps:
      1. sqlite3 data/olist_ops.db "ALTER TABLE action_tasks ADD COLUMN target_object_type TEXT;" 2>/dev/null; sqlite3 data/olist_ops.db "ALTER TABLE action_tasks ADD COLUMN target_object_id TEXT;" 2>/dev/null
      2. sqlite3 data/olist_ops.db "PRAGMA table_info(action_tasks)" | grep -c "target_object"
    Expected Result: Output should show 2 (both columns present)
    Failure Indicators: grep returns 0 or 1
    Evidence: .sisyphus/evidence/task-4-schema-migration.txt

  Scenario: Migration is idempotent
    Tool: Bash (sqlite3)
    Preconditions: Columns already exist from first run
    Steps:
      1. sqlite3 data/olist_ops.db "ALTER TABLE action_tasks ADD COLUMN target_object_type TEXT;" 2>&1
    Expected Result: Should show "duplicate column name" error (or be handled gracefully by IF NOT EXISTS)
    Failure Indicators: Different error or crash
    Evidence: .sisyphus/evidence/task-4-idempotent.txt
  ```

  **Commit**: YES
  - Message: `feat: add target_object columns to action_tasks schema`
  - Files: `sql/migrations/004_action_tasks_dimensional.sql`, `scripts/db_migrate.py`

- [ ] 5. Align `db_export_feishu.py` CSV column names with `feishu_base_schema.yml` field_ids

  **What to do**:
  - Read `feishu_base_schema.yml` to extract the canonical `field_id` for each field
  - Compare with current `db_export_feishu.py` EXPORT_SPECS column names
  - **Direction decision**: Rename CSV export columns to match schema field_ids (NOT the reverse). Schema is the source of truth because `sync_feishu_bitable.py` reads CSV column names directly as Feishu API record keys.
  - Fix mismatches in `alert_events`: current `event_id` → `alert_id` (matches schema PK), `event_date` → `simulated_date`(? check schema), ensure all column names match schema field_ids exactly
  - Fix mismatches in `strategy_recommendations`: `strategy_title` → `title`(?), `strategy_detail` → `detail`(?), check all names
  - Fix mismatches in `action_tasks`: `task_title` → `title`, `task_description` → `description`, `task_source` → keep or rename based on schema
  - Fix mismatches in `review_retro`: check `actual_result` vs `outcome`, etc.
  - Update `TABLE_MAP` if SQLite table names differ from CSV names (currently maps CSV filename to SQLite table)

  **Must NOT do**:
  - Do NOT modify schema field_ids to match CSV (wrong direction)
  - Do NOT change the data content, only column header names
  - Do NOT remove any columns

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Column name remapping in a single file. Clear mapping table required.
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with Task 4)
  - **Parallel Group**: Wave 2
  - **Blocks**: Tasks 10, 14
  - **Blocked By**: Tasks 1, 2

  **References**:
  - `scripts/db_export_feishu.py:19-52` — EXPORT_SPECS with current column names
  - `config/feishu_base_schema.yml:55-106` — alert_events field_ids (authority for CSV column names)
  - `config/feishu_base_schema.yml:154-206` — recommendations field_ids
  - `config/feishu_base_schema.yml:107-152` — action_tasks field_ids
  - `scripts/sync_feishu_bitable.py:101-119` — records_to_dicts(): CSV column names used directly as Feishu record keys

  **Acceptance Criteria**:
  - [ ] Every column name in EXPORT_SPECS matches exactly one `field_id` in `feishu_base_schema.yml`
  - [ ] `python3 scripts/db_export_feishu.py --all` produces CSVs with updated headers
  - [ ] Alert events CSV header includes `alert_id` (renamed from `event_id`), plus all dimensional columns
  - [ ] `python3 scripts/sync_feishu_bitable.py --all --dry-run` passes with new column names

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: All CSV column names match schema field_ids
    Tool: Bash (python -c)
    Preconditions: Updated db_export_feishu.py and feishu_base_schema.yml exist
    Steps:
      1. python3 scripts/db_export_feishu.py --all
      2. python3 -c "
  import csv, yaml
  schema = yaml.safe_load(open('config/feishu_base_schema.yml'))
  # Build map of table_id -> {field_id, ...}
  schema_fields = {}
  for t in schema['tables']:
      schema_fields[t['table_id']] = {f['field_id'] for f in t['fields']}
  # Check each CSV
  import os, sys
  sys.path.insert(0, 'scripts'); import config
  for fname in ['alert_events_for_feishu.csv', 'strategy_recommendations_for_feishu.csv', 'action_tasks_for_feishu.csv']:
      path = os.path.join(config.FEISHU_DIR, fname)
      with open(path) as f:
          reader = csv.reader(f)
          headers = next(reader)
      table_id = fname.replace('_for_feishu.csv', '')
      if table_id == 'alert_events': table_id = 'alert_events'
      # Map CSV name to schema table_id
      mapping = {'daily_metrics_for_feishu.csv': 'daily_metrics', 'alert_events_for_feishu.csv': 'alert_events',
                 'strategy_recommendations_for_feishu.csv': 'recommendations',
                 'action_tasks_for_feishu.csv': 'action_tasks',
                 'review_retro_for_feishu.csv': 'review_retro'}
      s_table_id = mapping.get(fname)
      if s_table_id:
          expected = schema_fields.get(s_table_id, set())
          csv_set = set(headers)
          extra = csv_set - expected
          missing = expected - csv_set - {'_record_id', '_table', '_pulled_at'}  # metadata fields
          issues = []
          if extra: issues.append(f'Extra CSV columns not in schema: {extra}')
          if missing: issues.append(f'Missing CSV columns in schema: {missing}')
          status = 'PASS' if not issues else 'FAIL'
          print(f'{fname}: {status}')
          if issues:
              for i in issues: print(f'  {i}')
          else:
              print(f'  All {len(csv_set)} columns match schema')
  "
    Expected Result: All CSVs show "PASS"
    Failure Indicators: Any CSV shows "FAIL" with extra/missing columns
    Evidence: .sisyphus/evidence/task-5-column-alignment.txt
  ```

  **Commit**: YES (groups with Task 4)
  - Message: `fix: align db_export CSV columns with schema field_ids`
  - Files: `scripts/db_export_feishu.py`
  - ⚠️ **Commit ordering**: This commit (5) comes before commit (7) which also modifies `db_export_feishu.py`. Execute sequentially within Wave 2/3 to avoid merge conflicts.

- [ ] 6. Wire `action_templates.yml` into `db_dimensional_rule_engine.py`

  **What to do**:
  - Import template loading functions from `scripts/db_generate_recommendations.py`:
    - `load_dimensional_templates()` — loads and returns templates from YAML
    - `RULE_TO_TEMPLATE` — mapping dict from rule_id to template name
    - `_render_template(template, context)` — `string.Template.safe_substitute()`
  - In `db_dimensional_rule_engine.py` `run_engine()`, after alert generation loop (around line 156):
    - Load templates: `templates = load_dimensional_templates()`
    - For each non-suppressed alert, look up template by `rule_id` via `RULE_TO_TEMPLATE`
    - Build context dict from alert event: `{object_id, current_value, sample_size, affected_gmv, rule_id, metric_name, baseline_value, change_rate}`
    - Render `strategy_detail_template` using `_render_template()` → replace inline f-string at line 172-174
    - Render `task_title` using template → replace inline f-string at line 193
  - Verify template rendering produces NO unresolved `${...}` in output
  - Populate `target_object_type` and `target_object_id` in:
    - `strategy_recommendations` INSERT (line 161-178) — values from `ev['object_type']`, `ev['object_id']`
    - `action_tasks` INSERT (line 185-198) — same values

  **Must NOT do**:
  - Do NOT modify template text content in `action_templates.yml`
  - Do NOT change the template variable substitution syntax
  - Do NOT create new template loading functions — reuse existing ones
  - Do NOT change the flow of alert → strategy → task → outbox generation

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: Cross-module refactoring with import dependencies, template rendering, context building. Non-trivial integration work.
  - **Skills**: [`writing`]
    - `writing`: Template context documentation and clear variable naming

  **Parallelization**:
  - **Can Run In Parallel**: NO (sequential due to dependency on Task 4)
  - **Parallel Group**: Wave 3 (sequential after Task 4, parallel with Task 7)
  - **Blocks**: Tasks 7, 11, 14
  - **Blocked By**: Task 4

  **References**:
  - `scripts/db_generate_recommendations.py` — Contains `load_dimensional_templates()`, `RULE_TO_TEMPLATE`, `_render_template()` — REUSE these, don't duplicate
  - `scripts/db_dimensional_rule_engine.py:156-198` — Current inline strategy_detail/task_title generation (to be replaced)
  - `scripts/db_dimensional_rule_engine.py:161-178` — strategy_recommendations INSERT (add target_object_type/id)
  - `scripts/db_dimensional_rule_engine.py:185-198` — action_tasks INSERT (add target_object_type/id)
  - `config/action_templates.yml:1-83` — Template definitions with ${var} syntax
  - `config/dimensional_alert_rules.yml:1-66` — rule_id → template name mapping

  **Acceptance Criteria**:
  - [ ] `db_dimensional_rule_engine.py` imports template functions from `db_generate_recommendations.py`
  - [ ] Generated `strategy_detail` text contains NO `${...}` unresolved placeholders
  - [ ] Generated `task_title` uses template rendering (not inline f-string)
  - [ ] `strategy_recommendations` rows have `target_object_type` and `target_object_id` populated
  - [ ] `action_tasks` rows have `target_object_type` and `target_object_id` populated
  - [ ] `python3 scripts/db_dimensional_rule_engine.py --mode full` completes without errors

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: Strategy text has no unresolved template variables
    Tool: Bash (python -c + sqlite3)
    Preconditions: Pipeline run with templates wired
    Steps:
      1. python3 scripts/run_db_pipeline.py --mode full --dimensional
      2. python3 -c "
  import sqlite3, re
  conn = sqlite3.connect('data/olist_ops.db')
  rows = conn.execute('SELECT recommendation_id, strategy_detail FROM strategy_recommendations LIMIT 20').fetchall()
  unresolved = []
  for rid, detail in rows:
      if detail and re.search(r'\$\{[^}]+\}', detail):
          unresolved.append(rid)
  if unresolved:
      print(f'FAIL: {len(unresolved)} records have unresolved placeholders: {unresolved[:5]}')
  else:
      print(f'PASS: {len(rows)} records checked, no unresolved placeholders')
  conn.close()
  "
    Expected Result: "PASS: N records checked, no unresolved placeholders"
    Failure Indicators: Any unresolved ${...} found
    Evidence: .sisyphus/evidence/task-6-template-resolution.txt

  Scenario: target_object columns populated in action_tasks
    Tool: Bash (sqlite3)
    Preconditions: Pipeline run complete
    Steps:
      1. sqlite3 data/olist_ops.db "SELECT COUNT(*) FROM action_tasks WHERE target_object_type IS NULL OR target_object_type = ''"
      2. sqlite3 data/olist_ops.db "SELECT COUNT(*) FROM action_tasks WHERE target_object_id IS NULL OR target_object_id = ''"
    Expected Result: Both return 0 (no nulls)
    Failure Indicators: Non-zero count of null/empty values
    Evidence: .sisyphus/evidence/task-6-target-object-populated.txt
  ```

  **Commit**: YES
  - Message: `refactor: wire action_templates into dimensional rule engine`
  - Files: `scripts/db_dimensional_rule_engine.py`

- [ ] 7. Add `target_object_type`/`target_object_id` to action_tasks export spec

  **What to do**:
  - In `db_export_feishu.py` EXPORT_SPECS, add `target_object_type` and `target_object_id` to action_tasks columns list
  - Ensure column names match the schema field_ids (from Task 2 fixes)
  - The DB already has these columns (from Task 4 migration) and the engine populates them (from Task 6)
  - Verify export CSV contains these two new columns with populated values

  **Must NOT do**:
  - Do NOT change other action_tasks columns
  - Do NOT add columns that don't exist in the DB table

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Two-line addition to an array. Straightforward.
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with Task 6, once both are unblocked)
  - **Parallel Group**: Wave 3
  - **Blocks**: Tasks 10, 14
  - **Blocked By**: Tasks 4, 6

  **References**:
  - `scripts/db_export_feishu.py:40-45` — action_tasks export spec
  - `sql/migrations/004_action_tasks_dimensional.sql` — DB columns to export
  - `config/feishu_base_schema.yml:107-152` — Schema field_ids for column names

  **Acceptance Criteria**:
  - [ ] `action_tasks_for_feishu.csv` header includes `target_object_type` and `target_object_id`
  - [ ] Exported values match DB values for both columns
  - [ ] At least 90% of exported rows have non-null target_object_type and target_object_id

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: action_tasks CSV has target_object columns
    Tool: Bash (python -c)
    Preconditions: Pipeline run + export complete
    Steps:
      1. python3 scripts/db_export_feishu.py --all
      2. python3 -c "
  import csv
  with open('data/feishu/action_tasks_for_feishu.csv') as f:
      reader = csv.reader(f)
      headers = next(reader)
      assert 'target_object_type' in headers, 'target_object_type missing from CSV headers'
      assert 'target_object_id' in headers, 'target_object_id missing from CSV headers'
      non_null = 0
      total = 0
      for row in reader:
          total += 1
          type_idx = headers.index('target_object_type')
          id_idx = headers.index('target_object_id')
          if row[type_idx] and row[id_idx]:
              non_null += 1
      pct = non_null/total*100 if total > 0 else 0
      assert pct >= 90, f'Only {pct:.1f}% of rows have target_object populated'
      print(f'PASS: {non_null}/{total} rows ({pct:.1f}%) have target_object populated')
  "
    Expected Result: "PASS: N/M rows (>=90.0%) have target_object populated"
    Failure Indicators: Missing columns or <90% populated
    Evidence: .sisyphus/evidence/task-7-action-tasks-export.txt
  ```

  **Commit**: YES (groups with Task 6)
  - Message: `fix: add target_object columns to action_tasks export`
  - Files: `scripts/db_export_feishu.py`

- [ ] 8. Create `scripts/db_import_feishu_status.py` — Feishu status → SQLite

  **What to do**:
  - Create new script that reads `data/ops/action_task_status_snapshot.csv` (produced by `pull_feishu_status.py`)
  - Parse the unioned CSV, split records by `_table` column (`action_tasks` vs `review_retro`)
  - For `action_tasks` records: match by `task_id`, UPDATE only `{status, feedback}` columns
  - For `review_retro` records: match by `review_id`, UPDATE only `{status, feedback}` columns
  - **HUMAN_FIELDS protection**: During import, ONLY update the human-editable fields. Never overwrite `status` if the local DB already has a human-set value (e.g., `in_progress` should not be overwritten by Feishu's `todo`)
  - Implementation pattern: if local `status NOT IN ('todo', 'new')`, skip update (human has already acted)
  - Log all changes to `data/system/feishu_sync_log.csv` with action type `import`
  - Support `--dry-run` (default) and `--apply` modes
  - Support `--db` flag for custom DB path

  **Must NOT do**:
  - Do NOT INSERT new records — only UPDATE existing ones
  - Do NOT overwrite human-set status values
  - Do NOT modify any columns other than `{status, feedback}`
  - Do NOT call real Feishu API (reads from local CSV only)

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: New script with DB operations, CSV parsing, HUMAN_FIELDS protection logic, dry-run/apply modes, audit logging.
  - **Skills**: []
    - Skills Evaluated but Omitted: `database-design` not needed — simple UPDATE operations

  **Parallelization**:
  - **Can Run In Parallel**: YES (with Task 9)
  - **Parallel Group**: Wave 4
  - **Blocks**: Tasks 12, 16
  - **Blocked By**: None

  **References**:
  - `scripts/pull_feishu_status.py:80-101` — CSV output format with `_table`, `_record_id` metadata columns
  - `scripts/sync_feishu_bitable.py:79-86` — HUMAN_FIELDS structure: `{"action_tasks": {"status", "feedback", "completed_at"}, "alert_events": {"status"}}`
  - `sql/schema.sql:action_tasks` — action_tasks table schema (know which columns exist)
  - `sql/schema.sql:review_retro` — review_retro table schema
  - `scripts/db_export_feishu.py:13-17` — DB_PATH pattern for --db flag
  - `scripts/db_export_feishu.py:84-101` — main() argparse pattern to follow

  **Acceptance Criteria**:
  - [ ] `scripts/db_import_feishu_status.py` exists and is executable
  - [ ] `--dry-run` (default) prints planned changes without modifying DB
  - [ ] `--apply` updates only `{status, feedback}` for matching records
  - [ ] Human-set status (`in_progress`, `done`) is NOT overwritten by import
  - [ ] Non-matching `task_id`/`review_id` are skipped (not inserted)
  - [ ] Changes are logged to `data/system/feishu_sync_log.csv`

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: Dry-run shows planned changes without modifying DB
    Tool: Bash
    Preconditions: action_task_status_snapshot.csv exists with at least 1 record, DB has matching task
    Steps:
      1. sqlite3 data/olist_ops.db "SELECT task_id, status FROM action_tasks LIMIT 1"  # Note current state
      2. python3 scripts/db_import_feishu_status.py --dry-run
      3. sqlite3 data/olist_ops.db "SELECT task_id, status FROM action_tasks LIMIT 1"  # Verify unchanged
    Expected Result: Dry-run logs planned changes. DB status is unchanged from before.
    Failure Indicators: DB modified during dry-run
    Evidence: .sisyphus/evidence/task-8-dry-run.txt

  Scenario: Human-set status is protected during import
    Tool: Bash (sqlite3 + python3)
    Preconditions: Task exists in both DB and snapshot CSV
    Steps:
      1. sqlite3 data/olist_ops.db "UPDATE action_tasks SET status='in_progress' WHERE task_id='<test_task_id>'"
      2. # Ensure CSV has status='todo' for same task_id
      3. python3 scripts/db_import_feishu_status.py --apply
      4. sqlite3 data/olist_ops.db "SELECT status FROM action_tasks WHERE task_id='<test_task_id>'"
    Expected Result: Status remains 'in_progress' (NOT overwritten to 'todo')
    Failure Indicators: Status changes to 'todo' (human edit lost)
    Evidence: .sisyphus/evidence/task-8-human-protection.txt
  ```

  **Commit**: YES (groups with Task 9)
  - Message: `feat: add db_import_feishu_status.py with HUMAN_FIELDS protection`
  - Files: `scripts/db_import_feishu_status.py`

- [ ] 9. Create `scripts/create_feishu_views.py` — YAML-driven Feishu view management

  **What to do**:
  - Create new script that reads view configurations from `config/feishu_base_schema.yml` (view_config sections) or a new dedicated config
  - Add 6 view definitions for v0.3.1:
    1. `high_impact_alerts` on alert_events: filter `status IN [new, investigating]`, sort `impact_score DESC`
    2. `seller_alerts` on alert_events: filter `object_type = seller`, sort `impact_score DESC`
    3. `category_alerts` on alert_events: filter `object_type = category`, sort `impact_score DESC`
    4. `region_alerts` on alert_events: filter `object_type = region`, sort `impact_score DESC`
    5. `high_confidence_strategies` on recommendations: filter `confidence IN [high, medium]`, sort `risk_level DESC, created_at DESC`
    6. `object_tasks` on action_tasks: filter `status != done`, sort `priority DESC, due_at ASC`
  - Support `--dry-run` (default): validate all field references exist in schema, print view specifications, exit 0
  - Support `--apply`: call Feishu API to create views (requires real credentials — but dry-run is the primary use for v0.3.1)
  - Validate that all filtered/sorted fields exist in their respective table schemas

  **Must NOT do**:
  - Do NOT call real Feishu API in default dry-run mode
  - Do NOT create views for tables other than: alert_events, recommendations, action_tasks
  - Do NOT modify existing views

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: New script with Feishu API integration, view config validation, dry-run/apply modes.
  - **Skills**: []
    - Skills Evaluated but Omitted: None

  **Parallelization**:
  - **Can Run In Parallel**: YES (with Task 8)
  - **Parallel Group**: Wave 4
  - **Blocks**: Task 13
  - **Blocked By**: Task 2

  **References**:
  - `config/feishu_base_schema.yml:104-105` — Existing view_config pattern (default_view)
  - `config/feishu_base_schema.yml:55-106` — alert_events fields (for view filter field validation)
  - `config/feishu_base_schema.yml:154-206` — recommendations fields
  - `config/feishu_base_schema.yml:107-152` — action_tasks fields
  - `scripts/sync_feishu_bitable.py:279-315` — argparse + dry-run pattern to follow
  - `scripts/feishu_client.py` — FeishuClient class for API calls (reuse for --apply mode)
  - `scripts/config.py` — Project path constants

  **Acceptance Criteria**:
  - [ ] `scripts/create_feishu_views.py` exists and is executable
  - [ ] `--dry-run` prints all 6 view specifications with filter/sort details
  - [ ] Dry-run validates that all referenced fields exist in schema
  - [ ] Dry-run exits 0 when all validations pass
  - [ ] Dry-run exits non-zero with clear error when a referenced field doesn't exist

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: Dry-run validates all 6 views and exits 0
    Tool: Bash
    Preconditions: Fixed feishu_base_schema.yml with all dimensional fields
    Steps:
      1. python3 scripts/create_feishu_views.py --dry-run
    Expected Result: Output shows 6 views with filter/sort specs. Exit code 0.
    Failure Indicators: Non-zero exit code or missing views
    Evidence: .sisyphus/evidence/task-9-dry-run-success.txt

  Scenario: Dry-run catches invalid field reference
    Tool: Bash
    Preconditions: create_feishu_views.py exists
    Steps:
      1. # Temporarily test with a non-existent field name to verify validation
      2. python3 -c "
  import yaml, sys
  # Load schema, try to reference a non-existent field
  s = yaml.safe_load(open('config/feishu_base_schema.yml'))
  ae = [t for t in s['tables'] if t['table_id']=='alert_events'][0]
  valid_fields = {f['field_id'] for f in ae['fields']}
  assert 'nonexistent_field' not in valid_fields, 'Test setup: field should not exist'
  print('PASS: validation logic can detect invalid fields')
  "
    Expected Result: "PASS: validation logic can detect invalid fields"
    Failure Indicators: Assertion error
    Evidence: .sisyphus/evidence/task-9-invalid-field.txt
  ```

  **Commit**: YES (groups with Task 8)
  - Message: `feat: add create_feishu_views.py with dry-run validation`
  - Files: `scripts/create_feishu_views.py`

- [ ] 10. Extend `test_db_feishu_export.py` — dimensional export tests

  **What to do** (TDD: Write tests FIRST, then verify they pass with implementation from previous waves):
  - Test: `alert_events_for_feishu.csv` contains header columns `object_type`, `object_id`, `affected_orders`, `affected_gmv`, `impact_score`
  - Test: All alert_events rows have non-null `object_type` and `object_id`
  - Test: `affected_orders`, `affected_gmv`, `impact_score` are numeric (not text/empty)
  - Test: `action_tasks_for_feishu.csv` contains header columns `target_object_type`, `target_object_id`
  - Test: All action_tasks rows have non-null `target_object_type` and `target_object_id` (or at least >90%)
  - Test: `strategy_recommendations_for_feishu.csv` contains `decision_source`, `target_object_type`, `target_object_id`
  - Test: All CSV column names match their corresponding schema field_ids (cross-reference with `feishu_base_schema.yml`)
  - Follow existing test patterns in `tests/test_db_feishu_export.py` (fixtures, assertions, conftest.py)

  **Must NOT do**:
  - Do NOT remove existing tests
  - Do NOT test Feishu API connectivity (unit tests only)

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Extending existing test file with data validation assertions. Follows established patterns.
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with Tasks 11, 12, 13)
  - **Parallel Group**: Wave 5
  - **Blocks**: Task 17
  - **Blocked By**: Tasks 5, 7

  **References**:
  - `tests/test_db_feishu_export.py` — Existing test patterns to follow/extend
  - `tests/conftest.py` — pytest fixtures (project_root, data_dir)
  - `scripts/db_export_feishu.py` — Export logic being tested
  - `config/feishu_base_schema.yml` — Schema field_ids for column validation

  **Acceptance Criteria**:
  - [ ] All new tests in `test_db_feishu_export.py` pass with `python -m pytest tests/test_db_feishu_export.py -v`
  - [ ] Tests verify dimensional column presence and data quality
  - [ ] Tests verify column-schema parity

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: All export tests pass
    Tool: Bash (pytest)
    Preconditions: All implementation complete from previous waves
    Steps:
      1. python -m pytest tests/test_db_feishu_export.py -v
    Expected Result: All tests PASS. No failures. No errors.
    Failure Indicators: Any test failure with details
    Evidence: .sisyphus/evidence/task-10-test-output.txt
  ```

  **Commit**: YES (groups with Tasks 11, 12, 13 in Wave 5)
  - Message: `test: extend feishu export tests for dimensional columns`
  - Files: `tests/test_db_feishu_export.py`

- [ ] 11. Extend `test_db_rule_engine.py` — template rendering + target_object tests

  **What to do** (TDD):
  - Test: Generated `strategy_detail` text contains NO `${...}` unresolved placeholders (regex check)
  - Test: Template rendering uses `string.Template.safe_substitute()` (verify no KeyError for missing vars)
  - Test: `strategy_recommendations` rows have non-null `target_object_type` and `target_object_id`
  - Test: `action_tasks` rows have non-null `target_object_type` and `target_object_id`
  - Test: `decision_source` is `'heuristic'` for all dimensional recommendations
  - Test: Template variable mapping is complete — all `${...}` in templates have corresponding keys in rule engine context dict
  - Test: Idempotency: running engine twice doesn't duplicate records (existing test may cover this)

  **Must NOT do**:
  - Do NOT remove existing tests
  - Do NOT test template text quality (only structural correctness)

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Extending existing test file with template validation assertions.
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with Tasks 10, 12, 13)
  - **Parallel Group**: Wave 5
  - **Blocks**: Task 17
  - **Blocked By**: Task 6

  **References**:
  - `tests/test_db_rule_engine.py` — Existing test patterns
  - `tests/test_db_dimensional_rules.py` — Existing dimensional rule tests to extend
  - `scripts/db_dimensional_rule_engine.py` — Implementation being tested
  - `config/action_templates.yml` — Template variables to validate

  **Acceptance Criteria**:
  - [ ] All new tests pass with `python -m pytest tests/test_db_rule_engine.py tests/test_db_dimensional_rules.py -v`
  - [ ] Template rendering tests verify no unresolved placeholders

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: All template/engine tests pass
    Tool: Bash (pytest)
    Preconditions: Template engine wired (Task 6 complete)
    Steps:
      1. python -m pytest tests/test_db_rule_engine.py tests/test_db_dimensional_rules.py -v
    Expected Result: All tests PASS
    Failure Indicators: Any test failure
    Evidence: .sisyphus/evidence/task-11-test-output.txt
  ```

  **Commit**: YES (groups with Tasks 10, 12, 13)
  - Message: `test: extend rule engine tests for template rendering and target_object`
  - Files: `tests/test_db_rule_engine.py`, `tests/test_db_dimensional_rules.py`

- [ ] 12. Create `tests/test_db_import_feishu_status.py` — import script tests

  **What to do** (TDD):
  - Test: `db_import_feishu_status.py --dry-run` exits 0 and prints planned changes
  - Test: Import updates only `{status, feedback}` columns for matching `task_id`
  - Test: Import does NOT insert new records (only UPDATE existing)
  - Test: Human-set status (`in_progress`) is NOT overwritten by Feishu `todo` status
  - Test: Non-matching `task_id` in CSV is skipped (no error, logged)
  - Test: Missing CSV file produces clear error message
  - Test: Empty CSV produces "0 records" output with exit 0
  - Test: `--apply` flag is required for real modifications

  **Must NOT do**:
  - Do NOT test with real Feishu API
  - Do NOT modify actual production DB in tests (use test fixtures or temp DB)

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Creating test file with predictable DB operations. Uses fixtures.
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with Tasks 10, 11, 13)
  - **Parallel Group**: Wave 5
  - **Blocks**: Task 17
  - **Blocked By**: Task 8

  **References**:
  - `scripts/db_import_feishu_status.py` — Script being tested
  - `tests/test_db_feishu_export.py` — Test pattern to follow (fixtures, assertions)
  - `tests/conftest.py` — Pytest fixtures (DB path, temp dirs)

  **Acceptance Criteria**:
  - [ ] All tests pass with `python -m pytest tests/test_db_import_feishu_status.py -v`
  - [ ] HUMAN_FIELDS protection test verifies no overwrite

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: All import tests pass
    Tool: Bash (pytest)
    Preconditions: db_import_feishu_status.py exists
    Steps:
      1. python -m pytest tests/test_db_import_feishu_status.py -v
    Expected Result: All tests PASS
    Failure Indicators: Any test failure
    Evidence: .sisyphus/evidence/task-12-test-output.txt
  ```

  **Commit**: YES (groups with Tasks 10, 11, 13)
  - Message: `test: add db_import_feishu_status tests with HUMAN_FIELDS protection`
  - Files: `tests/test_db_import_feishu_status.py`

- [ ] 13. Create `tests/test_feishu_views.py` — view creation tests

  **What to do** (TDD):
  - Test: `create_feishu_views.py --dry-run` exits 0 and prints all 6 view specifications
  - Test: View config references valid fields (all filter/sort fields exist in schema)
  - Test: Referencing a non-existent field causes validation error (non-zero exit)
  - Test: All 6 required views are present in dry-run output:
    - `high_impact_alerts`, `seller_alerts`, `category_alerts`, `region_alerts`
    - `high_confidence_strategies`, `object_tasks`
  - Test: View filter conditions use valid operators

  **Must NOT do**:
  - Do NOT test Feishu API calls (unit tests only)
  - Do NOT create real views in Feishu

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Creating test file for CLI script validation.
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with Tasks 10, 11, 12)
  - **Parallel Group**: Wave 5
  - **Blocks**: Task 17
  - **Blocked By**: Task 9

  **References**:
  - `scripts/create_feishu_views.py` — Script being tested
  - `config/feishu_base_schema.yml` — Schema for field validation
  - `tests/test_db_feishu_export.py` — Test pattern to follow

  **Acceptance Criteria**:
  - [ ] All tests pass with `python -m pytest tests/test_feishu_views.py -v`
  - [ ] Test verifies all 6 views are defined

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: All view tests pass
    Tool: Bash (pytest)
    Preconditions: create_feishu_views.py exists
    Steps:
      1. python -m pytest tests/test_feishu_views.py -v
    Expected Result: All tests PASS
    Failure Indicators: Any test failure
    Evidence: .sisyphus/evidence/task-13-test-output.txt
  ```

  **Commit**: YES (groups with Tasks 10, 11, 12)
  - Message: `test: add create_feishu_views dry-run validation tests`
  - Files: `tests/test_feishu_views.py`

- [ ] 14. Run full pipeline + verify data completeness

  **What to do**:
  - Execute: `python3 scripts/run_db_pipeline.py --mode full --dimensional`
  - Verify each step completes without error
  - Check output row counts:
    - `metric_dimension_daily`: has data (seller/category/region rows)
    - `alert_events`: at least 35 dimensional alerts
    - `strategy_recommendations`: at least 35
    - `action_tasks`: at least 35
    - `event_outbox`: has `feishu_cli`/`manual`/`local_cli` channel distribution
  - Execute: `python3 scripts/db_export_feishu.py --all`
  - Verify 5 CSV files produced in `data/feishu/`
  - Verify `alert_events_for_feishu.csv` has expected headers and data
  - Verify `action_tasks_for_feishu.csv` has `target_object_type`/`target_object_id` columns populated

  **Must NOT do**:
  - Do NOT call real Feishu API sync
  - Do NOT modify pipeline scripts

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: Pipeline execution + multi-step verification with data inspection.
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with Tasks 15, 16)
  - **Parallel Group**: Wave 6
  - **Blocks**: Task 17
  - **Blocked By**: Tasks 5, 6, 7

  **References**:
  - `scripts/run_db_pipeline.py` — Pipeline orchestrator
  - `scripts/db_export_feishu.py` — Feishu CSV export
  - `scripts/db_status.py` — DB status reporting (if available)
  - `README_RUNBOOK.md:60-63` — Full pipeline invocation pattern

  **Acceptance Criteria**:
  - [ ] Pipeline completes ALL STEPS without error
  - [ ] `metric_dimension_daily` table has data
  - [ ] `alert_events` has ≥35 dimensional alerts
  - [ ] `strategy_recommendations` has ≥35 records
  - [ ] `action_tasks` has ≥35 records
  - [ ] `event_outbox` has records with 3+ different `target_channel` values
  - [ ] All 5 CSV files in `data/feishu/` have content (not just headers)

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: Pipeline completes with dimensional data
    Tool: Bash
    Preconditions: All implementation from previous waves complete
    Steps:
      1. python3 scripts/run_db_pipeline.py --mode full --dimensional 2>&1 | tail -20
      2. python3 scripts/db_status.py 2>/dev/null || sqlite3 data/olist_ops.db "SELECT 'alert_events:', COUNT(*) FROM alert_events; SELECT 'strategy_recs:', COUNT(*) FROM strategy_recommendations; SELECT 'action_tasks:', COUNT(*) FROM action_tasks; SELECT 'outbox:', COUNT(*) FROM event_outbox; SELECT 'dim_metrics:', COUNT(DISTINCT dimension_type) FROM metric_dimension_daily;"
    Expected Result: Pipeline logs "ALL STEPS COMPLETED". DB status shows alert_events≥35, strategy_recs≥35, action_tasks≥35, outbox>0, dim_metrics has 3 dimension types
    Failure Indicators: Pipeline step failure or low row counts
    Evidence: .sisyphus/evidence/task-14-pipeline-output.txt
  ```

  **Commit**: NO (execution only, no code changes)
  - Pre-commit: `python -m pytest tests/ -v`

- [ ] 15. Sample strategy & task quality assessment

  **What to do**:
  - From generated data, sample 10 alert_events, 10 strategy_recommendations, 10 action_tasks, 10 event_outbox entries
  - **Strategy scoring** (each out of 10): Check for: event_id present, target_object_type, target_object_id, trigger rule explained, metric evidence, impact scope, clear action suggested, owner role assigned, success metric defined, no generic/vague language
  - **Task scoring** (each out of 8): Check for: source event/strategy linked, target object specified, clear action, owner role, priority, due date, executable by human operator, non-duplicate
  - **Outbox routing check** (each out of 10): Check for: target_channel reasonable, payload_json parseable, payload has source_id, payload has recommended_action, channel assignment logical given severity/context
  - Calculate average scores. Flag low-quality items with specific issues.
  - Document findings for acceptance report

  **Must NOT do**:
  - Do NOT modify data to improve scores
  - Do NOT make subjective judgments — use checklist criteria
  - Do NOT skip low-scoring items — flag them honestly

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: Systematic sampling and scoring against defined rubrics. Requires judgment but following explicit criteria.
  - **Skills**: [`qa-expert`]
    - `qa-expert`: For structured quality assessment with scoring rubrics

  **Parallelization**:
  - **Can Run In Parallel**: YES (with Tasks 14, 16)
  - **Parallel Group**: Wave 6
  - **Blocks**: Task 17
  - **Blocked By**: Task 14 (needs data)

  **References**:
  - `config/action_templates.yml` — Template quality baseline for comparison
  - `config/dimensional_alert_rules.yml` — Rules to verify against
  - `sql/schema.sql:alert_events` — Expected fields for completeness check

  **Acceptance Criteria**:
  - [ ] Strategy sample average score ≥ 8/10
  - [ ] Task sample average score ≥ 6.5/8
  - [ ] Outbox routing reasonable rate ≥ 80% (8/10)
  - [ ] Sampling report saved with itemized scores

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: Strategy quality sampling meets threshold
    Tool: Bash (python -c)
    Preconditions: Generated data from Task 14
    Steps:
      1. python3 -c "
  import sqlite3, json, random
  conn = sqlite3.connect('data/olist_ops.db')
  rows = conn.execute('SELECT * FROM strategy_recommendations ORDER BY RANDOM() LIMIT 10').fetchall()
  cols = [d[0] for d in conn.execute('PRAGMA table_info(strategy_recommendations)').fetchall()]
  scores = []
  for row in rows:
      r = dict(zip(cols, row))
      score = 0
      if r.get('event_id'): score += 1
      if r.get('target_object_type'): score += 1
      if r.get('target_object_id'): score += 1
      if r.get('rule_id'): score += 1
      if r.get('strategy_detail') and len(r['strategy_detail'])>50: score += 1
      if r.get('expected_impact'): score += 1
      if r.get('owner_role'): score += 1
      if r.get('success_metric'): score += 1
      if r.get('decision_source') == 'heuristic': score += 1
      detail = r.get('strategy_detail','')
      if detail and len(detail.split()) > 10 and '?' not in detail: score += 1  # substantive, not vague
      scores.append(score)
  avg = sum(scores)/len(scores)
  print(f'Strategy sample: {len(scores)} items, avg score: {avg:.1f}/10')
  print(f'Individual scores: {scores}')
  print(f'THRESHOLD: 8.0')
  print(f'RESULT: {\"PASS\" if avg >= 8.0 else \"FAIL\"} ')
  conn.close()
  "
    Expected Result: "PASS" with avg ≥ 8.0
    Failure Indicators: "FAIL" with avg < 8.0 — individual low scores shown
    Evidence: .sisyphus/evidence/task-15-strategy-sample.txt
  ```

  **Commit**: NO (assessment only)
  - Output: `.sisyphus/evidence/task-15-quality-sample.md`

- [ ] 16. Status pullback round-trip test

  **What to do**:
  - Select 3 tasks from action_tasks: 1 seller task, 1 category task, 1 region task
  - Manually modify their status in the DB: `UPDATE action_tasks SET status='in_progress', feedback='已开始排查' WHERE ...`
  - Simulate pullback: `python3 scripts/pull_feishu_status.py --dry-run` (since no real Feishu, manually create a test CSV snapshot)
  - Execute import: `python3 scripts/db_import_feishu_status.py --apply` with test CSV
  - Verify the 3 human-modified statuses were NOT overwritten (HUMAN_FIELDS protection)
  - Run sync again: `python3 scripts/sync_feishu_bitable.py --table action_tasks --apply` (dry-run equivalent)
  - Verify human-edited status is still preserved after re-sync

  **Must NOT do**:
  - Do NOT call real Feishu API
  - Do NOT use `--apply` with real Feishu credentials
  - Do NOT modify the HUMAN_FIELDS protection logic

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: Multi-step round-trip testing with DB manipulation, CSV creation, and verification.
  - **Skills**: []

  **Parallelization**:
  - **Can Run In Parallel**: YES (with Tasks 14, 15)
  - **Parallel Group**: Wave 6
  - **Blocks**: Task 17
  - **Blocked By**: Task 8

  **References**:
  - `scripts/sync_feishu_bitable.py:79-86` — HUMAN_FIELDS definition
  - `scripts/db_import_feishu_status.py` — Import script being tested
  - `scripts/sync_feishu_bitable.py:198-202` — HUMAN_FIELDS stripping logic

  **Acceptance Criteria**:
  - [ ] 3 tasks manually set to `in_progress` with feedback
  - [ ] After import, all 3 remain `in_progress` (not overwritten)
  - [ ] After re-sync dry-run, status still preserved
  - [ ] `data/ops/action_task_status_snapshot.csv` exists or test CSV created

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: Human status preserved through pullback cycle
    Tool: Bash (sqlite3)
    Preconditions: 3 tasks with modified status
    Steps:
      1. sqlite3 data/olist_ops.db "SELECT task_id, status, feedback FROM action_tasks WHERE status='in_progress' LIMIT 3"
      2. # Simulate import cycle
      3. sqlite3 data/olist_ops.db "SELECT task_id, status FROM action_tasks WHERE task_id IN (SELECT task_id FROM action_tasks WHERE status='in_progress' LIMIT 3)"
    Expected Result: All 3 tasks still show status='in_progress'
    Failure Indicators: Any task shows status='todo' (overwritten)
    Evidence: .sisyphus/evidence/task-16-round-trip.txt
  ```

  **Commit**: NO (test only, no code changes)

- [ ] 17. Generate v0.3.1 acceptance report

  **What to do**:
  - Create `.sisyphus/v0.3.1-acceptance.md` with the following sections:
    1. **数据同步结果**: Row counts for all 5 tables, pipeline completion status
    2. **飞书表字段校准**: Per-table field completeness checklist (alert_events, strategy_recommendations, action_tasks)
    3. **看板与视图校准**: 6 views defined and validated
    4. **策略质量抽样**: Sample size, average score, low-score strategy issues, template fix recommendations
    5. **任务质量抽样**: Sample size, average score, duplicate/inexecutable task flags
    6. **Outbox 分流检查**: Channel distribution counts, routing reasonable rate
    7. **状态回流测试**: Manual modification count, pullback success, overwrite protection confirmed
    8. **结论**: Pass/fail for all 11 acceptance criteria. Whether v0.3.1 passes. Whether it's ready for v0.4.
  - Include the 10 acceptance criteria from the spec section 9 (plus AC11 for template quality)
  - Reference evidence files from Tasks 14-16
  - Use pass/fail table format with evidence column

  **Must NOT do**:
  - Do NOT fabricate results — use actual data from pipeline execution
  - Do NOT claim pass if any criterion fails
  - Do NOT skip failed criteria — document them honestly with remediation notes

  **Recommended Agent Profile**:
  - **Category**: `writing`
    - Reason: Structured report generation from collected evidence. Documentation.
  - **Skills**: [`writing`]
    - `writing`: For clear, concise acceptance report with pass/fail tables

  **Parallelization**:
  - **Can Run In Parallel**: NO (sequential — needs all prior task evidence)
  - **Parallel Group**: Wave 7
  - **Blocks**: F1-F4
  - **Blocked By**: Tasks 10-16

  **References**:
  - `reports/v0.2_acceptance_report.md` — Acceptance report format to follow
  - User spec section 8-9 — Required report structure and acceptance criteria
  - `.sisyphus/evidence/` — Evidence files from Tasks 14-16

  **Acceptance Criteria**:
  - [ ] `.sisyphus/v0.3.1-acceptance.md` exists
  - [ ] All 8 sections present with data (not placeholders)
  - [ ] 11 acceptance criteria evaluated with pass/fail
  - [ ] Evidence references are valid (files exist)
  - [ ] Conclusion explicitly states whether v0.3.1 passes

  **QA Scenarios (MANDATORY)**:

  ```
  Scenario: Acceptance report exists with complete structure
    Tool: Bash
    Preconditions: All prior tasks complete
    Steps:
      1. ls -la .sisyphus/v0.3.1-acceptance.md
      2. grep -c "^## " .sisyphus/v0.3.1-acceptance.md
    Expected Result: File exists. At least 8 sections (## headers).
    Failure Indicators: File missing or fewer than 8 sections
    Evidence: .sisyphus/evidence/task-17-report-exists.txt

  Scenario: All 11 acceptance criteria evaluated
    Tool: Bash (grep)
    Preconditions: Report exists
    Steps:
      1. grep -E "(PASS|FAIL)" .sisyphus/v0.3.1-acceptance.md | wc -l
    Expected Result: At least 11 lines with PASS or FAIL
    Failure Indicators: Fewer than 11 criteria evaluated
    Evidence: .sisyphus/evidence/task-17-criteria-count.txt
  ```

  **Commit**: YES
  - Message: `docs: generate v0.3.1 acceptance report`
  - Files: `.sisyphus/v0.3.1-acceptance.md`

---

## Final Verification Wave

- [ ] F1. **Plan Compliance Audit** — `oracle`
  Read the plan end-to-end. For each "Must Have": verify implementation exists (read file, run command). For each "Must NOT Have": search codebase for forbidden patterns — reject with file:line if found. Check evidence files exist in `.sisyphus/evidence/`. Compare deliverables against plan.
  Output: `Must Have [N/N] | Must NOT Have [N/N] | Tasks [N/N] | VERDICT: APPROVE/REJECT`

- [ ] F2. **Code Quality Review** — `unspecified-high`
  Run `python -m pytest tests/ -v`. Review all changed files for: bare excepts, console.log in prod, commented-out code, unused imports. Check AI slop: excessive comments, over-abstraction, generic names (data/result/item/temp).
  Output: `Tests [N pass/N fail] | Files [N clean/N issues] | VERDICT`

- [ ] F3. **Real Manual QA** — `unspecified-high`
  Start from clean state. Execute EVERY QA scenario from EVERY task — follow exact steps, capture evidence. Test cross-task integration (features working together, not isolation). Test edge cases: empty state, invalid input, rapid actions. Save to `.sisyphus/evidence/final-qa/`.
  Output: `Scenarios [N/N pass] | Integration [N/N] | Edge Cases [N tested] | VERDICT`

- [ ] F4. **Scope Fidelity Check** — `deep`
  For each task: read "What to do", read actual diff (git log/diff). Verify 1:1 — everything in spec was built (no missing), nothing beyond spec was built (no creep). Check "Must NOT do" compliance. Detect cross-task contamination: Task N touching Task M's files. Flag unaccounted changes.
  Output: `Tasks [N/N compliant] | Contamination [CLEAN/N issues] | Unaccounted [CLEAN/N files] | VERDICT`

---

## Commit Strategy

| # | Message | Files | Depends |
|---|---------|-------|---------|
| 1 | `fix: align feishu_field_mapping keys with VALID_TABLES` | config/feishu_field_mapping.yml | — |
| 2 | `fix: add missing dimensional fields to feishu_base_schema` | config/feishu_base_schema.yml | — |
| 3 | `feat: add channel_routing_rules.yml config` | config/channel_routing_rules.yml | — |
| 4 | `feat: add target_object columns to action_tasks schema` | sql/migrations/004_action_tasks_dimensional.sql, scripts/db_migrate.py | — |
| 5 | `fix: align db_export CSV columns with schema field_ids` | scripts/db_export_feishu.py | 1, 2 |
| 6 | `refactor: wire action_templates into dimensional rule engine` | scripts/db_dimensional_rule_engine.py | 4 |
| 7 | `fix: add target_object columns to action_tasks export` | scripts/db_export_feishu.py | 4, 6 |
| 8 | `feat: add db_import_feishu_status.py with HUMAN_FIELDS` | scripts/db_import_feishu_status.py | — |
| 9 | `feat: add create_feishu_views.py with dry-run` | scripts/create_feishu_views.py | 2 |
| 10 | `test: add tests for template engine, export, import, views` | tests/test_db_*.py | 6-9 |
| 11 | `docs: generate v0.3.1 acceptance report` | .sisyphus/v0.3.1-acceptance.md | 10 |

---

## Success Criteria

### Verification Commands
```bash
# 全管道执行
python3 scripts/run_db_pipeline.py --mode full --dimensional
# Expected: ALL STEPS COMPLETED (all 9 steps pass)

# 导出验证
python3 scripts/db_export_feishu.py --all
# Expected: 5 CSVs exported with correct headers

# 测试验证
python -m pytest tests/ -v
# Expected: ALL tests pass, ≥80% coverage on changed code

# Dry-run 验证
python3 scripts/sync_feishu_bitable.py --all --dry-run
python3 scripts/pull_feishu_status.py --dry-run
python3 scripts/db_import_feishu_status.py --dry-run
python3 scripts/create_feishu_views.py --dry-run
# Expected: ALL dry-run pass without errors
```

### Final Checklist
- [ ] All "Must Have" present (7 items)
- [ ] All "Must NOT Have" absent (9 items)
- [ ] 11 acceptance criteria in report all PASS
- [ ] All pytest tests pass
- [ ] v0.3.1 acceptance report exists at `.sisyphus/v0.3.1-acceptance.md`
